# 变更记录

本文件记录 MediaHub 的版本变更。格式参考 Keep a Changelog，版本号遵循语义化
版本。

## [未发布]

### 进行中

- v0.2 URL 直播播放（2026-08-05 至 2026-08-06）
  - “文件 → 打开网络地址”支持用户自带的 `http`、`https`、`rtsp`、
    `rtmp`、`rtmps`、`udp`、`rtp` 和 `srt` 地址。
  - 完整 URL 仅在当前进程内播放；列表、界面错误和日志不记录凭据、
    查询参数或片段。
  - 第一增量已使用本机 HTTP MPEG-TS 完成真实画面和声音验收。
  - 第二增量增加可见缓冲百分比、连接期间立即取消、15 秒打开超时、
    可重试界面和网络失败中文映射。
  - 取消或超时后的迟到事件不得覆盖界面；移除正在连接的最后一项会
    同步停止超时计时器。
  - 修复 libVLC 3 在正常直播切换到慢连接时同步停止导致的界面卡死；控制命令
    改为专用线程串行执行，网络取消通过替换播放器立即隔离旧流。
  - 第二增量 Debug CTest `49/49`、核心独立 CTest `33/33` 通过；用户
    已确认正常直播→慢连接→取消→恢复正常直播全流程不再卡死。
  - 第三增量区分缓冲与真实断流；已播放直播失败或结束后最多按 1、2、4 秒
    自动重连 3 次，手动停止、媒体切换和关闭会取消旧地址重连。
  - 修复真实 HTTP MPEG-TS 停止输入后 libVLC 可能长期保持 `Playing`、不发送失败或
    结束事件的问题；内核每 250ms 采样输入、解复用和音视频活动统计，Presenter
    每 500ms 检查一次，连续 15 秒无活动时进入同一有界重连流程。网络播放位置持续
    前进时也作为活动兜底，兼容 HTTP-FLV 的 libVLC 统计短暂冻结；从未提供统计或
    位置活动的协议仍不猜测断流。
  - 本次会话最近使用的 10 个地址按最近优先去重，仅显示在可编辑输入下拉框，
    不写入设置或日志。
  - 本机可控直播已验证无终止事件断流后自动恢复音画、三次耗尽、手动重试、停止和
    本地媒体切换取消、最近地址下拉框及重启清空；旧直播恢复后不会抢回本地歌曲。
  - 最终 Debug/Release 全量 CTest 均为 `49/49`，核心独立 CTest 为 `33/33`；
    `MEDIAHUB_BUILD_TESTS=OFF` 构建、修改文件格式检查和空白检查均通过。
  - 第四增量完成真实直播画面与声音、音量与静音、暂停继续、停止、本地 MP3/MP4
    往返、歌词、波形、进度定位、全屏、最近地址、15 秒慢连接超时、重试取消和
    播放中安全关闭的完整实机回归；项目版本元数据更新为 `0.2.0`。
  - 公网 HLS/HTTP-FLV 兼容复验发现，部分直播会报告可定位窗口或在播放正常时冻结
    libVLC 活动统计。网络媒体现始终显示 `--:--` 并禁用进度定位；曾将活动确认窗口
    延长到 15 秒并加入位置兜底，用于确认自动判断仍会受不同协议行为影响。
  - 修复旧直播后台停止可能覆盖新媒体音频状态的问题：跨媒体切换会创建独立的
    libVLC 实例，旧播放器在后台关闭音视频轨并停止；新播放器继承音量和静音状态，
    进入 `Playing` 后再次应用。公网直播 A→直播 B→本地歌曲实机往返无需调节音量
    即可立即听到声音。
  - 根据实机体验撤销所有自动重连和活动停滞推断。网络直播新增“刷新直播”按钮与
    `F5` 快捷键，播放、缓冲、失败或用户观察到普通画面停顿时均可主动重开当前地址；
    明确失败或结束只提示“直播已断开，请点击刷新”。
  - 手动刷新复用当前网络列表项，不新增列表记录或最近地址；刷新仍使用独立 libVLC
    实例隔离旧直播并保留音量、静音和全屏状态。本地媒体不显示刷新按钮，按 `F5`
    不执行任何播放命令。
  - 最终手动刷新版本 Debug/Release 全量 CTest 均为 `50/50`，分别用时约 18.87 秒
    和 15.83 秒；核心独立 CTest `33/33` 通过，用时约 0.69 秒；无测试构建成功且
    CTest 为 0，格式、空白和依赖边界扫描通过。
  - 用户在最新 Release 中确认按钮与 `F5` 均能重新恢复真实直播音画；刷新后直接
    切回本地歌曲立即有声音，无需调节音量。
  - 单 URL 直播四个增量现已封存；第五增量在纯核心层增加 `LiveChannel`、
    `LiveChannelLibrary` 以及 UTF-8 M3U 解析和序列化。
  - M3U 支持 BOM、CRLF/LF、中文、分类、EPG/Logo 元数据和稳定顺序；非法、不完整和
    重复地址使用不携带完整 URL 的结构化诊断，序列化遇到不安全模型不会返回半成品。
  - 新增 17 个 M3U 单元场景；第五增量 Debug/Release 全量 CTest 均为 `67/67`，
    Core-only `50/50`，无测试构建成功且 CTest 为 0。
  - GUI 测试的歌词校准设置改写入临时目录，不再读取、写入或污染真实用户配置；
    产品运行时设置路径不变。
  - 补充单 URL 稳定后的执行路线：纯核心 M3U、本地频道管理、频道播放、
    导入/重新加载/导出和完整回归；远程订阅、Logo 与 EPG 后置。

### 已封存

- 阶段 12：v0.1 Release 交付（2026-08-04）
  - 新增 `MEDIAHUB_PACKAGE_RELEASE` CMake 安装规则，生成只包含正式程序、Qt 运行库、
    libVLC 运行库与插件、许可证材料的独立 Windows x64 发布目录。
  - 新增发布包结构校验和隔离播放脚本；拒绝静态库、PDB、测试程序、Debug DLL 和
    VC++ 运行库安装器混入发布包。
  - 空目录 Debug/Release 构建均完成，CTest 均为 `44/44`；Release GUI 套件连续
    5 轮通过。
  - 当前发布目录共 `396` 个文件、约 `185.86 MiB`，含 `365` 个 VLC DLL 插件；
    清空 Qt/VLC 环境变量并缩减 PATH 后可播放、正常关闭且退出码为 `0`。
  - 生成 78.91 MiB 的 Windows x64 ZIP 并记录 SHA-256；用户确认强制 125%/150%
    发布版布局正常，最终交付候选清单明确保留第二台机器和系统级 DPI 复测项。
  - 用户接受上述未覆盖项作为本地自用限制，阶段 12 按本地交付封存；v0.2 仅在用户
    后续明确通知后开始，不自动进入。

### 已完成

- 在线歌词增量（2026-08-04）
  - 音量按钮右侧新增圆形“词”按钮，纯音频可在真实波形与歌词页之间切换。
  - 支持 ID3 标题、艺术家、专辑和 `USLT` 内嵌歌词，文件名作为标签缺失时的降级。
  - 在线查询按酷狗、网易云、LRCLIB、TheAudioDB 顺序降级，并使用标题、艺术家、
    专辑和时长共同匹配，避免盲目采用第一条同名结果。
  - 同步歌词随播放位置高亮当前行，普通歌词保留全文滚动；查询异步、可取消且有
    6 秒超时，不阻塞播放线程。
  - 查询结果缓存到 Qt 应用缓存目录，不修改媒体文件，不向服务端发送本地完整路径。
  - Qt 缺少 OpenSSL 1.1 时自动使用网易云 HTTP，并跳过无法建立 HTTPS 的备用源。
  - 修复本地与曲库演唱者署名不同导致歌词被拒绝的问题；只有核心标题相同且时长
    相差不超过 5 秒才降低歌手不匹配惩罚，避免误配其他同名歌曲。
  - 酷狗优先按歌曲分组和音频条目哈希查找同步歌词，并根据显示标题排除 DJ、Live、
    Cover、纯音乐、伴奏等录音变体；失败后继续使用原有歌词源。
  - 歌词页增加单曲 `-0.5s`、`+0.5s` 和重置校准；偏移使用歌曲信息 SHA-256 键保存
    到用户 INI 配置，不记录本地完整路径，也不影响其他歌曲。
  - 同步歌词、普通歌词、文件名、来源标签和时间校准控件按实际歌词区域宽高响应式
    缩放；大屏下同步歌词最高支持当前行 `104px`、上下文 `64px`，缩小窗口后自动
    恢复紧凑尺寸。
  - Release 全量 CTest `44/44` 通过；真实“套马杆”原始时间轴同步，“够爱”取得酷狗
    71 行同步歌词并使用 `+0.5 秒` 单曲校准达到可用状态；歌词全屏、退出全屏和
    响应式字号已完成实机验收。

- 阶段 0：项目文档与边界确定（2026-08-03）
  - 确定项目名称 `MediaHub`，可执行文件 `MediaHub.exe`。
  - 确定播放内核方案：v0.1 使用 libVLC，保留 `PlayerEngine` 可替换抽象接口，
    v0.3 补充自研 FFmpeg 内核并与之并存。
  - 确定 v0.1 范围：仅本地音视频播放，不含任何网络能力。
  - 确定 12 个开发阶段的产出与通过条件。
  - 建立规划、设计、测试、学习、交付五类文档。
  - 确认本机开发环境：Qt 5.14.2 msvc2017_64、MSVC 14.44、Ninja、CMake 4.4。
- 阶段 1：环境准备与依赖接入（2026-08-03）
  - 从 VideoLAN 官方获取 VLC 3.0.21 win64 SDK，并通过官方 SHA-256 校验。
  - 通过 `MEDIAHUB_VLC_ROOT` 接入外部 SDK，缺失或错误路径在配置阶段明确失败。
  - 增加 `MediaHubVlcProbe.exe`，使用 RAII 创建和释放 libVLC 实例。
  - Debug 与 Release 均输出 `libVLC version: 3.0.21 Vetinari`，CTest 1/1 通过。
  - 初始化本地 Git 仓库和构建产物忽略规则，并配置 Gitee SSH 远程仓库。
- 阶段 2：最小工程骨架（2026-08-03）
  - 固定接入 GoogleTest 1.17.0，并保留完整上游源码和许可证。
  - 建立 `mediahub_core`、`mediahub_engine_vlc`、`mediahub_gui_support`、
    `mediahub_gui`、`mediahub_tests` 和 `mediahub_gui_tests` 基础目标。
  - 使用 Qt 5.14.2 Widgets 生成可启动和正常关闭的空窗口 `MediaHub.exe`。
  - 接入 GoogleTest、Qt Test、CTest 和构建后 Qt 运行库部署。
  - Debug、Release CTest 均为 3/3 通过，依赖隔离与无测试构建验证通过。
- 阶段 3：播放内核抽象与状态机（2026-08-03）
  - 定义媒体项、来源、播放状态、位置、错误和播放模式等纯 C++ 值类型。
  - 建立不暴露 Qt、libVLC、FFmpeg 或 Windows 类型的 `PlayerEngine` 与事件接口。
  - 建立 8 状态转换表，区分状态改变、重复事件和非法事件。
  - 建立测试专用 `FakePlayerEngine`，控制请求与异步事件时序可独立验证。
  - 增加 `MEDIAHUB_CORE_ONLY`，在不查找 Qt/libVLC 时构建核心库与单元测试。
  - Debug、Release CTest 均为 15/15 通过，核心独立 CTest 为 13/13 通过。
- 阶段 4：libVLC 内核实现与本地音频（2026-08-03）
  - 使用 PImpl 建立不暴露 libVLC 类型的 `VlcPlayerEngine`，实现全部抽象方法。
  - 用自定义删除器管理实例、媒体和播放器句柄，并显式执行安全释放顺序。
  - 映射状态、位置、时长、可定位、结束和失败事件，位置事件按 200 毫秒节流。
  - 测试模式通过构造选项启用 libVLC 哑音频和哑视频输出。
  - 集成测试运行时生成中文空格路径的无版权静音 PCM/WAV，不提交媒体文件。
  - Debug、Release CTest 均为 23/23 通过，无测试构建与核心独立回归通过。
- 阶段 5：GUI 播放控制与线程边界（2026-08-03）
  - 建立正式主窗口、文件菜单、打开入口、播放、暂停、停止和内联状态提示。
  - 建立 `PlayerPresenter`，集中驱动状态机与全部控件启用规则。
  - 建立 `EngineEventBridge`，通过 Qt 队列连接把五类内核事件投递到 GUI 主线程。
  - `main.cpp` 组装真实 `VlcPlayerEngine`，GUI 支持库继续只依赖核心接口与 Qt。
  - 为正式 GUI 目标部署 libVLC DLL 与插件目录，并验证 Release 窗口正常启动退出。
  - Qt Test 11/11 通过，Debug、Release、核心独立与无测试构建回归通过。
- 阶段 6：本地视频输出（2026-08-03）
  - 建立原生视频输出控件，接入 libVLC 窗口句柄、画面清理、缩放和纯画面全屏。
  - 修复 Qt 绘制覆盖视频帧，以及迟到缓冲事件使界面错误退回缓冲状态的问题。
  - 使用真实 H.264/AAC MP4 完成画面、音频、控制、全屏和关闭委托实机验收。
  - Qt Test 14/14 通过，Debug、Release、核心独立与无测试构建回归通过。
- 阶段 7：进度与音量（2026-08-03）
  - 加入当前位置、总时长、归一化进度、未知时长和不可定位状态展示。
  - 实现拖动预览锁定与松手单次定位，内核位置事件不会覆盖用户拖动值。
  - 加入 0 至 100 音量滑块和静音往返，播放或暂停状态不受操作影响。
  - Qt Test 18/18 通过，并使用真实 MP4 完成进度、定位、音量和静音实机验收。
- 阶段 8：播放列表（2026-08-03）
  - 在纯核心层实现有序播放列表、当前索引及顺序/列表循环/单曲循环规则。
  - 加入只读列表模型、多选打开、保持顺序的本地文件拖放、双击切换和移除。
  - 加入上一首/下一首、播放模式选择，并让命令行支持多个媒体路径。
  - Debug/Release 全量 CTest 各 29/29 通过，核心独立 CTest 19/19 通过。
- 阶段 9：错误处理与结构化日志（2026-08-03）
  - 新增 `mediahub_logging`，以单行 UTF-8 结构输出终端日志，支持并发写入和异常保护。
  - 日志只保留媒体显示名/文件名，不记录完整路径，也不创建日志文件。
  - 增加损坏 MP4 与文本伪装文件的 `UnsupportedFormat` 分类和中文失败提示。
  - 失败项可继续切换其他列表项；Debug/Release 全量 CTest 各 33/33 通过。
- 阶段 10：自动化测试补强（2026-08-03）
  - 提取运行时媒体生成器，统一创建和清理中文空格路径的静音 WAV 与错误媒体。
  - 补齐播放列表边界、媒体名称推导、PlaylistModel 数据角色和重置信号测试。
  - 补齐 Presenter 非法/迟到事件与六类错误日志映射测试，不改变业务行为。
  - Debug/Release 全量 CTest 各 38/38、核心独立 CTest 27/27 通过。

### 已封存

- 阶段 11：真实验收与播放器交互增强（2026-08-03 至 2026-08-04）
  - 使用真实 MP4 完成画面、声音、暂停继续、定位、音量、静音和快速切换验收。
  - 修复带 UTF-8 BOM 的纯文本伪装 MP4 被当作自然结束并推进列表的问题。
  - 忽略媒体切换后的迟到结束事件，失败项不会自动跳到下一项。
  - 增加关闭超时保护，人工确认失败媒体关闭后无后台进程残留。
  - 修复暂停定位后进度条被禁用的问题，支持保持暂停状态连续定位。
  - 修复 Qt 文件选择器正斜杠路径无法交给 Windows libVLC 打开的问题。
  - 使用真实 MP3/MP4 确认文件选择器导入和两种媒体来回切换正常。
  - 修复最后一项自然结束后进度没有稳定停在末端的问题。
  - 支持单击进度条轨道直接定位；自然结束后重新打开当前媒体，再播放并定位到目标。
  - 新增空格播放/暂停、上下键调音量、左右键快进后退快捷键。
  - 新增 5、10、15、20 秒的左右键跳转步长悬停菜单，默认值为 5 秒。
  - 新增 0.5× 至 3.0× 六档播放倍速（含 0.75×），并将倍速和跳转步长改为圆角悬停菜单。
  - 音量改为向上悬停弹出的竖向滑块；静音显示 0% 并保留恢复音量；音量、跳转秒数和
    倍速与进度条在同一行展示。
  - 音量弹层改为不抢占点击的独立悬浮面板，修复点击静音时整框闪现；同时修复点击
    跳转秒数或倍速时菜单方向不一致的问题。
  - 将进度条、上一首、播放/暂停、下一首、停止、倍速、音量、跳转秒数、播放模式和
    全屏合并到同一控制行，播放与暂停共用一个动态图标按钮。
  - 上一首、下一首、停止、音量和全屏改为状态相关图标；音量悬浮框只显示百分比，
    静音时同步显示 0% 和静音图标。
  - 播放模式移入控制行并改用图标菜单，在顺序、列表循环、单曲循环之外新增随机播放。
  - 全屏控制在全屏状态保持可见，同一个按钮可进入或退出全屏，图标随状态切换。
  - 播放列表新增左右箭头折叠控制，折叠和展开时音视频区域随可用空间自动伸缩。
  - “打开文件”移动到原“移出选中”位置；列表新增右键播放、重命名显示名、置顶、
    上移、下移和移出列表，并支持 `Ctrl` 独立多选与 `Shift` 连续多选；多选时只开放
    移出列表，重命名不会修改磁盘文件名。
  - 悬停命中改为跟随控件进入/离开事件，修复 125%/150% 缩放下音量与倍速弹层
    无法稳定打开的问题。
  - 音量按钮移动到停止按钮右侧；倍速、跳转秒数和播放模式改用音量同款的不抢占
    独立悬浮窗，修复菜单在按钮上持续悬停时反复闪烁的问题。
  - 播放模式按钮支持每次点击循环切换下一种模式，悬停列表的当前勾选项随按钮同步。
  - 纯音频播放新增实际 PCM 驱动的底部单曲线；24 个圆滑控制段只向上形成疏密适中
    的高波峰，并按当前声音强度快速升起和回落，暂停时停止。
  - 波形画布只保留文件名，移除内部进度轨道、百分比和说明文字；控制行主进度条不变。
  - 音频曲线固定使用 192 个绘制点并缓存静态背景，消除全屏时按画面宽度重复计算
    多层曲线造成的卡顿。
  - 音频画面开放全屏，同一个动态图标按钮可进入或退出，行为与视频全屏一致。
  - 长按右方向键临时以 2.0× 播放并显示 2.0×，松开后恢复用户选择的倍速。
  - 倍速时钟重建仅用于纯音频；视频使用 30ms 本地缓存，切换倍率不再跳转或明显卡顿。
  - 新增 `Ctrl+左/右` 切换上一首/下一首快捷键，并保持普通方向键原有行为。
  - 封存前 Debug/Release 全量 CTest 均为 44/44、核心独立为 30/30，无测试构建成功。
  - FLAC、MKV 等其他真实格式与 125%/150% 实际缩放保留为已知遗留，不记为通过。

## 版本计划

| 版本 | 主题 | 状态 |
|---|---|---|
| v0.1.0 | 本地音视频播放，libVLC 内核 | 本地自用已封存 |
| v0.2.0 | URL 直播播放、缓冲与断流处理 | 单 URL 与纯核心 M3U 已完成，频道页面待实现 |
| v0.3.0 | 自研 FFmpeg 内核，与 libVLC 内核并存对照 | 未开始 |
