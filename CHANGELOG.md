# 变更记录

本文件记录 MediaHub 的版本变更。格式参考 Keep a Changelog，版本号遵循语义化
版本。

## [未发布]

### 已完成

- Qt 6.8.3 迁移（2026-08-24）
  - CMake、源码、Qt Test、部署脚本和当前维护文档迁移为 Qt6-only；构建系统不再查找或
    链接 Qt 5.14.2。
  - 适配 `qsizetype`、Qt6 重定向策略、UTF-16 缓冲、`QEnterEvent` 和 `QMouseEvent` 接口；
    MSVC 目标显式使用 `/EHsc`，Debug/Release 全目标编译无警告。
  - Qt6 Debug/Release 全量 CTest 均为 `93/93`，Core-only 为 `52/52`，无测试 Release
    构建成功且 CTest 为 `0` 项；最新迁移回归分别耗时 425.39 秒和 96.39 秒。
  - Qt6 播放状态改用独立的 `MediaHubQt6` 本机配置，首次启动从旧 `MediaHub` 配置导入；
    直播源备忘按地址去重，Qt6 已有备注优先，迁移一次后允许正常删除。用户已确认旧版
    5 条直播源地址和备注全部显示在 Qt6“帮助 -> 直播源”窗口中。
  - Qt6 正式包包含 398 个文件和 365 个 VLC 插件；结构校验、隔离本地播放、真实
    WebView2 初始化和本地视频跨页连续播放通过，不包含 `Qt5*.dll` 或 Qt6 Debug DLL。
  - 用户已确认当前可用直播源的画面、声音和跨页后台播放正常；迁移提交 `192fa98` 已普通
    推送到 Gitee 和 GitHub。旧 Qt5 包随后移入 Windows 回收站，`dist` 只保留唯一 Qt6
    正式目录；正式路径结构校验和隔离 MP3 播放再次通过。

- v0.7 直播跨页后台播放修复（2026-08-24）
  - 进入网页页不再主动暂停正在打开、缓冲或播放的直播/本地媒体；页面切换只改变页面和
    WebView2 可见性，播放会话继续在后台运行。
  - 新增直播播放中切网页、切回直播并刷新后恢复 Playing 的连续回归，保留主动暂停、停止、
    切台、自动重连和关闭语义。
  - Debug/Release 定向用例、GUI 主套件和全量 CTest 均为 `93/93` 通过，真实直播人工验收
    通过；正式包已更新并完成结构校验与隔离播放。
  - 正式包包含 400 个文件、365 个 VLC 插件；`MediaHub.exe` SHA-256 为
    `C352706B9E149B249B3ADC5D2733C0BAB4AB0242F423F36ED608CC27A97E9111`。

- v0.7 直播断流自动重连修复（2026-08-24）
  - 已播放直播收到 libVLC 的失败或结束事件后，按 1、2、4 秒自动重连；恢复成功后重置
    连续失败预算，首次无法播放的坏源不反复请求。
  - 自动重连和手动刷新均保留原音量与静音；停止、切台、切本地媒体和关闭程序会取消
    旧重连任务。
  - 移除唯一内核控制线程中已无消费者的 libVLC 网络统计轮询，避免卡住输入拖延刷新、
    切台和停止命令。
  - Debug 定向 GUI、核心单元和 libVLC 切台回归均通过；Release 全量 CTest `93/93` 通过。
  - 正式 `dist/MediaHub-0.7.0-win64` 已替换，结构校验、隔离 `test.mp3` 播放和正常退出均
    通过；包内 400 个文件、365 个 VLC 插件，未包含 Debug/测试文件。

- v0.7 本地音频倍速切换短暂丢音修复（2026-08-21）
  - 本地音频主播放器和波形分析播放器统一使用 `:file-caching=30`，避免从 `1.0×` 切换到
    `2.0×` 时等待默认约一秒文件缓存。
  - 保留纯音频切速后的原位时钟重建，新增长音频空档回归和音频媒体选项断言。
  - Release 全量 CTest `93/93` 通过；正式 `dist/MediaHub-0.7.0-win64` 已更新并完成
    结构校验、隔离 MP3 播放和正常退出验证。

- v0.7 长直播切换失效修复（2026-08-17）
  - 旧直播停止和释放不再使用会永久封死 `open()` 的硬回收名额；最多 4 个回收线程，
    其余旧会话排队清理，新直播和本地媒体仍可继续打开。
  - 从直播切换到任意新媒体时创建全新 libVLC 实例，隔离长时间网络播放后的旧实例状态。
  - 新增 4 个旧播放器同时阻塞后继续切直播、切本地 WAV 和 Stop 的回归；Debug/Release
    全量 CTest 均为 `92/92` 通过，用户已放行当前 Debug 候选。
  - 使用 Release 构建重新生成正式 `dist/MediaHub-0.7.0-win64`，包内 400 个文件、365 个
    VLC 插件，结构校验和隔离播放通过；随后删除 Debug 构建目录。

- v0.7 本地与直播界面及主题重构（2026-08-17）
  - 本地和直播页统一为连续媒体舞台、响应式队列与底部播放坞，顶部文件、视图、帮助和
    主题入口改为右上角统一图形按钮；网页页内部 UI 保持原样。
  - 增加深色/浅色模式、五套完整预设、自定义 RGB、本机背景图片、模糊度和背景清晰度，
    波形与歌词共享整窗背景裁切。
  - RGB、模糊度和背景清晰度滑杆支持轨道绝对定位；拖动期间不刷新整窗 QSS，松手后只
    提交一次主题预览，解决多次点击和拖动阻塞。
  - 音频波形改为从底部向上的单向真实 PCM 曲线；播放列表横向滚动条、浅色列表和倍速等
    悬浮面板同步适配主题。
  - 快捷键和直播源备忘窗口完成平面桌面风格重构；跨页窗口缩放时先同步 WebView2 边界，
    避免切回网页页出现旧尺寸黑区。
  - 用户最终实机确认通过；Release 全量 CTest `92/92`，发布包结构、依赖来源、正常启动与
    退出均通过，正式目录更新为 `dist/MediaHub-0.7.0-win64`。

- v0.6 音频残留视频轨弹窗修复（2026-08-17）
  - 本地音频扩展名现在会同时为主播放器和波形分析播放器提交 `:no-video`；即使文件
    内部仍保留 MP4 视频轨，也只播放音频，不再由 libVLC 创建独立视频窗口。
  - 新增扩展名与内容不匹配的运行时媒体回归，确认两个播放器都禁用视频且声音、波形
    和播放状态保持正常。
  - 用户使用原问题 MP3 完成人工验收；Debug/Release 全量 CTest 均为 `92/92`，新版
    `dist/MediaHub-0.6.0-win64` 结构校验通过，包含 365 个 VLC 插件且无 Debug/测试文件。

- v0.6 内置网页浏览器本地自用交付（2026-08-15）
  - 增加 Edge 风格两层 Chrome、图标导航与统一右侧侧栏，历史、收藏、下载、声音、
    标签搜索、分组和网站权限不再使用分散窗口。
  - 完成多标签恢复/固定/分组/搜索/缩放、Bing 首页、网页内查找、收藏导入导出、
    会话恢复、并发下载中心和后台连续运行语义。
  - 历史/收藏资料、声音列表、标签窗口、搜索输入、下载进度和会话检查点均使用缓存、
    脏标记或延迟合并，避免高标签数下重复读取、重建和持久化。
  - 移除网页宿主不必要的 `WA_NativeWindow`，WebView2 挂到既有原生祖先并使用相对物理
    像素边界；用户确认持续拖动跟手、网页不黑屏，浅色界面清晰。
  - BrowserPage `84/84`、Debug/Release 全量 CTest `91/91`、Core-only `52/52`、
    无测试 Release `0` 项和真实 WebView2 Runtime `57/57` 通过。
  - 最终发布目录为 400 个文件、365 个 VLC 插件和六项法律材料；发布包校验通过，
    正常退出后无 MediaHub 或其 WebView2 子进程残留。
  - v0.6 按本地自用版本完成。未逐项补证的真实账号、DRM、外部协议和跨机器场景
    继续作为公开发布与兼容性限制，不扩大解释为通过。

- v0.6 内置网页浏览器基线自动化证据（2026-08-12，历史记录）
  - 增加独立“网页”展示模式，基于固定 WebView2 SDK 提供地址导航、后退/前进、
    刷新、主页、响应式工具栏和原生 child HWND 快捷键路由。
  - MediaHub 使用隔离 Profile；主页面与受控登录弹窗共享同一资料，支持确认后清除
    网页数据，并对权限、屏幕捕获、下载、上传入口、外部协议和证书错误执行安全决策。
  - 网页全屏、模式切换静音/挂起、原生媒体暂停，以及弹窗、事件 token、Controller、
    Environment 和 COM 引用的确定性关闭已有假后端与真实 Runtime 自动化覆盖。
  - 浏览器 GUI `27/27`、主 GUI `95/95`、普通环境 Debug WebView2 `44/44`
    （12.021 秒）和 Release WebView2 `44/44`（11.276 秒，零失败、零跳过）通过；
    四项 GUI CTest `4/4`、Core-only `52/52`（7.43 秒）通过，无测试 Release 为 0 项。
  - 沙箱 Debug/Release 全量 CTest 均为 `80/81`（233.30/157.17 秒），唯一失败是
    `integration.webview2.initialization` 在 90.04/90.03 秒超时；这是已确认的沙箱
    WebView2 子进程限制，未记作全绿。
  - Task 12 更新项目版本为 `0.6.0` 后，普通 Windows Release/Debug 都重新配置、生成并
    构建退出 0；沙箱外全量 CTest 分别为 `81/81`（103.20 秒）和 `81/81`
    （226.09 秒），WebView2 初始化集成测试为 11.97/12.74 秒。该 fresh 结果补齐 W-18，
    不改写 Task 11 的沙箱历史环境差异。
  - `dist/MediaHub-0.6.0-win64` 校验为 400 个文件、365 个 VLC 插件、六项法律材料，
    reparse 与禁入项均为 0；架构边界和敏感日志扫描零违规。Evergreen Runtime 不随包
    分发，目标 Windows x64 环境需预先安装。
  - W-01 至 W-18 已逐项回填自动化证据和边界；在 2026-08-12 基线节点，含真实账号、
    公共网站、网页音画、DRM、系统交互、实际 DPI 或进程观察语义的编号仍只记为
    “自动化部分通过，整项待人工”。后续本地自用收口见上一条，原矩阵未补证项仍保留。

- 本地视频末项自然结束后重播画面修复（2026-08-11）
  - 修复顺序播放到列表最后一个本地视频并自然结束后，点击播放只有进度、没有画面，
    必须再次双击列表项才能恢复的问题。
  - 本地播放器仍绑定旧媒体时，在更换媒体或 HWND 前无条件显式 Stop，确保 libVLC
    终态下仍可能残留的旧 vout 完成清理。
  - 重播继续创建不同的非空原生子窗口，并保持 Open -> Play -> Seek(0) 顺序；旧
    句柄在播放器释放后才销毁。
  - Debug/Release 全量 CTest 均为 `78/78`，分别用时 120.61 秒和 78.53 秒，三档
    GUI 全部通过；用户确认真实末项视频只点击播放即可恢复画面和进度。
  - 本修复不生成或替换 ZIP；仓库外已有的 v0.5 ZIP 保持不变。

- 直播切换阻塞与连接超时误报修复（2026-08-11）
  - 修复旧长直播播放器回收长期占用唯一内核控制线程，导致新频道尚未打开、旧频道仍
    播放并被 GUI 误报网络连接超时的问题。
  - 每次打开使用唯一请求编号；15 秒网络计时和自动播放只在内核确认当前请求真正开始
    后触发，旧请求的迟到确认不会污染当前频道。
  - 每次视频切换使用独立 Qt 原生子窗口，不同句柄的旧播放器在独立线程回收；新旧
    vout 不共用 HWND，也不把播放器改绑到空句柄。
  - 共享事件分发器在旧播放器完全释放后安全回收 GUI 子窗口，并隔离引擎关闭后的迟到
    事件；同句柄转交和本地同步切换的句柄所有权已有回归覆盖。
  - 普通切换最多保留 3 个异步回收，并为 Stop 预留第 4 个名额；达到上限会明确提示
    稍后重试并归还新视频窗口，不会无限累积线程或播放器。
  - Release/Debug 全量 CTest 均为 `77/77`，分别用时 56.10 秒和 114.89 秒，三档
    GUI 全部通过；用户确认真实直播 A→B→C、旧声音停止、无超时误报、无独立 VLC
    窗口以及 Stop/再次播放/关闭均无问题。
  - 本修复不生成或替换 ZIP；用户已有的 v0.5 ZIP 继续保留在仓库外。

- 文档命名与导航整理（2026-08-10）
  - `docs/` 下 50 个 Markdown 文件按分类统一为两位序号，版本、阶段和增量名称补齐
    分隔符与前导零；同步重排文档导航并更新仓库内全部引用。
  - 按内容补齐 v0.3、v0.4、v0.5 三份版本完成说明，并在导航中建立规划、测试和
    完成说明的版本归档矩阵；v0.5 ZIP 按用户实际状态记录为仓库外独立完成。

- v0.4 状态持久化与快捷键（2026-08-07）
  - 使用可注入的 GUI 存储边界保存本地播放列表、最后直播清单 URL 和最近 20 项
    成功清单历史；启动恢复只更新界面，不自动打开、解析或播放。
  - URL 编辑框右侧增加纯图标历史按钮，独立窗口支持回填、删除和取消；回填不会
    自动载入清单，载入期间原按钮可取消并隔离迟到结果。
  - `F11` 改为不依赖菜单栏可见性的窗口快捷键，可连续进入和退出全屏；新增
    `Ctrl+下键` 静音和 `Ctrl+上键` 恢复，`Ctrl+F5` 不分配功能。
  - 菜单栏增加“帮助 → 快捷键”说明窗口。
  - “帮助”下增加独立“直播源”资料窗口，以两列保存地址和备注；新增、删除、返回、
    保存及确认窗口使用高对比主题。保存按钮和仅限该窗口的 `Ctrl+S` 会先确认，
    返回与 `X` 对未保存内容提供保存或放弃选择；主窗口 `Ctrl+M` 可快速打开该窗口，
    不改变现有播放列表和播放链路。
  - 历史直播源窗口移除无功能的标题栏“？”按钮；直播源资料长备注默认完整换行并
    按内容增高，无需进入编辑状态即可阅读全文。
  - 修复直播源资料单元格编辑时右方向键被主窗口播放快捷键拦截的问题；弹窗内方向键
    只移动文本光标，不再触发主窗口定位或临时倍速。

- v0.3 界面重构（2026-08-07）
  - 建立本地音频、本地视频和直播三种展示模式，只改变视觉属性、文字层级和布局密度。
  - 本地音频改为浅色绿色音乐界面，重绘真实 PCM 波形背景并统一歌词页视觉。
  - 本地视频改为深色影院画布；活动视频继续由 libVLC 直接输出，不改变原生句柄链路。
  - 直播改为 PotPlayer 方向的紧凑工具侧栏，远程清单地址、载入、定位和状态集中显示。
  - 所有既有按钮、快捷键、悬浮层、列表模型、右键规则及 Presenter 命令语义保持不变。
  - 新增三模式对象身份、画布同步和响应式布局回归；项目版本元数据更新为 `0.3.0`。
  - 本地/直播侧栏切换保持同宽同高，宽度与字号随窗口响应式调整；真实直播播放中
    连续调整窗口保持稳定。
  - 历史直播源窗口增加石墨黑与暖金主题；快捷键帮助窗口“确定”使用独立高对比蓝色
    主按钮，避免被父窗口通用样式覆盖。
  - Release/Debug 全量 CTest 均为 `74/74`，最终结果分别为 46.74 秒和 91.79 秒；
    用户完成人工确认，v0.3 本地自用版本完成。

- v0.2 URL 直播播放（2026-08-05 至 2026-08-06）
  - “文件 → 打开网络地址”支持用户自带的 `http`、`https`、`rtsp`、
    `rtmp`、`rtmps`、`udp`、`rtp` 和 `srt` 地址。
  - 完整 URL 仅在当前进程内播放；列表、界面错误和日志不记录凭据、
    查询参数或片段。
  - 第一增量已使用本机 HTTP MPEG-TS 完成真实画面和声音验收。
  - 第二增量增加可见缓冲百分比、连接期间立即取消、15 秒打开超时、
    可重试界面和网络失败中文映射。
  - 取消或超时后的迟到事件不得覆盖界面；移除正在连接的最后一项会
    同步停止超时计时器。
  - 修复 libVLC 3 在正常直播切换到慢连接时同步停止导致的界面卡死；控制命令
    改为专用线程串行执行，网络取消通过替换播放器立即隔离旧流。
  - 第二增量 Debug CTest `49/49`、核心独立 CTest `33/33` 通过；用户
    已确认正常直播→慢连接→取消→恢复正常直播全流程不再卡死。
  - 第三增量区分缓冲与真实断流；已播放直播失败或结束后最多按 1、2、4 秒
    自动重连 3 次，手动停止、媒体切换和关闭会取消旧地址重连。
  - 修复真实 HTTP MPEG-TS 停止输入后 libVLC 可能长期保持 `Playing`、不发送失败或
    结束事件的问题；内核每 250ms 采样输入、解复用和音视频活动统计，Presenter
    每 500ms 检查一次，连续 15 秒无活动时进入同一有界重连流程。网络播放位置持续
    前进时也作为活动兜底，兼容 HTTP-FLV 的 libVLC 统计短暂冻结；从未提供统计或
    位置活动的协议仍不猜测断流。
  - 本次会话最近使用的 10 个地址按最近优先去重，仅显示在可编辑输入下拉框，
    不写入设置或日志。
  - 本机可控直播已验证无终止事件断流后自动恢复音画、三次耗尽、手动重试、停止和
    本地媒体切换取消、最近地址下拉框及重启清空；旧直播恢复后不会抢回本地歌曲。
  - 最终 Debug/Release 全量 CTest 均为 `49/49`，核心独立 CTest 为 `33/33`；
    `MEDIAHUB_BUILD_TESTS=OFF` 构建、修改文件格式检查和空白检查均通过。
  - 第四增量完成真实直播画面与声音、音量与静音、暂停继续、停止、本地 MP3/MP4
    往返、歌词、波形、进度定位、全屏、最近地址、15 秒慢连接超时、重试取消和
    播放中安全关闭的完整实机回归；项目版本元数据更新为 `0.2.0`。
  - 公网 HLS/HTTP-FLV 兼容复验发现，部分直播会报告可定位窗口或在播放正常时冻结
    libVLC 活动统计。网络媒体现始终显示 `--:--` 并禁用进度定位；曾将活动确认窗口
    延长到 15 秒并加入位置兜底，用于确认自动判断仍会受不同协议行为影响。
  - 修复旧直播后台停止可能覆盖新媒体音频状态的问题：跨媒体切换会创建独立的
    libVLC 实例，旧播放器在后台关闭音视频轨并停止；新播放器继承音量和静音状态，
    进入 `Playing` 后再次应用。公网直播 A→直播 B→本地歌曲实机往返无需调节音量
    即可立即听到声音。
  - 根据实机体验撤销所有自动重连和活动停滞推断。网络直播新增“刷新直播”按钮与
    `F5` 快捷键，播放、缓冲、失败或用户观察到普通画面停顿时均可主动重开当前地址；
    明确失败或结束只提示“直播已断开，请点击刷新”。
  - 手动刷新复用当前网络列表项，不新增列表记录或最近地址；刷新替换播放器并复用
    当前 libVLC 实例，保留音量、静音和全屏状态。本地媒体不显示刷新按钮，按 `F5`
    不执行任何播放命令。
  - 最终手动刷新版本 Debug/Release 全量 CTest 均为 `50/50`，分别用时约 18.87 秒
    和 15.83 秒；核心独立 CTest `33/33` 通过，用时约 0.69 秒；无测试构建成功且
    CTest 为 0，格式、空白和依赖边界扫描通过。
  - 用户在最新 Release 中确认按钮与 `F5` 均能重新恢复真实直播音画；刷新后直接
    切回本地歌曲立即有声音，无需调节音量。
  - 单 URL 直播四个增量现已封存；第五增量在纯核心层增加 `LiveChannel`、
    `LiveChannelLibrary` 以及 UTF-8 M3U 解析和序列化。
  - M3U 支持 BOM、CRLF/LF、中文、分类、EPG/Logo 元数据和稳定顺序；非法、不完整和
    重复地址使用不携带完整 URL 的结构化诊断，序列化遇到不安全模型不会返回半成品。
  - 新增 17 个 M3U 单元场景；第五增量 Debug/Release 全量 CTest 均为 `67/67`，
    Core-only `50/50`，无测试构建成功且 CTest 为 0。
  - GUI 测试的歌词校准设置改写入临时目录，不再读取、写入或污染真实用户配置；
    产品运行时设置路径不变。
  - 用户取消原本地频道库、频道分类和逐频道增删改方案。后续改为输入远程 M3U/M3U8
    清单 URL，显示当前链接中的扁平直播列表，并与本地播放列表明确分开。
  - 新路线不持久化带凭据的清单 URL，不显示分类、Logo 或 EPG；清单文本使用异步
    读取、超时、正文/条目上限和迟到结果隔离，失败时保留上次成功直播列表。
  - 播放列表侧栏新增“本地播放列表 / 直播列表”切换；本地文件、单直播 URL 和远程
    清单条目分别进入正确列表，切换列表不会停止当前媒体。
  - 直播页新增清单 URL 输入、载入/刷新、读取状态及扁平只读列表；分类字段不进入
    页面，直播条目不提供重命名、排序和删除。
  - 直播条目右键菜单只保留“播放/暂停”、“停止”、“标记/取消标记”和
    “收藏/取消收藏”，不提供删除；停止只对当前可停止的直播源启用。右键单击只弹出
    菜单，右键双击不会激活播放；标记项在本次会话内显示
    “【不可用】”、浅红警示背景和加粗文字，同时置灰且不可选择或播放，悬停会提示
    右键取消标记，标记当前播放源会立即停止。
  - 会话内收藏按直播源地址识别，以“★”前缀显示；收藏、取消收藏、标记和取消标记
    都只刷新目标行，不再重置模型或把长列表滚回第一项。直播列表新增“定位正在播放”
    按钮，可按当前播放源地址滚动到对应行并居中选中；停止播放、当前源不在清单中或
    当前源被标记时按钮自动禁用。
  - `LivePlaylistService` 只读取 M3U/M3U8 文本，不下载音视频；限制 10 秒、5 次安全
    重定向、2 MiB 正文和 5000 项，禁用 Cookie，并拒绝把普通 HLS 分片清单列为频道。
  - 修复 Qt 5.14 未部署 OpenSSL 1.1 时所有 HTTPS 清单都会读取失败的问题；Windows
    版改用系统 WinHTTP 在线程池读取，保留证书校验、重定向和资源限制且不新增第三方
    TLS 运行库。
  - `Ctrl+L` 现在识别 `.m3u`/`.m3u8`：`.m3u` 始终按频道清单展开且失败时不会新增
    为单路直播；`.m3u8` 只有检测到行首 HLS 控制标签后才回退到直接播放链路。
  - 核心 M3U 解析新增可注入的相对地址补全边界；相对条目以最终响应 URL 解析，
    仍由核心网络协议白名单和重复规则完成最终校验。
  - 新增本机临时 HTTP 服务、清单自动识别与 HLS 回退测试；Core-only `52/52`，
    无测试构建成功且 CTest 为 0。
  - 修复卡顿直播连续切换时 libVLC 创建无法由 MediaHub 关闭的独立视频窗口的问题。
    原生窗口检查证明旧内嵌 vout 未退出时，新 vout 会回退到顶层窗口；切换现改为在
    内核控制线程完整停止并释放旧播放器后才绑定新播放器，GUI 主线程保持响应。等待
    期间再次选择其他源会丢弃已被取代的中间请求，只打开最后选择的源。直播间切换仍
    复用当前 libVLC 实例，切回本地媒体仍创建隔离实例。新增受控停止时序回归后，
    Debug/Release 全量 CTest 均为 `72/72`。
  - 直播右键菜单、停止路由、标记/收藏状态和清单刷新后播放源身份回归完成；标记与
    收藏动作会显式恢复滚动位置，标记当前播放源后定位按钮会立即禁用。最新
    Debug/Release 全量 CTest 均为 `72/72`，用时约 22.93 秒和 19.54 秒。
  - 远程清单名称与顺序、直播音画、停止/刷新、失败保留、双列表布局及本地/直播互不
    混入均已由用户完成 Release 人工验收。
  - 新增独立 `WindowIconManager`：启动时从 `MediaHub.exe` 旁读取
    `icons/taskbar.png` 和 `icons/window.jpg`，分别设置任务栏与标题栏左上角图标；
    文件缺失或损坏时按槽位回退到内嵌资源。构建和安装会生成默认文件，后续替换图片
    并重启即可生效，无需重新编译。图标加载回归加入现有 GUI 套件后，Debug/Release
    全量 CTest 均为 `72/72`，分别用时约 27.70 秒和 20.55 秒。
  - 用户在 Release 目录替换两张运行时图片并连续重启确认：任务栏与标题栏图标均按
    对应文件更新，两个位置显示正确。
  - 第八增量使用全新目录完成 Release `72/72`、Core-only `52/52` 和无测试构建；
    Release 发布目录结构、365 个 VLC 插件、许可证和两个运行时图标校验通过。
  - 同机最小环境真实 MP3 隔离运行确认 Qt/VLC 模块均来自发布目录，正常关闭后退出码
    为 0。已生成 `MediaHub-0.2.0-win64.zip` 并记录 SHA-256，v0.2 按本地自用交付封存。

### 已封存

- 阶段 12：v0.1 Release 交付（2026-08-04）
  - 新增 `MEDIAHUB_PACKAGE_RELEASE` CMake 安装规则，生成只包含正式程序、Qt 运行库、
    libVLC 运行库与插件、许可证材料的独立 Windows x64 发布目录。
  - 新增发布包结构校验和隔离播放脚本；拒绝静态库、PDB、测试程序、Debug DLL 和
    VC++ 运行库安装器混入发布包。
  - 空目录 Debug/Release 构建均完成，CTest 均为 `44/44`；Release GUI 套件连续
    5 轮通过。
  - 当前发布目录共 `396` 个文件、约 `185.86 MiB`，含 `365` 个 VLC DLL 插件；
    清空 Qt/VLC 环境变量并缩减 PATH 后可播放、正常关闭且退出码为 `0`。
  - 生成 78.91 MiB 的 Windows x64 ZIP 并记录 SHA-256；用户确认强制 125%/150%
    发布版布局正常，最终交付候选清单明确保留第二台机器和系统级 DPI 复测项。
  - 用户接受上述未覆盖项作为本地自用限制，阶段 12 按本地交付封存；v0.2 仅在用户
    后续明确通知后开始，不自动进入。

### 已完成

- 在线歌词增量（2026-08-04）
  - 音量按钮右侧新增圆形“词”按钮，纯音频可在真实波形与歌词页之间切换。
  - 支持 ID3 标题、艺术家、专辑和 `USLT` 内嵌歌词，文件名作为标签缺失时的降级。
  - 在线查询按酷狗、网易云、LRCLIB、TheAudioDB 顺序降级，并使用标题、艺术家、
    专辑和时长共同匹配，避免盲目采用第一条同名结果。
  - 同步歌词随播放位置高亮当前行，普通歌词保留全文滚动；查询异步、可取消且有
    6 秒超时，不阻塞播放线程。
  - 查询结果缓存到 Qt 应用缓存目录，不修改媒体文件，不向服务端发送本地完整路径。
  - Qt 缺少 OpenSSL 1.1 时自动使用网易云 HTTP，并跳过无法建立 HTTPS 的备用源。
  - 修复本地与曲库演唱者署名不同导致歌词被拒绝的问题；只有核心标题相同且时长
    相差不超过 5 秒才降低歌手不匹配惩罚，避免误配其他同名歌曲。
  - 酷狗优先按歌曲分组和音频条目哈希查找同步歌词，并根据显示标题排除 DJ、Live、
    Cover、纯音乐、伴奏等录音变体；失败后继续使用原有歌词源。
  - 歌词页增加单曲 `-0.5s`、`+0.5s` 和重置校准；偏移使用歌曲信息 SHA-256 键保存
    到用户 INI 配置，不记录本地完整路径，也不影响其他歌曲。
  - 同步歌词、普通歌词、文件名、来源标签和时间校准控件按实际歌词区域宽高响应式
    缩放；大屏下同步歌词最高支持当前行 `104px`、上下文 `64px`，缩小窗口后自动
    恢复紧凑尺寸。
  - Release 全量 CTest `44/44` 通过；真实“套马杆”原始时间轴同步，“够爱”取得酷狗
    71 行同步歌词并使用 `+0.5 秒` 单曲校准达到可用状态；歌词全屏、退出全屏和
    响应式字号已完成实机验收。

- 阶段 0：项目文档与边界确定（2026-08-03）
  - 确定项目名称 `MediaHub`，可执行文件 `MediaHub.exe`。
  - 确定播放内核方案：使用 libVLC，并以 `PlayerEngine` 抽象接口隔离界面、核心逻辑
    和真实播放能力。
  - 确定 v0.1 范围：仅本地音视频播放，不含任何网络能力。
  - 确定 12 个开发阶段的产出与通过条件。
  - 建立规划、设计、测试、学习、交付五类文档。
  - 确认本机开发环境：Qt 5.14.2 msvc2017_64、MSVC 14.44、Ninja、CMake 4.4。
- 阶段 1：环境准备与依赖接入（2026-08-03）
  - 从 VideoLAN 官方获取 VLC 3.0.21 win64 SDK，并通过官方 SHA-256 校验。
  - 通过 `MEDIAHUB_VLC_ROOT` 接入外部 SDK，缺失或错误路径在配置阶段明确失败。
  - 增加 `MediaHubVlcProbe.exe`，使用 RAII 创建和释放 libVLC 实例。
  - Debug 与 Release 均输出 `libVLC version: 3.0.21 Vetinari`，CTest 1/1 通过。
  - 初始化本地 Git 仓库和构建产物忽略规则，并配置 Gitee SSH 远程仓库。
- 阶段 2：最小工程骨架（2026-08-03）
  - 固定接入 GoogleTest 1.17.0，并保留完整上游源码和许可证。
  - 建立 `mediahub_core`、`mediahub_engine_vlc`、`mediahub_gui_support`、
    `mediahub_gui`、`mediahub_tests` 和 `mediahub_gui_tests` 基础目标。
  - 使用 Qt 5.14.2 Widgets 生成可启动和正常关闭的空窗口 `MediaHub.exe`。
  - 接入 GoogleTest、Qt Test、CTest 和构建后 Qt 运行库部署。
  - Debug、Release CTest 均为 3/3 通过，依赖隔离与无测试构建验证通过。
- 阶段 3：播放内核抽象与状态机（2026-08-03）
  - 定义媒体项、来源、播放状态、位置、错误和播放模式等纯 C++ 值类型。
  - 建立不暴露 Qt、libVLC 或 Windows 类型的 `PlayerEngine` 与事件接口。
  - 建立 8 状态转换表，区分状态改变、重复事件和非法事件。
  - 建立测试专用 `FakePlayerEngine`，控制请求与异步事件时序可独立验证。
  - 增加 `MEDIAHUB_CORE_ONLY`，在不查找 Qt/libVLC 时构建核心库与单元测试。
  - Debug、Release CTest 均为 15/15 通过，核心独立 CTest 为 13/13 通过。
- 阶段 4：libVLC 内核实现与本地音频（2026-08-03）
  - 使用 PImpl 建立不暴露 libVLC 类型的 `VlcPlayerEngine`，实现全部抽象方法。
  - 用自定义删除器管理实例、媒体和播放器句柄，并显式执行安全释放顺序。
  - 映射状态、位置、时长、可定位、结束和失败事件，位置事件按 200 毫秒节流。
  - 测试模式通过构造选项启用 libVLC 哑音频和哑视频输出。
  - 集成测试运行时生成中文空格路径的无版权静音 PCM/WAV，不提交媒体文件。
  - Debug、Release CTest 均为 23/23 通过，无测试构建与核心独立回归通过。
- 阶段 5：GUI 播放控制与线程边界（2026-08-03）
  - 建立正式主窗口、文件菜单、打开入口、播放、暂停、停止和内联状态提示。
  - 建立 `PlayerPresenter`，集中驱动状态机与全部控件启用规则。
  - 建立 `EngineEventBridge`，通过 Qt 队列连接把五类内核事件投递到 GUI 主线程。
  - `main.cpp` 组装真实 `VlcPlayerEngine`，GUI 支持库继续只依赖核心接口与 Qt。
  - 为正式 GUI 目标部署 libVLC DLL 与插件目录，并验证 Release 窗口正常启动退出。
  - Qt Test 11/11 通过，Debug、Release、核心独立与无测试构建回归通过。
- 阶段 6：本地视频输出（2026-08-03）
  - 建立原生视频输出控件，接入 libVLC 窗口句柄、画面清理、缩放和纯画面全屏。
  - 修复 Qt 绘制覆盖视频帧，以及迟到缓冲事件使界面错误退回缓冲状态的问题。
  - 使用真实 H.264/AAC MP4 完成画面、音频、控制、全屏和关闭委托实机验收。
  - Qt Test 14/14 通过，Debug、Release、核心独立与无测试构建回归通过。
- 阶段 7：进度与音量（2026-08-03）
  - 加入当前位置、总时长、归一化进度、未知时长和不可定位状态展示。
  - 实现拖动预览锁定与松手单次定位，内核位置事件不会覆盖用户拖动值。
  - 加入 0 至 100 音量滑块和静音往返，播放或暂停状态不受操作影响。
  - Qt Test 18/18 通过，并使用真实 MP4 完成进度、定位、音量和静音实机验收。
- 阶段 8：播放列表（2026-08-03）
  - 在纯核心层实现有序播放列表、当前索引及顺序/列表循环/单曲循环规则。
  - 加入只读列表模型、多选打开、保持顺序的本地文件拖放、双击切换和移除。
  - 加入上一首/下一首、播放模式选择，并让命令行支持多个媒体路径。
  - Debug/Release 全量 CTest 各 29/29 通过，核心独立 CTest 19/19 通过。
- 阶段 9：错误处理与结构化日志（2026-08-03）
  - 新增 `mediahub_logging`，以单行 UTF-8 结构输出终端日志，支持并发写入和异常保护。
  - 日志只保留媒体显示名/文件名，不记录完整路径，也不创建日志文件。
  - 增加损坏 MP4 与文本伪装文件的 `UnsupportedFormat` 分类和中文失败提示。
  - 失败项可继续切换其他列表项；Debug/Release 全量 CTest 各 33/33 通过。
- 阶段 10：自动化测试补强（2026-08-03）
  - 提取运行时媒体生成器，统一创建和清理中文空格路径的静音 WAV 与错误媒体。
  - 补齐播放列表边界、媒体名称推导、PlaylistModel 数据角色和重置信号测试。
  - 补齐 Presenter 非法/迟到事件与六类错误日志映射测试，不改变业务行为。
  - Debug/Release 全量 CTest 各 38/38、核心独立 CTest 27/27 通过。

### 已封存

- 阶段 11：真实验收与播放器交互增强（2026-08-03 至 2026-08-04）
  - 使用真实 MP4 完成画面、声音、暂停继续、定位、音量、静音和快速切换验收。
  - 修复带 UTF-8 BOM 的纯文本伪装 MP4 被当作自然结束并推进列表的问题。
  - 忽略媒体切换后的迟到结束事件，失败项不会自动跳到下一项。
  - 增加关闭超时保护，人工确认失败媒体关闭后无后台进程残留。
  - 修复暂停定位后进度条被禁用的问题，支持保持暂停状态连续定位。
  - 修复 Qt 文件选择器正斜杠路径无法交给 Windows libVLC 打开的问题。
  - 使用真实 MP3/MP4 确认文件选择器导入和两种媒体来回切换正常。
  - 修复最后一项自然结束后进度没有稳定停在末端的问题。
  - 支持单击进度条轨道直接定位；自然结束后重新打开当前媒体，再播放并定位到目标。
  - 新增空格播放/暂停、上下键调音量、左右键快进后退快捷键。
  - 新增 5、10、15、20 秒的左右键跳转步长悬停菜单，默认值为 5 秒。
  - 新增 0.5× 至 3.0× 六档播放倍速（含 0.75×），并将倍速和跳转步长改为圆角悬停菜单。
  - 音量改为向上悬停弹出的竖向滑块；静音显示 0% 并保留恢复音量；音量、跳转秒数和
    倍速与进度条在同一行展示。
  - 音量弹层改为不抢占点击的独立悬浮面板，修复点击静音时整框闪现；同时修复点击
    跳转秒数或倍速时菜单方向不一致的问题。
  - 将进度条、上一首、播放/暂停、下一首、停止、倍速、音量、跳转秒数、播放模式和
    全屏合并到同一控制行，播放与暂停共用一个动态图标按钮。
  - 上一首、下一首、停止、音量和全屏改为状态相关图标；音量悬浮框只显示百分比，
    静音时同步显示 0% 和静音图标。
  - 播放模式移入控制行并改用图标菜单，在顺序、列表循环、单曲循环之外新增随机播放。
  - 全屏控制在全屏状态保持可见，同一个按钮可进入或退出全屏，图标随状态切换。
  - 播放列表新增左右箭头折叠控制，折叠和展开时音视频区域随可用空间自动伸缩。
  - “打开文件”移动到原“移出选中”位置；列表新增右键播放、重命名显示名、置顶、
    上移、下移和移出列表，并支持 `Ctrl` 独立多选与 `Shift` 连续多选；多选时只开放
    移出列表，重命名不会修改磁盘文件名。
  - 悬停命中改为跟随控件进入/离开事件，修复 125%/150% 缩放下音量与倍速弹层
    无法稳定打开的问题。
  - 音量按钮移动到停止按钮右侧；倍速、跳转秒数和播放模式改用音量同款的不抢占
    独立悬浮窗，修复菜单在按钮上持续悬停时反复闪烁的问题。
  - 播放模式按钮支持每次点击循环切换下一种模式，悬停列表的当前勾选项随按钮同步。
  - 纯音频播放新增实际 PCM 驱动的底部单曲线；24 个圆滑控制段只向上形成疏密适中
    的高波峰，并按当前声音强度快速升起和回落，暂停时停止。
  - 波形画布只保留文件名，移除内部进度轨道、百分比和说明文字；控制行主进度条不变。
  - 音频曲线固定使用 192 个绘制点并缓存静态背景，消除全屏时按画面宽度重复计算
    多层曲线造成的卡顿。
  - 音频画面开放全屏，同一个动态图标按钮可进入或退出，行为与视频全屏一致。
  - 长按右方向键临时以 2.0× 播放并显示 2.0×，松开后恢复用户选择的倍速。
  - 倍速时钟重建仅用于纯音频；视频使用 30ms 本地缓存，切换倍率不再跳转或明显卡顿。
  - 新增 `Ctrl+左/右` 切换上一首/下一首快捷键，并保持普通方向键原有行为。
  - 封存前 Debug/Release 全量 CTest 均为 44/44、核心独立为 30/30，无测试构建成功。
  - FLAC、MKV 等其他真实格式与 125%/150% 实际缩放保留为已知遗留，不记为通过。

## 版本计划

| 版本 | 主题 | 状态 |
|---|---|---|
| v0.1.0 | 本地音视频播放，libVLC 内核 | 本地自用已封存 |
| v0.2.0 | URL 直播播放、缓冲与断流处理 | 本地自用已封存 |
| v0.3.0 | 本地音频、本地视频与直播界面重构 | 本地自用已完成 |
| v0.4.0 | 状态持久化、直播源历史与快捷键帮助 | 本地自用已完成 |
| v0.5.0 | 直播搜索、收藏持久化、最近播放与断点续播 | 本地自用已完成，ZIP 在仓库外保留 |
| v0.6.0 | WebView2 浏览器、多标签、性能优化与 Edge 风格界面 | 本地自用已完成 |
