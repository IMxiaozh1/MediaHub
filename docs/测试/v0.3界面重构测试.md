# MediaHub v0.3 界面重构测试

> 状态：进行中，阶段 A 已完成。
>
> 最后更新：2026-08-07。
>
> 分类：测试。

## 1. 测试目标

界面重构必须证明两件事：三种场景具有独立且稳定的视觉模式；视觉切换没有改变现有
播放、列表、快捷键、悬浮层、全屏和关闭时序。自动化只验证可客观判断的结构与行为，
整体美感、真实画面合成和 Windows 实际 DPI 继续由人工验收。

## 2. 阶段 A：主题基础与控件契约

### 2.1 实现范围

- 新增独立主题模块和本地音频、本地视频、直播三种展示模式。
- 主窗口标题区改为场景标题栏，主题通过动态属性应用到既有控件。
- 保留全部既有 `objectName`、信号连接、Presenter 路由和快捷键。
- 新增模式往返测试，验证关键控件不会因换肤被销毁或替换。

### 2.2 自动化结果

| 验证 | 命令 | 结果 |
|---|---|---|
| Release 全目标构建 | `cmake --build cmake-build-release` | 通过 |
| Release 全量测试 | `ctest --test-dir cmake-build-release --output-on-failure` | `72/72` 通过，24.42 秒 |
| GUI 套件 | CTest `gui.stage10_coverage` | 通过，包含新增模式往返场景 |
| 空白检查 | `git diff --check` | 通过 |

构建必须在 VS 开发者环境中执行。普通 PowerShell 直接调用重新生成后的构建目录时，
`cl.exe` 无法取得 MSVC 标准库 `INCLUDE`；这属于本机环境要求，不是代码缺陷。

### 2.3 已通过验收项

| 编号 | 状态 | 证据 |
|---|---|---|
| U-01 | 通过 | 初始模式为本地视频，既有控件对象名全部可查找 |
| U-02 | 部分通过 | 本地音频会切换主题且控件身份不变；画布视觉待阶段 B |
| U-03 | 部分通过 | 本地视频会切换主题且控件身份不变；画布视觉待阶段 B |
| U-04 | 部分通过 | 直播标签切换主题且控件身份不变；完整直播视觉待阶段 C |
| U-05 | 通过 | 模式往返复用同一播放按钮、进度条和列表对象 |
| U-06 至 U-10 | 待后续阶段 | 按规划逐项回填 |

## 3. 后续测试边界

阶段 B 完成后回归音频/视频往返、波形、歌词、定位、倍速、全屏和列表折叠；阶段 C
完成后回归直播地址、远程清单、刷新、标记、收藏、失败保留和滚动位置；阶段 D 完成
最小尺寸、多分辨率、模拟 DPI、Debug/Release 全量测试及人工视觉验收。
