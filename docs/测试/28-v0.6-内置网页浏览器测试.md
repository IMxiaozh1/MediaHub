# MediaHub v0.6 内置网页浏览器测试

> 状态：实施中，Task 11 已完成；Task 12 尚未开始。
>
> 测试范围：第三展示模式、WebView2 导航、持久登录资料、安全策略、网页媒体、
> 全屏、生命周期、构建隔离和本地交付。

## 1. 测试原则

1. GUI 主流程使用 `FakeBrowserBackend`，不依赖 Edge Runtime、公共网络或真实账号。
2. 真实 WebView2 集成测试只访问 `127.0.0.1` 受控 HTTP 服务，Profile 使用运行时
   临时目录；RAII 先清理，套件结束时再有界复核并报告遗留数量。
3. 公共网站登录、视频画面、声音、DRM 和网站兼容性只做人工验收，不伪装为自动化。
4. 测试和日志不得包含账号、密码、Cookie、令牌、真实浏览记录或个人 Profile 路径。
5. 不提交有版权音视频。受控网页使用脚本生成的静态内容或运行时生成的无版权媒体。
6. 异步测试等待明确事件并设超时上限，禁止固定时长 `sleep` 或无条件 `qWait`。

## 2. 计划自动化场景

### 2.1 模式与原生播放协调

- 第三个“网页”入口存在，往返后本地/直播列表的条目、当前项和滚动位置不变。
- 进入网页时，Playing、Opening 和 Buffering 分别只产生一次暂停请求。
- 已暂停、已停止、播放结束或空闲时进入网页不产生多余内核命令。
- 离开网页不会自动继续 libVLC；网页后端收到静音和挂起请求。
- 返回网页恢复浏览器后端，但不发送脚本或模拟网页播放点击。
- 网页模式下原生进度、倍速、跳转、播放模式和全屏控件不可误操作。

### 2.2 地址栏与导航

- 空白输入不导航，无协议域名规范化为 HTTPS。
- 合法 HTTP/HTTPS 地址保持主机、端口、路径、查询和片段供当前页面展示。
- 用户明确选择的 `file:` 可以只读打开，网页后台发起的本地文件扩大访问被拒绝。
- 地址栏 `javascript:`、`data:` 和 WebView2 不支持的内部页被拒绝，但网站自身脚本、
  Data URL 子资源和 `blob:` 媒体正常工作。
- 外部应用协议展示目标并确认；取消时不启动应用，确认时只路由一次。
- 后退、前进、刷新/停止、主页命令准确路由到假后端。
- `Ctrl+L`、`Alt+左/右`、`Ctrl+R` 和 `F5` 只在正确窗口范围内生效。
- 导航状态变化会更新地址栏、标题、载入提示和按钮启用状态。
- 旧导航的迟到成功或失败事件不能覆盖当前页面状态。

### 2.3 初始化与错误

- WebView2 初始化成功后移除覆盖层并允许导航。
- Runtime 缺失、Profile 不可用、COM 模式错误和 Controller 创建失败分别显示稳定
  中文提示，主窗口和原生播放器仍可使用。
- 初始化中切换页面或关闭窗口不会重复创建后端，也不会访问已销毁控件。
- resize 和 DPI 连续变化只向后端提交有效的最新客户区尺寸。
- TLS 证书错误可以返回安全页或仅对当前会话继续，关闭后临时例外消失。
- 错误日志只有类别、HRESULT 数值和请求代次，不含完整 URL 或本地路径。

### 2.4 用户资料与清除

- 主页面与登录弹窗使用同一 Profile 标识。
- 正常重建浏览器后端时继续使用相同 MediaHub 用户数据目录。
- 测试 Profile 中设置的 Cookie 和 LocalStorage 在重建后仍可读取。
- Edge 个人资料目录从不作为 MediaHub Profile 输入。
- 密码自动保存关闭，MediaHub 配置快照中没有账号、密码、Cookie 或令牌字段。
- 取消“清除网页数据”不会调用后端；确认后只调用一次 Profile 清除命令。
- 清除成功后 Cookie、LocalStorage、IndexedDB 和缓存不可继续使用，并回到内部空白页。
- 清除失败显示可重试错误，不递归删除 Profile 目录。

### 2.5 弹窗、权限与下载

- 登录弹窗请求取得共享 Profile 的子浏览器，关闭后主页面继续存在。
- 同时最多创建 3 个子窗口，第 4 个请求被拒绝并显示说明。
- 主窗口关闭后不再接受新弹窗，现有子窗口先于主 Controller 释放。
- 摄像头、麦克风、位置、通知、屏幕捕获和剪贴板读取都显示真实来源和权限类型。
- “允许一次”“对本站记住”“拒绝”和关闭提示框分别准确回复原权限请求。
- 已记住权限只作用于对应来源，撤销或清除网页数据后重新询问。
- 下载可取消或选择目标，进度、完成和失败状态准确；同名目标不能静默覆盖。
- 文件上传只把系统选择器中当次明确选择的文件交给网页，取消时网页得不到路径。
- 权限、下载和弹窗日志不包含目标完整地址或网页内容。

### 2.6 网页全屏与快捷键隔离

- 网页全屏进入时隐藏 MediaHub 页头、侧栏和网页工具栏，只保留网页内容。
- `Esc` 先退出网页全屏，不触发现有 libVLC 全屏、停止或播放快捷键。
- 网页退出全屏后恢复此前模式、窗口状态和响应式尺寸。
- 网页输入框获得焦点时，空格和方向键由网页处理，不触发原生播放快捷键。
- 网页页以外的快捷键行为保持 v0.5 语义。

### 2.7 生命周期与关闭顺序

- 关闭后端前先清空事件监听器并递增请求代次。
- 事件令牌、弹窗、主 Controller、Environment 和 COM 生命周期按设计顺序释放。
- 关闭后到达的初始化、导航、全屏和清除数据回调全部丢弃。
- 浏览器关闭不会等待 WebView2 子进程，不阻塞 Qt 主线程。
- 有界检查确认 MediaHub 进程退出；测试不得结束系统 Edge 或其他程序的 WebView2
  进程。

## 3. 真实 WebView2 集成测试

计划新增独立的 Windows 集成测试目标，使用本机 Evergreen Runtime 和临时 Profile。
受控 HTTP 服务提供以下页面：

1. 静态标题、链接和重定向，用于导航、前进和后退。
2. 设置与读取 Cookie、LocalStorage、IndexedDB 的页面，用于重建和清除验证。
3. 调用 `window.open()` 的页面，用于共享 Profile 和弹窗生命周期。
4. 请求麦克风和下载的页面用于确认拒绝、写入和取消；上传页面只确认受控控件可加载。
5. 运行时生成的短无版权媒体页面确认 metadata；全屏和外部协议页面只确认入口可加载。
6. 本机自签名 HTTPS 页面，用于确认返回安全页和当前会话临时例外。

集成测试满足以下限制：

- 服务只监听 `127.0.0.1` 随机端口，不访问公网。
- 每个等待都绑定导航、DOM 或 WebView2 完成事件并有明确超时。
- Profile 只位于测试临时目录，测试开始和结束时不扫描用户浏览器目录。
- 声音和画面效果不作为自动化结论；自动化只确认事件、状态和资源释放。
- Runtime 缺失时该目标应给出明确环境结果；完整交付回归不得静默跳过。

## 4. 构建与静态检查矩阵

| 配置 | 计划命令 | 通过条件 |
|---|---|---|
| Debug 全量 | `cmake --build cmake-build-debug` 后运行 CTest | 全部目标和测试通过 |
| Release 全量 | `cmake --build cmake-build-release` 后运行 CTest | 全部目标和测试通过 |
| Core-only | `MEDIAHUB_CORE_ONLY=ON` | 不查找 Qt、libVLC 或 WebView2，全部核心测试通过 |
| 无测试 Release | `BUILD_TESTING=OFF`、`MEDIAHUB_BUILD_TESTS=OFF` | 生成可运行程序，CTest 为 0 |
| 三档 GUI | 100%、125%、150% | 三档完整 GUI 测试通过 |
| 隔离发布目录 | Release 安装到空目录 | 可启动；许可证齐全；不含 Profile 或测试数据 |

静态检查包括：

- `git diff --check`、Tab 和项目源码 100 字符扫描。
- 核心与 GUI 支持库不得出现 WebView2、COM 或 libVLC 直接依赖。
- WebView2 头文件只出现在适配目标，第三方文件保持上游原貌。
- 日志和测试源码扫描完整 URL、查询参数、Cookie、Authorization、Profile 路径和
  密码字段。
- Release 目录只包含 x64 Loader、必要许可证和应用运行文件，不包含 x86/ARM64、
  WebView2 测试程序、用户数据目录或缓存。

## 5. 人工验收计划

真实账号和公共网站由用户自行输入，地址、账号和画面不写入本文档。每项验收都使用
最新 Release，测试结束后确认应用可以正常关闭。

| 编号 | 场景 | 预期 | 状态 |
|---|---|---|---|
| W-M01 | 在本地、直播、网页三个入口间往返 | 页面切换正确，两个列表内容不变 | 待验收 |
| W-M02 | 输入用户选择的 HTTPS 网站 | 页面能够导航，地址和标题更新 | 待验收 |
| W-M03 | 使用后退、前进、刷新和地址快捷键 | 行为接近 Edge 基础导航 | 待验收 |
| W-M04 | 登录用户自己的合法网站 | 登录表单和二次验证可正常操作 | 待验收 |
| W-M05 | 正常关闭并重新启动 MediaHub | 网站允许时保持登录 | 待验收 |
| W-M06 | 同时查看系统 Edge | 两者登录资料互不自动共享 | 待验收 |
| W-M07 | 确认并清除网页数据 | 内置网页退出账号，Edge 不受影响 | 待验收 |
| W-M08 | 播放普通网页视频 | 网页内有画面和声音，控件可操作 | 待验收 |
| W-M09 | 播放用户选择的网页直播 | 网站允许时直播能够持续观看 | 待验收 |
| W-M10 | 进入网页视频全屏并按 `Esc` | 正常进入和退出，布局恢复 | 待验收 |
| W-M11 | 完成一次需要弹窗的登录 | 弹窗共享会话，关闭后主页面正常 | 待验收 |
| W-M12 | 原生媒体播放中进入网页 | 原生媒体暂停，不与网页同时出声 | 待验收 |
| W-M13 | 网页播放中切回本地或直播 | 隐藏网页不出声，原生媒体不自动续播 | 待验收 |
| W-M14 | 下载一个测试文件并选择一个文件上传 | 保存、进度、同名确认和选择边界正确 | 待验收 |
| W-M15 | 100%、125%、150% Windows 缩放 | 工具栏、页面、弹窗和全屏无裁剪 | 待验收 |
| W-M16 | 关闭含视频和登录弹窗的应用 | 窗口及时关闭，无 MediaHub 残留进程 | 待验收 |
| W-M17 | 分别允许一次、记住和拒绝站点权限 | 来源显示、决定作用域和撤销正确 | 待验收 |
| W-M18 | 打开本地页、外部协议和自签名测试页 | 都先确认，取消与当前会话继续正确 | 待验收 |

## 6. 网站兼容性记录规则

测试结果按能力而不是网站品牌作普遍承诺：

- “通过”只说明该次环境、Runtime、网站和账号组合可用。
- 网站拒绝 WebView2、DRM 不可用或地区限制时，记录为兼容性限制，不绕过限制。
- 网站自身宕机、账号风控或网络不稳定不归类为 MediaHub 自动化失败，但必须复核主
  窗口仍响应、错误可恢复且没有敏感日志。
- 不在仓库记录真实账号、付费状态、私人页面、完整访问地址或版权内容截图。

## 7. 阶段结果

实现过程中每个增量都要回填实际命令、总用例数、耗时、失败原因和人工结论，不能用
“计划通过”替代真实证据。

| 项目 | 当前结果 |
|---|---|
| WebView2 SDK 校验 | 1.0.4129.50 文件、签名、哈希与许可证已核对 |
| Debug/Release 构建 | 两套构建通过；沙箱 CTest 均 `80/81`，唯一 WebView2 90 秒超时 |
| GUI 假后端测试 | 浏览器 GUI `27/27`、主 GUI `95/95`，通过 |
| WebView2 本机集成测试 | 普通环境 Debug/Release 均 `44/44`，12.021/11.276 秒，零跳过 |
| Task 9 规定组合 CTest | 安全加固后 `3/3` 通过，47.27 秒 |
| Core-only 与无测试构建 | Core-only `52/52`，7.43 秒；无测试 Release 为 0 项 |
| 三档缩放 | 自动化四项 GUI CTest `4/4`；真实 Windows 观感仍待 W-M15 |
| 隔离 Release | 400 个文件、365 个 VLC 插件，法律材料齐全，发布校验通过 |
| W-M01 至 W-M18 | 待用户人工验收 |

Task 3 采用独立 `mediahub_browser_gui_tests` Qt Test 目标，避免与既有主窗口测试的
`QTEST_MAIN` 冲突。失败阶段先确认缺少 `browser_navigation_policy.h` 时编译失败；
实现后执行 `ctest --test-dir cmake-build-debug -R "gui.browser_page"
--output-on-failure`，结果为 `1/1` 通过，用时 0.17 秒。边界扫描只在浏览器抽象注释中
命中 WebView2/COM 名称，核心层和 GUI 支持实现没有包含 SDK 或 COM 类型。

Task 4 先确认缺少 `browser_page.h` 时新测试编译失败，再实现独立浏览器状态、地址和
导航工具栏、原生宿主、清除网页数据确认及稳定中文错误覆盖层。浏览器测试程序内部
6 项全部通过；与既有主窗口回归组合执行 `gui.stage10_coverage|gui.browser_page` 为
`2/2` 通过，用时 50.77 秒。旧导航代次不能覆盖当前地址或标题，取消清除不会调用
后端，确认后只调用一次，清除失败仍显示可重试提示。

Task 5 先让三模式测试在缺少新构造入口时失败，再将中央区域改为“模式入口栏 + 原生
播放页/网页页栈”，顶部入口始终可见；`MainWindow` 接受可注入后端，空后端只显示
Runtime 缺失提示。Presenter 进入网页时对 Opening、Buffering、Playing 各暂停一次，
重复点击、Paused 和离开网页均不重复或自动播放；网页页隐藏时同时静音、挂起和隐藏
后端。既有页签切换仍保持本地/直播模型隔离，`gui.stage10_coverage` 与浏览器测试
组合为 `2/2` 通过（31.09 秒），新增模式与状态场景定向运行通过。

Task 6 先新增独立 `mediahub_webview2_tests`，首次修正测试目标 Qt Widgets 链接后，
指定构建真实失败于 `webview2_browser_backend.h` 不存在，确认 RED 来自真实后端缺失。
实现后新增隔离的 `mediahub_browser_webview2` 静态目标；公开头仅暴露 PImpl，COM 与
WebView2 SDK 类型只存在于适配器实现。环境和 Controller 在 GUI STA 异步创建，所有
完成处理器检查生命周期令牌、生命周期序号和请求代次；导航用 WebView2 navigationId
保存代次，旧导航取消回调不会覆盖新导航。事件订阅由 RAII 撤销，关闭顺序为撤销事件、
关闭 Controller、释放 WebView/Controller/Environment 和 COM 引用，不等待子进程。

本阶段启用网页脚本和普通上下文菜单，关闭 DevTools、密码保存和常规自动填充；没有
Host Object、文档启动脚本或脚本注入。生产组合根使用
`AppLocalDataLocation/WebView2/Profile-v1`，日志只记录稳定事件名和 HRESULT，不记录
完整 URL 或 Profile 路径。使用指定 VS `vcvars64.bat` 环境构建
`mediahub_webview2_tests mediahub_gui` 成功；最终
`ctest --test-dir cmake-build-debug -R "webview2" --output-on-failure` 为 `2/2`
通过，Runtime 探针 0.02 秒、真实初始化 7.16 秒，总用时 7.22 秒。GUI 定向回归
`gui.stage10_coverage|gui.browser_page` 为 `2/2` 通过，总用时 31.99 秒。权限、下载、
弹窗和环回站点持久数据验证仍按 Task 7 至 Task 9 保持未完成。提交前组合复验覆盖
Runtime 探针、真实初始化和两项 GUI 回归，为 `4/4` 通过，总用时 39.65 秒。

Task 6 规格复核发现：仅保留 `answer*` 空实现不能阻止 WebView2 对未订阅敏感事件采用
默认行为。修复先增加结构门禁测试，要求权限、下载、证书错误、外部 URI 和新窗口五类
处理器都在 ready 前注册；RED 稳定失败于 `registerPermissionRequested()` 缺失，定向
结果为 `2` 项通过、`1` 项失败。实现随后对基础 PermissionRequested 立即设置 DENY，
通过 `ICoreWebView2_4` 取消并接管 DownloadStarting，通过 `ICoreWebView2_14` 取消
证书错误，通过 `ICoreWebView2_18` 取消外部 URI，并接管拒绝 NewWindowRequested。
任一版本接口查询或事件注册失败都会终止初始化、撤销已有订阅并关闭 Controller，不能
进入 ready。五类订阅全部由 `EventRegistration` 管理，并在 Controller Close 前于创建
COM 对象的 GUI STA 撤销；对应函数已补充调用线程和回调线程约束。

GREEN 阶段结构测试通过，真实 Evergreen Runtime 初始化同时证明 `_4`、`_14`、`_18`
及全部事件注册可用；测试程序内部 `4/4` 通过（6.717 秒、无 warning）。最终组合 CTest
覆盖 Runtime 探针、真实初始化、安全门禁和两项 GUI 回归，为 `4/4` 通过，总用时
36.87 秒。Task 6 不执行可能在 RED 阶段写入用户下载目录的端到端下载测试；受控环回
站点、下载目标和系统副作用验证仍由 Task 7 与 Task 9 完成，此处不据结构测试宣称这些
端到端场景已经通过。

Task 6 提交后的代码质量复核又发现两个异步关联缺陷和一项测试假绿。挂起实现原先在
`TrySuspend` 未完成时立即执行恢复，后到的挂起完成可能把已重新激活的网页挂起；现在
使用独立纯状态协调器维护 desired、actual、in-flight 和 request serial。恢复请求会在
挂起完成前保持静音，确认确实挂起后只发一次纠正 `Resume`，且仅在 `Resume` 成功后
解除强制静音；HRESULT 失败、`BOOL` 为 false、重复请求、旧 serial 和生命周期失效均
有确定性行为测试。对应 RED 为测试编译失败于缺少 `webview2_state.h`，GREEN 后挂起、
失败与迟到三组场景通过。

显式 `Navigate` 成功接受后现在立即进入 navigating，并把 generation 放入 FIFO；首次
收到新 navigationId 时才消费一个 generation，同 ID 重定向复用原绑定。无显式待请求的
后退、前进和刷新沿用当前 generation，清除资料推进 generation 时也同步追踪器；只有
active navigationId 的 Completed 才读取成功状态、上报错误或生成快照，旧 ID 只清理
映射。导航 RED 编译失败于
`NavigationTracker` 未声明，GREEN 覆盖连续 A/B、重定向、Starting 前 Stop、历史导航
和旧 A 失败晚到完全不投递。

默认拒绝测试不再通过全文件任意 `put_Cancel` 文本判定。五类拒绝动作提取为生产内部
窄策略，fake event args 分别精确记录 permission state、download cancel/handled、
certificate action、external cancel 和 popup handled 的参数及调用次数。初始化测试先
调用 `GetAvailableCoreWebView2BrowserVersionString`：本机 Runtime 存在时必须 Ready 且
无错误，确实缺失时只接受 RuntimeUnavailable。该轮 RED 编译失败于缺少
`webview2_default_deny.h`；GREEN 后仅保留注册函数段、ready gate 和
`releaseBrowserResources` 中 reset-before-Close 的辅助门禁。当前没有可注入的真实
注册失败后端，因此“注册失败不 Ready”的自动化仍是精确函数段门禁加本机真实注册，
不宣称为注入式端到端覆盖。

低风险收口同样保留 RED 证据：移除测试对 GUI 支持库的显式依赖后先失败于找不到
`QApplication`，适配器随后以 PUBLIC usage dependency 暴露 `mediahub_gui_support`，
WebView2 SDK 与 Windows COM 库仍为 PRIVATE；空/相对 Profile 的稳定错误测试先实际得到
InitializationFailed，现统一为 ProfileUnavailable/E_INVALIDARG，且测试使用空 HWND，
未启动 Runtime 或创建 Profile。组合根通过纯路径函数拒绝空/相对
AppLocalDataLocation，不再拼接工作目录相对路径。NewWindow 函数段测试先确认默认拒绝
仍调用 `onPopupRejected`，现只设置 Handled，不再显示“登录弹窗数量已达上限”。

最终使用 VS x64 环境构建 `mediahub_webview2_tests mediahub_gui` 成功；测试程序内部
`13/13` 通过（6.580 秒、零跳过），Runtime 探针、GUI 阶段覆盖、浏览器 GUI 与真实
初始化组合 CTest `4/4` 通过（35.47 秒）。下载文件、系统权限、外部应用、证书和弹窗
端到端副作用仍明确留在 Task 7 至 Task 9，不因本轮行为测试而改为已完成。

Task 7 已按测试先行完成来源感知的权限、外部协议、证书错误和安全下载。GUI 各轮 RED
分别明确失败于权限对话框不存在、外部协议确认框不存在、下载条不存在以及保存窗口空
选择处理接口不存在；GREEN 后权限窗口显示规范化 `scheme://host[:port]` 和中文权限名，
“仅本次允许”“对此来源记住允许”“拒绝”、关闭与超时都只回答当前 requestId 一次。
未知权限禁用两个允许按钮；屏幕捕获仅启用“仅本次允许”和“拒绝”，记住按钮禁用并显示
Runtime 限制说明。导航、清除资料、关闭和新请求替换会先安全拒绝旧请求，旧按钮与旧
timeout 的迟到信号不影响新请求。外部协议与证书确认同样使用当前请求检查，默认分别
保持 Cancel 和返回安全页。

下载条使用单任务可恢复展示，覆盖建议文件名、来源、已知或未知大小、进度、取消、完成、
失败和取消状态。测试临时目录证明空值、相对路径、目录、父目录不存在、Windows 不可接受
文件名和已存在文件都不会调用 `chooseDownloadPath`；只有父目录存在且目标不存在的绝对
路径恰好提交一次。保存窗口返回空值会恰好取消一次；大于普通文件范围的字节计数也不会
在百分比计算中溢出。测试只创建临时占位文件验证不覆盖，不触发真实网页下载或写入用户
目录。Failed 与 Cancelled 两个终态均有行为断言；新 requestId 建立后，旧下载更新不会
覆盖当前任务。未选择路径的下载在导航或清除资料时取消，已提交路径的 active 下载不会
因普通导航被取消。

真实适配器 RED 先失败于待决定请求策略头不存在，再失败于 handler 缺少 `GetDeferral`。
GREEN 后标准权限、屏幕捕获、外部协议、证书和下载 pending 都携带 lifecycle serial、
generation 和 requestId；`take` 后第二次
回答为无操作，30 秒 `QTimer::singleShot` 超时回调先检查弱生命周期令牌，再执行安全决定。
关闭时在撤销事件和 Controller Close 前对全部 store `takeAll`，逐项 DENY/CANCEL、调用
下载 operation `Cancel` 并 `Complete`，不会直接丢弃 deferral。Pinned SDK 中
`put_SavesInProfile` 实际位于 `ICoreWebView2PermissionRequestedEventArgs3`，因此记住允许
设置 TRUE，仅本次允许设置 FALSE；Profile 清除仍使用 `ALL_PROFILE`，随后异步调用
`ClearServerCertificateErrorActions`。

下载开始立即 `Handled=TRUE`、`Cancel=TRUE` 并取得 operation 和 deferral；来源只保留
规范化 origin，建议路径只取 `QFileInfo(...).fileName()`。后端在接受路径前重复执行绝对
路径、父目录、目标不存在和 Windows 文件名校验；合法路径才设置 ResultFilePath、解除
Cancel 并 Complete。BytesReceivedChanged 与 StateChanged 映射为稳定进度和终态，终态
在事件回调返回后撤销 token 并释放 operation。生命周期失效的安全事件回调不访问
`this`，而是使用窄默认策略拒绝；日志仍只有固定事件类别和 HRESULT。

规格复核新增三组可执行 RED/GREEN。下载启动事务最初编译失败于缺少
`startDownloadTransaction` 和 operation-aware 路径决定；GREEN 后可控注册失败、存储失败
及二次静态路径校验失败都断言 args Cancel、operation Cancel、deferral Complete 和订阅清理恰好
执行一次。标准权限顺序门禁最初失败于 base deferral 不在初始 DENY 之后；GREEN 后固定为
State DENY、base GetDeferral、Args2 Handled、Args3、来源/监听器校验，无效 origin、空
listener 或 Args3 不可用都通过专用拒绝函数完成已有 deferral。即使 GetDeferral 返回失败
HRESULT，只要给出非空 deferral 也仍会安全 Complete。

屏幕捕获策略 RED 先失败于决策函数缺失，GUI RED 失败于记住按钮仍可用，ready 门禁 RED
失败于缺少 `registerScreenCaptureStarting()`。GREEN 后只注册 `ICoreWebView2_27` 的主
WebView 事件，先 Cancel/Handled，再从 `OriginalSourceFrameInfo::get_Source` 规范化来源并
取得 deferral；AllowOnce 才解除 Cancel，Deny 和 RememberForOrigin 都保持取消并 exact-once
Complete。正常 Windows 平台的 Runtime 151 真实初始化通过，证明本机接口查询和事件注册
可用；真实网页触发屏幕捕获仍未自动化。

后续质量审查按五个独立 RED/GREEN 小循环收口。DownloadStarting 准备测试先编译失败于
缺少 `prepareDownloadRequest`；GREEN 后 Cancel、Handled、get operation 和 GetDeferral
无论前一步 HRESULT 都各执行一次，setter/getter 单独失败及组合失败均保留首错，并对已
取得的 operation/deferral 分别 Cancel/Complete。证书和外部协议准备函数也覆盖 setter
失败、GetDeferral 失败但返回非空对象及组合失败，已有 deferral 均 exact-once Complete。

GUI 取消 RED 证明点击按钮会过早变成 terminal 并接受新下载；GREEN 后保持 Cancelling，
只有 Completed、Failed 或 Cancelled 才释放单任务槽位。`CancelFailed` 的编译 RED 后新增
“取消失败，可重试”非终态；后端同步 Cancel 失败恢复可重试标志并投递该状态，shutdown
无论此前是否请求过取消都再次调用 operation Cancel。下载快照 RED 缺少 state-first helper；
GREEN 后 Completed+bytes 失败、Interrupted+total 失败仍投递终态并排队释放，旧 lifecycle
或 shutdown 后的排队 erase 不触碰新集合。

清除资料状态机 RED 缺少内部空白导航协调器；GREEN 后 Profile 和证书动作成功只请求
`about:blank`，不立即报告成功，只有匹配 navigationId 完成才调用
`onBrowsingDataCleared`。旧页面、其他 navigationId、内部导航失败或状态读取失败不会更新
普通地址/标题，失败稳定归类为 ClearDataFailed。假后端同时证明旧 generation 不清空界面，
当前 generation 完成后地址与标题才变为空白状态。因此本阶段不再把“只清 Profile”表述为
已经退出当前仍驻留 document 的账号。

外部协议旧二参数签名先产生编译 RED；GREEN 后稳定事件携带规范化 origin 和 target，GUI
严格拒绝带路径的伪 origin，并分别显示请求来源与目标，说明由 Windows 默认应用处理。
Windows 保留设备名测试先对 `CON.foo.bar`、`NUL.anything` 失败，现按首个 `.` 前段判断。
二次路径检查只能证明静态校验；WebView2 直写最终路径仍存在并发同名创建或 junction 替换
的 TOCTOU 残余，Task 9 再做“检查后创建同名目标”和目录联接实测，本地自用威胁模型下
保留该已知风险，不声称原子保证。

第二轮收口使用指定 VS x64 环境构建
`mediahub_browser_gui_tests mediahub_webview2_tests mediahub_gui` 成功。浏览器 GUI 测试程序
内部 `19/19` 通过，WebView2 测试程序内部 `23/23` 通过；
`ctest --test-dir cmake-build-debug -R "gui\\.browser_page|webview2" --output-on-failure`
最终 fresh 复验为 `3/3` 通过（6.81 秒），主 GUI `gui.stage10_coverage` 为 `1/1` 通过
（28.94 秒）。
`git diff --check`、模块链接边界和敏感日志扫描通过。

第三轮复核发现，尚未选择保存路径时点击取消会直接移除 pending 并同步调用 Cancel，
但没有先注册 StateChanged/BytesReceivedChanged，也没有存入 active 集合。Cancel 成功后
GUI 因此停在 Cancelling 且永远收不到真实 Cancelled；任一同步收口失败也不会投递
CancelFailed，operation 又已随 pending 释放，无法重试或在 shutdown 再次取消。

本轮 RED 新增生产内部待选路径取消事务，首次构建准确失败于缺少
`cancelPendingDownloadTransaction`、outcome 和 action。GREEN 后固定顺序为先注册终态事件、
再存 active、最后各一次设置 args Cancel、调用 operation Cancel 和完成 deferral。可观察且
收口成功时等待真实 WebView2 终态；任一收口失败时保留 active、恢复可重试标志并投递
CancelFailed，重试和 shutdown 都只再次调用 operation Cancel，不重复 args 或 deferral。
Interrupted 仍通过 state-first 快照映射 Cancelled，并由已有排队 erase 释放订阅和 operation。

事件注册或 active 存储失败时会先撤销已有 token。同步取消成功因无法可靠观察终态而直接
投递一次 Cancelled；同步取消失败则把无订阅 operation 保存在独立 retry 集合并投递
CancelFailed，后续重试成功直接投递 Cancelled，关闭时仍会再次 Cancel。GUI 回归明确覆盖
未调用 `chooseDownloadPath` 的等待路径状态：点击取消保持非终态、拒绝新下载，只有后端
Cancelled 后释放单任务槽；CancelFailed 后同一 requestId 可再次取消。

本轮 fresh 验证中三目标构建成功；浏览器 GUI 测试程序内部 `19/19` 通过（0.134 秒），
WebView2 测试程序内部 `26/26` 通过（6.536 秒、零跳过）。Runtime 探针、浏览器 GUI 与
真实初始化组合 CTest 为 `3/3` 通过（6.83 秒），主 GUI `gui.stage10_coverage` 为 `1/1`
通过（28.98 秒）。模块边界扫描仍为 core 零 Qt/libVLC/WebView2、GUI support 零 libVLC，
新增日志只通过固定事件名和 HRESULT 进入既有日志门禁；真实下载写入仍未自动化。

本阶段不把系统副作用伪装为自动化覆盖：真实摄像头/麦克风/位置/通知/剪贴板设备授权、
真实文件下载写入、外部应用启动和自签名 HTTPS 端到端仍等待 Task 9 受控环回站点或人工
验收。固定 SDK 没有 `COREWEBVIEW2_PERMISSION_KIND_SCREEN_CAPTURE` 枚举，因为屏幕捕获
使用独立的 `ScreenCaptureStarting` 事件；本阶段自动化只覆盖注册、默认拒绝、来源规范化
门禁、决策和生命周期完成路径，不宣称已完成真实页面端到端捕获。上传、登录子窗口和
网页全屏仍属于 Task 8/Task 9，未在 Task 7 扩展。

Task 8 已完成登录子窗口、网页全屏、网页模式挂起和确定性关闭的自动化实现。弹窗容量
行为测试验证主页面与嵌套请求统一最多预约 3 个窗口、shutdown 后拒绝新预约并可在新
生命周期重置；弹窗事务测试验证 Handled、GetDeferral、NewWindow、Handled、Complete
顺序和 exact-once 完成。真实适配器的门禁同时检查同 Environment、显式同 Profile 名称、
全部敏感 handler 在交付子 WebView 前注册，以及子窗口先于主 Controller 关闭。弹窗内
权限、屏幕捕获、下载、证书和外部协议当前默认拒绝，不把它们误报为已有完整确认 UI。

GUI 行为测试验证切出网页的“静音、挂起、隐藏”和切回网页的“显示、恢复、解除静音”
顺序；网页全屏隐藏网页工具栏、信息行和下载条，新下载也不会在全屏中露出。主窗口进入
网页全屏后隐藏页头、模式入口、侧栏和菜单，`Esc` 只请求退出网页全屏，退出事件恢复原
窗口状态与布局。假后端命令序列验证 listener 断开和登录子窗口关闭均早于后端 shutdown；
WebView2 生命周期门禁验证事件 token、子 Controller、主 Controller 与 Environment 的释放
边界，迟到回调继续依赖弱生命周期令牌、generation 和 serial 丢弃。

各轮 RED 实际失败于缺少 Esc 退出、容量协调器、ClosePopups 命令、全屏控件隐藏、主窗口
联动、安全 handler、全屏下载条保护、弹窗事务及失败 HRESULT 传播。最终使用 VS x64 环境
构建 `mediahub_browser_gui_tests mediahub_webview2_tests mediahub_gui` 成功；浏览器 GUI
测试程序内部 `23/23` 通过（0.385 秒），WebView2 测试程序内部 `28/28` 通过（6.501 秒、
零跳过）。`gui.stage10_coverage`、`gui.browser_page`、
`integration.webview2.initialization` 组合 CTest `3/3` 通过（30.30 秒），本机 Runtime 仍在
正常 Windows 平台真实初始化。diff check、模块链接边界和新增日志字段扫描通过。

W-M10、W-M11、W-M15 和 W-M16 仍保持待验收：真实网页 Fullscreen API、OAuth/SSO 会话共享、
100%/125%/150% Windows DPI 和含视频/登录弹窗的进程退出必须由 Task 9 环回站点或人工
验证。当前自动化不宣称覆盖真实账号、第三方站点兼容性、音画观感或浏览器子进程退出
时延。

Task 8 规格复核补充三项生命周期回归。`shutdownPermanentlyRejectsReinitialization`
先因缺少永久生命周期门禁产生编译 RED；GREEN 后纯 gate 和真实 backend 均证明 shutdown
后 initialize 不报告 Ready/Error，也不重新拉起资源，同时既有首次 initialize 与 Profile
错误分类测试保持通过。`controllerCompletionClosesOnlyStaleController` 先因完成事务缺失
产生编译 RED；fake controller 精确断言失效完成 Close 一次、有效完成 Close 零次、空指针
安全丢弃。

`shutdownFullScreenExitRemainsReachableAndOrdered` 先因 shutdown 提交事务缺失产生编译 RED；
补齐事务测试缝后继续得到行为 RED：`releaseBrowserResources` 中全屏退出调用不存在，结果
为 `2 passed, 1 failed`。GREEN 后请求动作恰好执行一次并保留 HRESULT，释放函数段顺序
固定为 shutdown 全屏请求、关闭 popup、撤销首个主 token、Close 主 Controller。公开 Esc
退出与关闭退出共用幂等脚本，完成回调只按弱生命周期/serial 处理，不等待 Runtime。

修复后的 fresh 三目标构建成功；浏览器 GUI 测试程序内部 `23/23` 通过（0.401 秒），
WebView2 测试程序内部 `31/31` 通过（6.593 秒、零跳过）。
`gui.stage10_coverage`、`gui.browser_page`、`integration.webview2.initialization` 组合 CTest
`3/3` 通过（30.30 秒）。真实 child HWND 的 `Esc` 路由不在本修复伪装为自动化覆盖，继续
保留在 Task 9/人工验收。

Task 8 质量复核继续用三个独立行为缝修正异常关闭路径。
`controllerCompletionAdoptsOnlyCurrentSuccess` 的首轮 RED 编译失败于缺少 Controller
adoption transaction；补齐行为缝后，生产接线门禁仍为 `2 passed, 1 failed`，准确指出主
完成回调未使用事务。GREEN 后覆盖迟到成功、当前失败、当前成功和空指针组合：只有当前
成功的非空 Controller 可被接管，其余非空对象均 Close 一次，并同时检查主页面与 popup
都接入事务。

`popupSafetyFailuresCloseBeforeReturning` 的首轮 RED 编译失败于缺少屏幕捕获取消和统一
popup 安全完成 helper；补齐行为缝后，接线门禁仍为 `2 passed, 1 failed`。测试覆盖权限、
屏幕捕获、下载、证书和外部协议 setter 失败，屏幕捕获与下载还证明 Cancel 失败时仍尝试
Handled。额外审查发现安全 setter 失败到延后关闭之间不能证明 fail-closed，于是先把测试
改为要求 handler 返回前同步关闭；RED 准确失败于旧 helper 调用约定，GREEN 后单项
`3/3` 通过。最终五个 handler 都是同步 setter，不调用 `GetDeferral` 或 `Complete`；拒绝
写入失败会同步关闭 Controller，popup 对象只由 owner 在回调栈退出后延迟 erase。

`popupFailureReclamationHasSingleOwner` 的首轮 RED 编译失败于缺少同步/异步失败时序和
单一回收 helper；接线门禁随后得到 `2 passed, 1 failed`。GREEN 行为缝复现同步失败立即由
调用方回收、下一 popup 复用相同模拟地址且不被旧回调删除，以及异步失败只排队一次 owner
回收；容量和释放计数对每个 popup 都只发生一次。

最终三项质量定向组合 `5/5` 通过（11 ms），fresh 三目标构建成功；浏览器 GUI 完整程序
`23/23`（0.416 秒），WebView2 完整程序在默认 Windows 平台 `33/33`（6.558 秒、零跳过）。
规定组合 CTest `3/3` 通过：`gui.stage10_coverage` 28.26 秒、`gui.browser_page` 0.51 秒、
`integration.webview2.initialization` 1.02 秒，总用时 29.81 秒。diff check、核心依赖、
GUI support 链接边界和新增日志字段扫描通过。

本轮仍不把真实环境行为冒充为自动化：真实 child HWND 获得焦点后的 `Esc` 路由、真实
OAuth/SSO 弹窗共享、网页 Fullscreen API、100%/125%/150% Windows DPI，以及含网页视频
和登录弹窗关闭后的 MediaHub 进程观察，继续等待 Task 9 环回站点或人工验收。程序化离开
Web 模式也尚未显式退出网页全屏，作为已记录的低风险残余保留到后续阶段。

Task 9 已建立只监听 IPv4 `127.0.0.1` 随机端口的确定性 HTTP 测试服务。服务只接受一个
有界 GET 请求头，提供持久 Cookie、LocalStorage、IndexedDB、302 重定向、共享 Profile
弹窗、麦克风请求、受控下载、上传控件、运行时生成的静音 WAV、Fullscreen API 入口和
外部协议链接。测试 Profile 和下载目标均使用临时目录；后端还把默认下载目录固定到
Profile 的 `Downloads` 子目录，防止预取消链路回归时写入用户 Downloads。服务与日志不
记录完整 URL、Profile 路径、Cookie 值或凭据。

测试先行的首次有效 RED 使用 VS x64 环境构建，准确失败于
`support/local_web_test_server.h` 不存在。实现后首次完整默认平台运行得到 `40/41`：唯一
失败固定分类为 `cookie=0;local=1;indexed=1`，证明 Profile、LocalStorage 和 IndexedDB
已经跨后端持久，原测试 Cookie 因没有 `Max-Age` 而只是会话 Cookie。只把测试 Cookie
改为 `Max-Age=3600` 后，持久与清除定向测试为 `3/3`（1.641 秒），没有延长 10 秒条件
等待，也没有改产品代码或放宽断言。

最终在普通 PowerShell、默认 Windows 平台执行完整 `mediahub_webview2_tests.exe`，安全
加固后 `41/41` 通过（9.399 秒、零跳过）。覆盖 Cookie、LocalStorage、IndexedDB 跨后端持久及
`ALL_PROFILE` 清除、loopback 重定向、弹窗共享 Profile、麦克风拒绝、受控下载写入与
取消，以及上传/媒体/全屏/外部协议页面可加载。非 Runtime 的纯逻辑与本地服务组合为
`33/33`（61 ms）；规定 `gui.stage10_coverage`、`gui.browser_page`、
`integration.webview2.initialization` CTest 安全加固后为 `3/3`（47.27 秒）。

Codex 受限运行环境曾使 WebView2 GPU 子进程以 `0xC0000022` 退出、Renderer 启动失败；
临时 ProcessFailed 诊断证明这属于沙箱子进程权限，并已全部移除。普通 PowerShell 的重定向
定向测试 `3/3`（1.438 秒）及最终 `41/41` 是 Runtime 结论依据。本阶段不把页面加载冒充
上传系统选择器、音画质量或用户手势全屏验证，也不声称覆盖真实 OAuth/SSO、公共站点、
DRM、实际 Windows DPI、真实 child HWND 的 `Esc` 路由、外部应用启动或自签名 HTTPS。
下载仍存在检查后同名创建和 junction 替换的 TOCTOU 残余；自动化只证明受控新路径写入
与取消，没有宣称原子创建保证。

质量审查先得到默认下载目录隔离的结构 RED `2/3`，随后在 Ready 前创建并设置
`<Profile>/Downloads`，失败即阻止 Ready；取消 Runtime 用例断言该目录在取消终态后仍空。
Profile 清理也从析构静默尝试改为套件结束二次有界清理，遗留只报告数量并使测试失败，
不输出 Profile 路径。上传、全屏和外部协议仍只证明入口页面加载，不表述为系统交互覆盖。

提交前另在 Codex 沙箱单独执行
`ctest --test-dir cmake-build-debug -R "webview2" --output-on-failure`：Runtime probe
通过，但 `integration.webview2.initialization` 在 CTest 的 90.04 秒上限超时，结果为
`1/2`，不计为通过。该现象只用于记录沙箱对子进程启动的限制，不归类为产品缺陷，也不
覆盖同版本普通 PowerShell 完整程序 `41/41`（9.399 秒、零跳过）和规定组合 CTest
`3/3`（47.27 秒）的产品门禁结论。

Task 10 为 BrowserPage 增加按页面可用宽度划分的 `compact`、`normal`、`large`、
`extraLarge` 四档属性，阈值分别为 900、1200 和 1500 像素。自动化在 960x640、
1200x800、1600x1000 下验证工具栏完整落在页面内、地址栏最小宽度不低于 180 像素、
七个关键控件可见、非空且映射后完全落在工具栏内；地址栏实际宽度不低于最小宽度，
其余按钮实际宽度不低于各自 `sizeHint()`。实际字体像素值同时证明主题选择器随档位生效。
MainWindow 保留原有宽高
联合门槛，避免低高度窗口放大播放器控制行；网页页按自身宽度独立响应，两者不冲突。

网页模式新增 `Alt+左键`/`Alt+右键` 后退前进，地址输入框持有焦点时同样路由；既有
`Ctrl+L` 聚焦并全选地址、`Ctrl+R`/`F5` 刷新和 `Esc` 优先退出网页全屏均保持实际行为。
主窗口切入网页模式会禁用“打开网络地址”的 Ctrl+L 菜单动作，避免与网页地址栏快捷键
产生二义性；退出网页模式后由现有视图状态继续控制该动作。
帮助表按模式准确区分网页 `Ctrl+L` 与直播打开网络地址、网页刷新与直播 F5，并明确
Esc 优先级。有效 RED 为浏览器程序 `23/25`：页面 `responsiveSize` 为空、Alt 后退命令
计数为 0；追加 Ctrl+L 实际路由测试又准确失败于快捷键二义性。帮助内容精确断言是在
实现后补强，不记作独立 RED。最终浏览器 GUI `26/26`、主 GUI `95/95`；offscreen 三档
布局与环境 scale 回归只能证明 Qt 布局和样式门禁，不能替代真实 Windows 125%/150% DPI
人工观感。

Task 10 提交后的规格补强把响应式测试从工具栏和地址栏扩展到后退、前进、刷新、主页、
地址栏、访问与清除数据全部七个控件，并逐档核对页面实际尺寸、可见性、非空几何、工具栏
包含关系及有效宽度。该强化测试在已有实现上首次运行即 `3/3` 通过（107 毫秒），因此准确
记录为实现后规格补强，不虚构独立 RED；只读核对 Task 10 前提交确认旧实现仍会先失败于
已有的空 `responsiveSize` 断言。

Task 10 质量修复区分三层证据：Qt 控件焦点测试只验证 `QShortcut` 路由；纯行为测试以 fake
Accelerator args 验证首次 KEY_DOWN/SYSTEM_KEY_DOWN 的 Ctrl+L、Alt+左/右、Ctrl+R、F5
和网页全屏 Esc 映射、Handled、repeat/KeyUp/未知键/附加修饰不吞，以及 getter/setter
失败语义；生产接线门禁则验证 Controller 的 `add_AcceleratorKeyPressed` 在 Ready 前注册、
稳定 listener 投递和 `remove_AcceleratorKeyPressed` 在 Close 前完成。BrowserPage fake
listener 还验证旧 generation 丢弃、当前代次聚焦全选和导航命令，以及非全屏 Esc 不产生
退出请求。真实 WebView2 child HWND 获得焦点后的物理按键不由 Qt offscreen 假冒，仍需
普通 Windows Runtime 自动化或人工验收。

本轮首个有效 RED 为 VS x64 构建准确失败于缺少 `webview2_accelerator.h`。GREEN 后三项
accelerator 策略与接线加 init/cleanup 为 `5/5`（2 毫秒），稳定 GUI 事件路由 `3/3`
（18 毫秒）。800x600 compact 也加入与原三档相同的七控件边界检查；全局
`QToolButton[responsiveSize]` 选择器已限定为浏览器工具栏后代，不影响其他工具按钮。

Fresh 四目标构建成功；浏览器 GUI `27/27`（0.703 秒）、主 GUI `95/95`（56.095 秒），
WebView2 非 Runtime 纯行为与接线 `36/36`（63 毫秒）。四项 GUI CTest `4/4` 通过，总计
195.26 秒。沙箱完整 WebView2 运行在 120 秒外层上限前从持久 Profile 场景开始至少六项
Runtime 行为失败且没有 Totals，继续归类为已知 Renderer 子进程环境限制，不算通过、也不
覆盖新增必需 Accelerator 注册。完整 42 个 test slots 加 init/cleanup 应为 `44/44`；必须
取得普通 PowerShell 结果后才能提交本修复。用户随后在普通 PowerShell 对同一 fresh 构建
产物运行完整 WebView2 程序，最终 `44/44` 通过（12.021 秒，0 failed/skipped）。新增
`mapsAndHandlesControllerAccelerators`、`acceleratorFailuresDoNotConsumeWebInput` 和
`registersAcceleratorBeforeReadyAndRevokesBeforeClose` 均通过，Task 9 全部 Runtime 回归
也保持通过。该证据证明必需事件注册没有阻止 Ready；它不把沙箱 Renderer 限制改写成
通过，也不等同于把真实物理键发送给 child HWND 的端到端输入验收。

Task 11 先扩充发布校验的法律材料门禁。在被 Git 忽略且原先不存在的
`cmake-build-debug/task11-validator-red` 中构造满足旧规则的受控假包后，运行
`cmake -DMEDIAHUB_PACKAGE_DIR=<受控目录> -P tools/validate_release_package.cmake`，
准确得到 `Release package is missing licenses/WebView2-LICENSE.txt.` 的 RED。GREEN 后
Release 配置会先检查固定 SDK 的 `LICENSE.txt` 和 `NOTICE.txt`，再分别安装为
`WebView2-LICENSE.txt` 与 `WebView2-NOTICE.txt`；第三方声明明确 Loader 为静态链接，
不发布 `WebView2Loader.dll`，Evergreen Runtime 仍是外部前置条件。

校验器兼容 `MEDIAHUB_PACKAGE_DIR` 和计划旧示例 `PACKAGE_ROOT`。受控夹具逐项证明它会
拒绝 Profile、Cache、Code Cache、Cookie、x86/ARM64 Loader、动态 Loader、测试程序、
probe、CMake 缓存和编译产物。首次真实包校验还准确暴露规则假阳性：VLC 合法的
`libcache_block_plugin.dll` 与 `libcache_read_plugin.dll` 被过宽的 `cache` 关键词拒绝。
规则按根因收窄为浏览器资料路径段后，全部禁入夹具仍失败，两个 VLC 插件保留并通过。

依赖扫描确认 `include/mediahub/core/` 与 `src/core/` 对 Qt、libVLC、WebView2、COM 和
Windows 类型均零命中；`mediahub_gui_support` 只链接 core、logging、Qt 和必要 Win32
系统库，不链接 libVLC 或 WebView2。生产 WebView2 日志只记录固定事件名和数值 HRESULT，
不记录完整 URL、Profile 路径、Cookie、Authorization、password、token 或下载目标。
测试源码的命中均为固定 `example.test`、IPv4 loopback/Cookie 持久性夹具，或验证敏感值
不进入日志的反向断言；`git diff --check` 通过。

构建矩阵使用 VS 2022 x64 环境和 Ninja。Debug 与启用发布安装规则的 Release 完整构建
均成功；两次沙箱全量 CTest 都是 `80/81`，分别耗时 233.30 秒和 157.17 秒，唯一失败均为
`integration.webview2.initialization` 在 90.04/90.03 秒超时，其余 80 项全部通过。该结果
与已记录的 Codex Renderer 子进程限制一致，未重复运行、未记作全绿。Debug 同一产品代码
已有用户普通环境 WebView2 `44/44`、12.021 秒、零跳过的证据；本轮 Release 同一测试程序
在普通 PowerShell 也为 `44/44`（11.276 秒，零失败、零跳过），Task 9/Task 10 的全部
Runtime 场景均通过。两次普通环境结果补齐沙箱无法提供的真实 Runtime 门禁。

原 `cmake-build-core-only` 只有 Visual Studio 17 生成器残留缓存，与本轮 Ninja 配置冲突；
该用户构建目录未被删除、移动或改写。本轮改用被忽略的
`cmake-build-core-only-v06`，完整构建 22 步后 CTest `52/52` 通过（7.43 秒），配置过程不
查找 Qt、libVLC 或 WebView2。新建 `cmake-build-release-no-tests` 并显式关闭
`BUILD_TESTING` 与 `MEDIAHUB_BUILD_TESTS`，完整构建 46 步成功，CTest 报告 0 项测试。

安装前两次只读确认 `dist/MediaHub-0.6.0-win64` 不存在，随后从 Release 安装到该空目录。
正式校验通过：共 400 个文件、365 个 VLC 插件 DLL，包含应用、Qt/libVLC 运行库、图标、
Qt/VLC/WebView2 六项法律材料；独立扫描禁入项为 0，不包含 Profile、Cache、Cookie、
x86/ARM64 Loader、任何 `WebView2Loader.dll`、测试/probe、库文件或构建缓存。Task 11 不做
窗口启动和人工网页验收，这些边界继续留给 Task 12。

Task 11 提交后的规格审查修复两个发布门禁。单配置 RED 使用全新、被忽略的
`cmake-build-task11-debug-package-red`，以 Ninja、Debug 和
`MEDIAHUB_PACKAGE_RELEASE=ON` 配置时错误成功；GREEN 后配置准确失败于
`MEDIAHUB_PACKAGE_RELEASE requires CMAKE_BUILD_TYPE=Release.`。多配置不读取通常为空的
`CMAKE_BUILD_TYPE`，改由安装脚本检查 `CMAKE_INSTALL_CONFIG_NAME`；Ninja Multi-Config
配置可成功，但 `cmake --install --config Debug` 会在复制文件前准确失败，Release 安装不受
影响。现有 Release package-ON 刷新和增量构建成功，无测试 package-OFF 配置不受该分支影响。

缓存目录 RED 在受控假包分别加入 `DawnWebGPUCache`、`DawnGraphiteCache` 和
`GrShaderCache`，三次校验都错误通过。GREEN 初次复验又发现原正则由 `set()` 的两个参数
组成 CMake list，分号令第二段 `ShaderCache`/Cookie 规则不可达；改为 `string(CONCAT)` 后，
上述三类以及 Profile-v1、Cache、Code Cache、GPUCache、ShaderCache、Cookie、Cookies
共十类逐项失败。规则只匹配明确浏览器资料路径段，VLC 合法
`plugins/stream_filter/libcache_block_plugin.dll` 和 `libcache_read_plugin.dll` 继续通过。
Release 重新安装并 fresh 校验仍为 400 个文件、365 个 VLC 插件和六项法律材料。

## 8. 完成门槛

以下条件全部满足后，v0.6 测试状态才能改为完成：

1. 规划 W-01 至 W-18 和本文 W-M01 至 W-M18 均有可核对证据。
2. 所有新增自动化与既有 Debug/Release 全量 CTest 通过，没有静默跳过。
3. Core-only 不接触 WebView2，GUI 测试不依赖真实 Runtime 或用户 Profile。
4. 至少一个真实登录网站、一个普通网页视频和一个网页直播完成人工音画验收。
5. 清除网页数据、资料隔离、三种权限决定、下载/上传、证书确认和敏感日志扫描通过。
6. 用户确认真实使用没有阻塞问题，测试记录和开发交接同步后才允许提交完成状态。
