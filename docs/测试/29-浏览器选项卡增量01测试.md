# 内置浏览器选项卡增量 01 测试

> 范围：阶段一“当前问题修复”。网页模块与本地媒体、直播播放链路隔离。

## 自动化验证

2026-08-12 使用 VS x64 开发环境构建：

```text
cmake --build cmake-build-debug --target mediahub_browser_gui_tests mediahub_gui_tests mediahub_gui -j 4
```

构建成功。定向 CTest：

```text
ctest --test-dir cmake-build-debug -R "gui.browser_page|gui.stage10_coverage" --output-on-failure
```

结果 `2/2` 通过。新增覆盖：

- 导航完成后地址栏保持可编辑；点击地址栏全选旧地址，输入新地址复用当前页面。
- `Ctrl+L` 和 WebView2 原生焦点快捷键继续全选当前地址。
- 连续提交两个 URL 只记录两个当前页面导航请求。
- 成功导航写入历史，重复 URL 去重并更新标题/访问时间。
- QSettings 历史跨实例恢复，非法协议过滤，访问时间下限归零。

## 待人工边界

真实 WebView2 Runtime 下的地址栏鼠标体验、真实站点导航历史和重启后的用户配置仍在
阶段二/三完成后统一验收；本阶段不宣称多标签、密码、Cookie 或清除数据覆盖。
