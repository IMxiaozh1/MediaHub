# 内置浏览器 Edge 风格 UI 重构测试

> 范围：浏览器顶部结构、图标工具栏、统一侧边面板、响应式布局和既有行为回归。
>
> 状态：实现、自动化、构建矩阵与最终人工验收全部通过。

## 1. 自动化矩阵

| 编号 | 场景 | 通过条件 | 状态 |
| --- | --- | --- | --- |
| BEU-A01 | 浏览器顶部结构 | 只有标签栏和导航栏常驻，信息栏已移除 | 通过 |
| BEU-A02 | 图标按钮 | 固定尺寸、非空图标、tooltip、accessible name 和完整启用状态 | 通过 |
| BEU-A03 | 标签状态 | 图标、关闭、固定、分组、出声和静音局部更新 | 通过 |
| BEU-A04 | 更多菜单 | 历史、收藏、缩放、设置、权限和清除命令可达 | 通过 |
| BEU-A05 | 统一侧边面板 | 六类页面互斥显示，关闭后 WebView2 恢复完整宽度 | 通过 |
| BEU-A06 | 历史/收藏列表 | 搜索、打开、编辑、删除、排序和导入导出不回归 | 通过 |
| BEU-A07 | 下载/声音列表 | 进度、取消、重试、切换和静音不回归 | 通过 |
| BEU-A08 | 页内查找 | 打开、查找、前后切换、Esc 关闭和焦点恢复正确 | 通过 |
| BEU-A09 | 800×600 布局 | 低频命令进入菜单，无文字裁切和控件重叠 | 通过 |
| BEU-A10 | 多尺寸和三档 DPI | 字体不随宽度变化，无越界，WebView bounds 与布局一致 | 通过 |
| BEU-A11 | 键盘操作 | 既有快捷键及 Ctrl+H/Ctrl+Shift+O/Ctrl+J/F6 正确 | 通过 |
| BEU-A12 | 后台播放与下载 | UI 开关不产生 Suspend、Mute、Navigate 或取消下载 | 通过 |
| BEU-A13 | 浏览器独立主题 | 切换本地/直播不改变网页 Chrome 配色与控件尺寸 | 通过 |
| BEU-A14 | 浅色层级与模式栏 | Chrome、侧栏和标题高对比，三段模式控件尺寸稳定 | 通过 |

结构测试应通过 object name、可见性、geometry、信号和后端命令断言完成，不使用像素颜色
代替功能证据。可见界面改动必须另留普通 Windows 截图和人工检查记录。

## 2. 真实 Runtime 与人工验收

| 编号 | 场景 | 通过条件 | 状态 |
| --- | --- | --- | --- |
| BEU-M01 | 多标签浏览 | 标签清晰、切换顺畅、关闭按钮不跳动 | 通过 |
| BEU-M02 | 地址与工具栏 | 常用操作一眼可识别，低频命令不拥挤 | 通过 |
| BEU-M03 | 六类侧边面板 | 开关即时、尺寸稳定、网页无闪烁或遮挡 | 通过 |
| BEU-M04 | 800×600/1280×720/1920×1080 | 内容、工具栏和面板无裁切重叠 | 通过 |
| BEU-M05 | 100%/125%/150% DPI | 图标清晰，文字和点击区域不漂移 | 通过 |
| BEU-M06 | 视频/直播与下载 | 切标签、开面板后连续播放和下载 | 通过 |
| BEU-M07 | 明暗主题与焦点 | 对比度、hover、pressed、focus、disabled 均明确 | 通过 |
| BEU-M08 | 关闭应用 | 无 MediaHub/WebView2 残留进程 | 通过 |

## 3. 回归命令

- `gui.browser_page`、`gui.ui_theme`、下载中心和浏览器资料定向测试。
- Debug/Release 排除 Runtime 全量 CTest。
- 普通 Windows 完整 `mediahub_webview2_tests.exe`。
- core-only 和关闭测试的 Release 构建。
- `git diff --check`、架构依赖和敏感日志扫描。

本轮实际结果：Debug/Release 全量 CTest `91/91`，core-only `52/52`，Release 无测试构建
成功且 CTest 为 `0` 项；完整 Runtime 测试 `57/57`。`gui.browser_page`、`gui.ui_theme`、
下载中心、浏览器资料和权限相关测试均通过，`git diff --check` 通过。Release 人工验证使用
普通窗口和最大化窗口；窗口关闭后未发现 MediaHub 或由其启动的 WebView2 进程残留。视频/下载
人工场景使用本机临时 HTTP 服务和已有测试媒体，下载任务在未选择保存位置前取消，未写入用户
媒体目录。2026-08-15 最终候选补充验证 BrowserPage `84/84`、完整 WebView2 Runtime
`57/57`，真实 Bing 的统一历史侧栏与关闭后全宽恢复正常。

最终浅色候选补充结果：网页标签栏、导航栏、地址栏、历史/收藏侧栏均使用浅色中性表面，
侧栏标题有独立底色、边框和蓝色识别线；“本地 / 直播 / 网页”使用固定 `128×36` 的浅色
分段控件。Debug 普通窗口、网页页、历史和收藏侧栏均完成工具实机截图检查，无文字裁切或
重叠。最终候选 Debug/Release 完整 CTest 均为 `91/91`，耗时 `228.45s` 与 `92.65s`；
core-only `52/52`，无测试 Release 为 `0` 项，Release BrowserPage 与真实 WebView2 Runtime
定向复跑 `2/2` 通过，`git diff --check` 通过。

## 4. 完成门禁

BEU-A01 至 BEU-A14、BEU-M01 至 BEU-M08 和完整构建矩阵全部通过。用户完成最终浅色配色
和持续拖动联合验收并确认无问题；规划 13 已完成。
