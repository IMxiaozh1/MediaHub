# MediaHub Qt 6.8 迁移测试

> 状态：已完成；自动化、人工验收、正式包切换和正式路径复验全部通过。
>
> 日期：2026-08-24。

## 1. 测试范围

验证仓库源码继续位于原路径，并以 `C:\Qt6.8\6.8.3\msvc2022_64` 作为外部 Qt SDK 完成
Qt6-only 配置、源码编译、测试、部署、正式包隔离运行和必要的 Windows 界面验收。

## 2. 自动化结果

| 场景 | 实际结果 |
|---|---|
| Qt6 Debug 全量构建 | 成功，编译无警告 |
| Qt6 Debug CTest | `93/93`，425.39 秒 |
| Qt6 Release 全量构建 | 成功，编译无警告 |
| Qt6 Release CTest | `93/93`，96.39 秒 |
| Core-only | 构建成功，`52/52`，3.76 秒；未依赖 Qt/libVLC |
| Release 无测试 | 构建成功，CTest 为 `0` 项 |
| 发布包校验 | 365 个 VLC 插件；无 Qt5、Qt6 Debug、测试、构建缓存或链接 |
| Qt6 Debug DLL 反向探针 | `Qt6Cored.dll` 被校验器拒绝 |
| Qt5 残留反向探针 | `Qt5Core.dll` 被校验器拒绝 |
| 正式包隔离本地 MP3 | 100%/150% 各 5 秒播放、包内 Qt6/libVLC 模块、正常关闭通过；正式路径切换后再次复验通过 |
| 正式包隔离本地 MP4 | 3 秒播放、包内 Qt6/libVLC 模块、正常关闭通过 |
| WebView2 Runtime | 真实 Bing 页面初始化通过 |
| Qt5 状态迁移 | 首次完整导入；Qt6 已有备忘优先、按 URL 去重、删除后不重复导入 |

主要命令：

```powershell
cmake --build cmake-build-debug
ctest --test-dir cmake-build-debug --output-on-failure
cmake --build cmake-build-release
ctest --test-dir cmake-build-release --output-on-failure
cmake -S . -B cmake-build-core -G Ninja -DCMAKE_BUILD_TYPE=Debug `
      -DMEDIAHUB_CORE_ONLY=ON -DMEDIAHUB_BUILD_TESTS=ON
cmake --build cmake-build-core
ctest --test-dir cmake-build-core --output-on-failure
cmake --build cmake-build-release-notests
ctest --test-dir cmake-build-release-notests -N
cmake "-DMEDIAHUB_PACKAGE_DIR=dist/MediaHub-0.7.0-win64" `
      -P tools/validate_release_package.cmake
```

## 3. Windows 人工验收

| 场景 | 观察结果 | 结论 |
|---|---|---|
| Qt6 本地 MP4 | 真实画面显示，控制行和进度正常 | 通过 |
| 本地页切到网页页 | WebView2 成功打开 Bing；原生媒体继续运行并自然结束 | 通过 |
| 网页页切回本地页 | 播放状态、结束时间和列表身份保持一致 | 通过 |
| Qt6 直播清单 | HTTPS M3U 解析为 389 项，重复 5 项，跳过 0 项 | 通过 |
| 帮助直播源迁移 | 用户确认 Qt6“帮助 -> 直播源”显示旧版全部 5 条地址和备注 | 通过 |
| 直播抽查源 | 抽查的 B 站/虎牙源返回连接失败提示，源本身不可用 | 未通过/外部条件 |
| 真实直播音画 | 用户以当前可用源确认画面和声音正常 | 通过 |
| 直播跨页后台播放 | 用户确认切到网页页后直播继续播放 | 通过 |

## 4. 已知限制

- Qt6 迁移测试不能证明任意公网直播地址可用；直播服务状态、区域、协议和源合法性由
  用户提供的实际地址决定。
- 旧 Qt5 发布包已按用户指定顺序在迁移提交双远端推送成功后移入 Windows 回收站；当前
  `dist` 只保留 `dist/MediaHub-0.7.0-win64/` 这一份 Qt6 正式目录。
- 历史 Qt5 测试文档保留其当时的环境和结果，不与本次 Qt6 结果混写。

## 5. 结论

Qt6 代码、构建、自动化测试、部署校验、帮助直播源迁移及真实直播音画/跨页人工回归均已
完成。迁移提交 `192fa98` 已普通推送到 Gitee 和 GitHub；旧 Qt5 包随后移入回收站，唯一
Qt6 正式路径的结构校验与隔离 MP3 播放复验通过。
