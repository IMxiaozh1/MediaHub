# 内置浏览器多标签性能优化测试

> 范围：多标签静音、辅助窗口、历史/收藏持久化和会话检查点的宿主性能。
>
> 状态：自动化、构建矩阵与最终人工验收全部通过。

## 1. 自动化矩阵

| 编号 | 场景 | 通过条件 | 状态 |
| --- | --- | --- | --- |
| BPF-A01 | 100 标签下单标签静音 | 后端命令 1 次；隐藏声音列表不重建 | 通过 |
| BPF-A02 | 可见声音窗口单项静音 | 仅目标行文本和状态变化 | 通过 |
| BPF-A03 | 500 条历史首次与再次显示 | 第二次无存储读取、无列表重建 | 通过 |
| BPF-A04 | 历史搜索快速输入 | 只执行最终查询的全量筛选 | 通过 |
| BPF-A05 | 隐藏历史/收藏/声音/搜索/分组窗口 | 数据变更只设脏标记，不创建行 | 通过 |
| BPF-A06 | 连续成功导航 | QSettings 写入合并，GUI 热路径不 `sync()` | 通过 |
| BPF-A07 | 无变更的会话检查点 | DPAPI 保存调用为 0 | 通过 |
| BPF-A08 | 状态变更后正常退出 | 最终快照保存且可恢复 | 通过 |
| BPF-A09 | 后台网页与下载回归 | 不增加 Suspend、Mute、Navigate 或 Controller 重建 | 通过 |
| BPF-A10 | 下载高频进度 | UI 更新合并限频，完成/失败/取消终态立即呈现 | 通过 |
| BPF-A11 | Composition 候选诊断 | 候选自动化通过但实机无改善，源码已回退 | 未采用 |

调用次数优先作为稳定自动化证据；时间阈值只用于 Release 基准和人工对比，不在共享
CI 中使用过紧的毫秒断言。

### 1.1 首个切片结果（2026-08-14）

- `cachesBrowserDataAndSkipsUnchangedListRebuilds` 使用 500 条历史验证：首次打开只读取
  一次存储，第二次打开存储读取、`rowsInserted`、`rowsRemoved` 和 `modelReset` 均为 0。
- 历史窗口隐藏后记录新导航时，存储缓存继续复用且列表结构信号为 0；再次显示后才用
  缓存刷新，并保持 500 条上限与 URL 敏感参数清理。
- 收藏窗口首次加载后，未变化的再次打开同样不读取存储、不触发列表结构信号。
- Debug `gui.browser_page` 为 `1/1` 通过；Debug 排除
  `integration.webview2.initialization` 的全量 CTest 为 `90/90` 通过；Release
  `gui.browser_page` 为 `1/1` 通过；`git diff --check` 通过。

### 1.2 声音列表切片结果（2026-08-14）

- `updatesOnlyChangedAudioRowAndSkipsHiddenRebuilds` 建立 100 个出声标签，声音窗口隐藏时
  单标签静音只增加一次 `SetTabAudioMuted`，列表模型的 `rowsInserted`、`rowsRemoved`、
  `modelReset` 和 `dataChanged` 均为 0。
- 再次显示声音窗口后，对仍在出声的目标标签切换静音只产生一次 `dataChanged`；列表结构
  信号均为 0，逐行文本比较确认只有目标行发生变化。
- Debug `gui.browser_page` 为 `1/1` 通过；Debug 排除
  `integration.webview2.initialization` 的全量 CTest 为 `90/90` 通过；Release
  `gui.browser_page` 为 `1/1` 通过；`git diff --check` 通过。
- BPF-A01、BPF-A02 已通过，BPF-A05 的声音部分已通过；用户在普通 Windows 真实网页下
  确认单标签静音、全局静音及恢复逻辑正常，BPF-M01、BPF-M02 已通过。BPF-M04 仍待后续
  下载与后台播放综合回归。

### 1.3 标签搜索与分组窗口切片结果（2026-08-14）

- `skipsHiddenTabSearchAndGroupListRebuilds` 验证标签搜索和分组窗口在资料未变化时重复打开
  不产生列表结构信号。
- 两个窗口隐藏后新增并完成第三个标签导航，标签搜索列表结构信号仍为 0；再次显示后才
  刷新为 3 行。
- 两个窗口隐藏时确认清除 Profile，标签搜索和分组列表结构信号均为 0；再次显示后分别
  刷新为 1 个保留标签和 0 个分组。
- Debug `gui.browser_page` 为 `1/1` 通过；Debug 排除
  `integration.webview2.initialization` 的全量 CTest 为 `90/90` 通过；Release
  `gui.browser_page` 为 `1/1` 通过；`git diff --check` 通过。
- BPF-A05 已全部通过；用户已在普通 Windows 下确认标签搜索与分组窗口重复打开、隐藏
  期间修改标签和再次显示均正常且无明显卡顿。

### 1.4 历史与收藏搜索输入合并切片结果（2026-08-14）

- `coalescesHistoryAndFavoriteSearchInput` 对 500 条历史连续设置三个查询，定时器到期前
  `rowsInserted`、`rowsRemoved` 和 `modelReset` 均为 0；到期后列表只保留最终查询命中的
  1 行。
- 收藏搜索使用同一门禁连续设置三个查询，定时器到期前列表结构信号同样为 0，最终仅
  显示“third note”对应条目。
- 两个搜索定时器均为 150 毫秒单次定时器；测试通过 `QTRY_COMPARE` 等待明确结果，并
  断言最终刷新后定时器不再活动，不使用固定时长等待碰运气。
- Debug `gui.browser_page` 为 `1/1` 通过；Debug 排除
  `integration.webview2.initialization` 的全量 CTest 为 `90/90` 通过；Release
  `gui.browser_page` 为 `1/1` 通过；`git diff --check` 通过。
- BPF-A04 已通过；用户已在普通 Windows 下确认快速输入不掉字、列表只显示最终结果且
  主窗口无明显卡顿，BPF-M03 已通过。

### 1.5 窗口拖动性能切片结果（2026-08-14）

- `coalescesParentWindowPositionNotifications` 连续提交 100 次父窗口位置通知，确认 50
  毫秒单次定时器到期前后端调用为 0、到期后调用为 1，并验证 shutdown 取消待处理通知。
- `updatesBoundsOnlyForActiveTab`、`usesRawPixelBoundsForEveryController` 和
  `notifiesWebView2WhenParentWindowMoves` 定向测试均以退出码 0 通过。
- 第一轮普通 Windows 人工复验未通过：短拖相对正常，持续按住标题栏移动仍逐渐落后；
  用户确认本地、直播和网页三个页面都会复现，不能继续把问题限定在 WebView2 路径。
- 第二轮新增 `suspendsClientUpdatesDuringInteractiveWindowMove`，验证 Windows 交互移动开始
  后主窗口关闭客户区更新，结束后恢复，重复开始和结束保持幂等。网页位置通知测试同时
  扩展为拖动期间 100 次通知不调用后端，结束后最终只增加一次。两个定向测试均退出码 0。
- 第二轮实机验收仍失败于网页页：快速持续拖动时窗口明显落后，鼠标停止后继续移动一段
  时间。第三轮加入旧 `WM_MOVING` 坐标折叠和 `SWP_NOREDRAW` 后，GUI 定向测试退出码为
  0、Debug 主程序构建成功，但用户实机确认现象不变；两项实验性消息处理已撤回。
- 同一源码的 Release 对照仍复现，排除 Debug 运行库和编译优化等级是主要原因。曾以
  `keepsNativeBrowserHostFromPromotingLayoutAncestors` 验证阻止 Qt 提升普通布局祖先为原生窗口，
  对应实现和 Release 构建均成功，但用户实机确认没有改善，已撤回该无收益改动。后续不把
  WebView2 嵌入 `QWebEngineView`；应评估迁移到 WebView2 Composition Controller。
- `mediahub_browser_gui_tests.exe` 完整运行退出码为 1；单独复跑
  `searchesEditsDeletesAndReordersFavorites` 可稳定复现既有失败：实际第二项为 `Three`，
  期望 `Two edited`，失败位置为 `tests/gui/browser_page_test.cpp:888`。该用例不经过窗口
  移动通知路径，但完整回归门禁仍不能写成通过。
- WebView2 三项定向组合继续以退出码 0 通过。原 Debug 输出被 PID 31236 的旧
  `MediaHub.exe` 锁定，强制关闭请求因自动审批服务故障未执行；独立目录
  `cmake-build-debug-move-fix/` 已成功配置并单线程构建 `mediahub_gui`。完整并行构建曾因
  多个测试目标同时运行 `windeployqt`、竞争复制同一测试 DLL 失败，不是源码编译错误。
- 2026-08-14 本次会话补齐源码后，`coalescesParentWindowPositionNotifications`、
  `updatesBoundsOnlyForActiveTab`、`usesRawPixelBoundsForEveryController` 和
  `notifiesWebView2WhenParentWindowMoves` 定向组合均通过。Debug `mediahub_gui`、
  `mediahub_browser_gui_tests`、`mediahub_webview2_tests` 与 `mediahub_gui_tests` 均成功构建；
  `integration.webview2.initialization` 通过，新版 `MediaHub.exe` 启动后保持响应并正常关闭，
  退出码为 0。完整 BrowserPage 回归仍失败于既有收藏排序用例和恢复冷却用例，不能写成
  全量通过；BPF-M06 仍待用户在新版程序上人工复验。
- 用户复验上述 50 毫秒通知合并候选后仍确认网页页快速拖动明显落后，因此该通知链已被
  Composition Controller 迁移取代，不再作为当前通过证据。
- `recognizesCompositionMouseMessages` 验证移动、左右中键、扩展键、双击、滚轮和离开消息
  进入 Composition 输入通道；`usesCompositionControllersAndDetachesVisuals` 验证主页面和
  标签页均使用 Composition 创建接口、DirectComposition 根视觉目标存在，并在
  `controller_->Close()` 前解除视觉目标。
- `usesRawPixelBoundsForEveryController`、`updatesBoundsOnlyForActiveTab`、Controller 接管
  生命周期与新增两项 Composition 测试组合为 `7/7` 通过。
- 完整 `mediahub_webview2_tests.exe` 为 `61 passed, 0 failed`，耗时约 15.35 秒；其中真实
  Runtime 初始化、共享 Profile 标签创建关闭、弹窗、权限、下载、上传与全屏均通过。
- BrowserPage 显示、标签切换和关闭定向组合为 `6 passed, 0 failed`；Debug
  `mediahub_webview2_tests`、`mediahub_browser_gui_tests`、`mediahub_gui_tests` 和
  `mediahub_gui` 均单线程构建成功。
- 新候选 `cmake-build-debug-move-fix/MediaHub.exe` 启动为 PID `11892` 后保持响应，但用户
  普通 Windows 人工拖动仍确认窗口明显落后于鼠标。Composition 迁移没有改善 BPF-M06，
  本轮人工验收失败；候选源码随后回退且未提交，不构成当前实现的通过证据。

### 1.6 历史与会话持久化切片结果（2026-08-14）

- `reusesCurrentPageAndRecordsSuccessfulNavigation` 验证连续成功导航与标题更新在定时器
  到期前保存次数为 0，到期后只写入一次；下一次导航重新开启独立合并窗口。
- `flushesPendingHistoryWhenShuttingDown` 验证定时器尚未到期时正常关闭仍写入最终历史。
- `periodicallyCheckpointsBrowserSession` 验证第一次检查点保存后，无变化的第二次检查点
  不再调用存储；标题变化后关闭会保存最终快照。
- 历史与会话相关定向组合为 `11 passed, 0 failed`，覆盖历史规范化、上限、删除清空、
  启动恢复和固定标签顺序。BPF-A06、BPF-A07、BPF-A08 通过。

### 1.7 下载进度与后台语义切片结果（2026-08-14）

- `coalescesFrequentProgressUpdates` 连续发送 100 条进度事件，定时器到期前进度条更新
  为 0，到期后只应用最后一条；刷新窗口固定为 150 毫秒。
- `defersHiddenProgressUntilShown` 验证隐藏面板不启动定时器、不刷新控件，显示后只应用
  最新进度；`appliesTerminalStateWithoutWaitingForProgressTimer` 验证终态绕过定时器立即
  更新并停止待刷新任务。
- 下载中心全量自动化 `13 passed, 0 failed`，浏览器下载页面相关组合 `12 passed, 0 failed`。
  BPF-A10 通过。
- `keepsBackgroundWebAndDownloadSemantics` 与已有可见性切换回归组合为 `4 passed, 0 failed`；
  页面隐藏期间不产生 Navigate、CreateTab、CloseTab、Mute、Suspend 或 CancelDownload，
  后台下载进度在恢复显示后可见。BPF-A09 通过。
- 收藏查询从一个单行结果切换到另一个单行结果时，旧列表在定时器到期前保持旧内容，
  原测试只等待 `count() == 1` 会提前通过并删除旧索引。实现现会在等待期间禁用旧列表，
  测试同时等待最终条目文本；收藏编辑、删除和重排全流程通过。

### 1.8 最终自动化结果（2026-08-14）

- `gui.browser_page` 全量通过；收藏筛选与搜索合并定向组合为 `2/2` 通过。
- Debug 全量 CTest `91/91` 通过，耗时约 196.47 秒。
- Release 全量 CTest `91/91` 通过，耗时约 81.61 秒。
- core-only CTest `52/52` 通过；Release 无测试构建成功，CTest 为 `0` 项。
- 真实 WebView2 Runtime 初始化在 Debug/Release 全量中通过；先前一次失败来自未重建的旧
  测试二进制，重建 `mediahub_webview2_tests` 后定向与全量均通过。

### 1.9 原生宿主层级回归（2026-08-15）

- `hostsWebViewInExistingNativeAncestor` 验证网页占位控件没有 `WA_NativeWindow`，WebView2
  初始化句柄来自最近的既有原生祖先，Bounds 左上角按相对祖先坐标和 DPI 转为物理像素。
- BrowserPage 全量为 `84 passed, 0 failed, 0 skipped, 0 blacklisted`；恢复冷却回归使用
  100 毫秒测试窗口，既覆盖第三次失败后的临时锁定，也在 1000 毫秒内验证自动解除。
- Debug/Release 全量 CTest 分别为 `91/91`，耗时 `234.16s` 与 `100.33s`；core-only 为
  `52/52`，无测试 Release 构建成功且 CTest 为 `0` 项；完整 Release WebView2 Runtime 为
  `57 passed, 0 failed, 0 skipped, 0 blacklisted`。
- 真实 Bing 下历史侧栏打开、关闭和 WebView2 全宽恢复正常。Computer Use 驱动真实 Windows
  窗口拖动，本地、直播、网页三页分别约 `99ms`、`104ms`、`113ms`；网页拖动后保持画面，
  未再出现黑屏。该工具证据不替代用户对持续拖动跟手感的最终判断。

### 1.10 最终联合候选回归（2026-08-15）

- 浅色网页 Chrome、侧栏标题和模式切换栏改动后，Debug/Release 全目标构建成功。
- Debug/Release 完整 CTest 均为 `91/91`，耗时分别为 `228.45s` 和 `92.65s`；
  core-only 为 `52/52`，无测试 Release 构建成功且 CTest 为 `0` 项。
- Release `gui.browser_page` 与真实 `integration.webview2.initialization` 定向复跑
  `2/2` 通过，`git diff --check` 通过。
- 用户使用最终 Release 候选完成持续拖动、网页黑屏和浅色界面联合检查，确认无问题；
  BPF-M06 通过。

## 2. 构建与回归

- `gui.browser_page`、数据存储和主题定向测试。
- Debug/Release 排除 Runtime 全量 CTest。
- 普通 Windows 完整 `mediahub_webview2_tests.exe`。
- core-only `52/52` 和无测试 Release `0` 项。
- `git diff --check`、架构边界和敏感日志扫描。

## 3. 人工验收

| 编号 | 场景 | 通过条件 | 状态 |
| --- | --- | --- | --- |
| BPF-M01 | 多个真实网页后连续静音/恢复 | 按钮即时反馈，播放不停顿 | 通过 |
| BPF-M02 | 重复打开历史、收藏和声音窗口 | 窗口即时出现，滚动和选择流畅 | 通过 |
| BPF-M03 | 历史/收藏快速搜索 | 输入不掉字、不卡住主窗口 | 通过 |
| BPF-M04 | 后台视频/直播与下载 | 优化前后连续播放和下载语义不变 | 通过 |
| BPF-M05 | 关闭与重启 | 无残留进程，最后标签与状态恢复正确 | 通过 |
| BPF-M06 | 本地、直播和网页页持续拖动标题栏 | 三页均紧跟鼠标；松手后立即重绘且网页弹出层坐标正常 | 通过 |

## 4. 完成门禁

BPF-A01 至 BPF-A10、构建矩阵和 BPF-M01 至 BPF-M06 全部通过。BPF-A11 是已回退候选的
诊断记录，不是当前实现门禁。规划 12 已完成，代码提交为 `eb4743b`。
