# 内置浏览器多标签性能优化测试

> 范围：多标签静音、辅助窗口、历史/收藏持久化和会话检查点的宿主性能。
>
> 状态：基线待建立，实现与验收未完成。

## 1. 自动化矩阵

| 编号 | 场景 | 通过条件 | 状态 |
| --- | --- | --- | --- |
| BPF-A01 | 100 标签下单标签静音 | 后端命令 1 次；隐藏声音列表不重建 | 待实现 |
| BPF-A02 | 可见声音窗口单项静音 | 仅目标行文本和状态变化 | 待实现 |
| BPF-A03 | 500 条历史首次与再次显示 | 第二次无存储读取、无列表重建 | 待实现 |
| BPF-A04 | 历史搜索快速输入 | 只执行最终查询的全量筛选 | 待实现 |
| BPF-A05 | 隐藏历史/收藏/搜索/分组窗口 | 数据变更只设脏标记，不创建行 | 待实现 |
| BPF-A06 | 连续成功导航 | QSettings 写入合并，GUI 热路径不 `sync()` | 待实现 |
| BPF-A07 | 无变更的会话检查点 | DPAPI 保存调用为 0 | 待实现 |
| BPF-A08 | 状态变更后正常退出 | 最终快照保存且可恢复 | 待实现 |
| BPF-A09 | 后台网页与下载回归 | 不增加 Suspend、Mute、Navigate 或 Controller 重建 | 待验证 |
| BPF-A10 | 下载高频进度 | UI 更新合并限频，完成/失败/取消终态立即呈现 | 待实现 |

调用次数优先作为稳定自动化证据；时间阈值只用于 Release 基准和人工对比，不在共享
CI 中使用过紧的毫秒断言。

## 2. 构建与回归

- `gui.browser_page`、数据存储和主题定向测试。
- Debug/Release 排除 Runtime 全量 CTest。
- 普通 Windows 完整 `mediahub_webview2_tests.exe`。
- core-only `52/52` 和无测试 Release `0` 项。
- `git diff --check`、架构边界和敏感日志扫描。

## 3. 人工验收

| 编号 | 场景 | 通过条件 | 状态 |
| --- | --- | --- | --- |
| BPF-M01 | 多个真实网页后连续静音/恢复 | 按钮即时反馈，播放不停顿 | 待验收 |
| BPF-M02 | 重复打开历史、收藏和声音窗口 | 窗口即时出现，滚动和选择流畅 | 待验收 |
| BPF-M03 | 历史/收藏快速搜索 | 输入不掉字、不卡住主窗口 | 待验收 |
| BPF-M04 | 后台视频/直播与下载 | 优化前后连续播放和下载语义不变 | 待验收 |
| BPF-M05 | 关闭与重启 | 无残留进程，最后标签与状态恢复正确 | 待验收 |

## 4. 完成门禁

只有 BPF-A01 至 BPF-A10、构建矩阵和 BPF-M01 至 BPF-M05 通过，才能把本增量标记为完成并
创建性能优化功能提交。
