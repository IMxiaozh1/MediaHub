# MediaHub v0.2 直播第四增量测试

> 状态：单 URL 完整自动化与实机回归全部通过（2026-08-05）。
>
> 范围：正常直播、慢连接、直播与本地媒体往返、控制与全屏、安全关闭、构建矩阵、
> 版本元数据和交付边界。

## 1. 自动化与构建结果

在 VS 2022 x64 开发者环境中，项目版本更新为 `0.2.0` 后重新配置并执行：

| 配置 | 构建结果 | CTest 结果 | 用时 |
|---|---|---:|---:|
| Debug 全量 | 通过 | `49/49` | 18.90 秒 |
| Release 全量 | 通过 | `49/49` | 14.93 秒 |
| Core-only Debug | 通过 | `33/33` | 0.70 秒 |
| `MEDIAHUB_BUILD_TESTS=OFF` | 通过 | 0 个测试 | - |

Debug 全量中的 GUI 套件 `1/1` 通过，用时约 8.34 秒。既有测试继续覆盖 URL 校验、
隐私化显示、缓冲、超时、主动取消、迟到事件隔离、有限重连、活动停滞识别、最近
地址、本地播放、歌词、播放列表、进度、音量、全屏和关闭边界。

`git diff --check`、100 字符行和核心/GUI/libVLC 依赖边界在提交前检查通过。第四
增量没有修改 C++ 源文件，不需要对源码执行新的格式化写入。

## 2. 正常直播实机回归

本机 VLC 3.0.21 将 `test.mp4` 循环封装为 HTTP MPEG-TS，通过
`http://127.0.0.1:18766/live.ts` 提供测试流。测试媒体位于用户目录，不进入仓库。

- [x] 从“文件 -> 打开网络地址”打开测试流，真实画面和声音持续正常。
- [x] 直播没有固定总时长，主进度条不可拖动。
- [x] 播放期间移动和缩放窗口，画面与界面持续响应。
- [x] 进入和退出全屏后，画面、声音和控制行正常。
- [x] 调整音量、静音和恢复后，声音及百分比显示正确。
- [x] 暂停、继续和停止立即响应；停止后等待超过 8 秒没有自动重连。
- [x] 最近地址下拉框可直接选择刚使用的直播地址并重新播放。

## 3. 本地媒体往返与关闭

- [x] 从直播切换到 `test.mp3` 后，声音、总时长、进度定位、波形和歌词正常。
- [x] MP3 进入和退出全屏后，声音、波形和控制行正常。
- [x] 切换到 `test.mp4` 后，画面、声音、总时长、进度定位和暂停继续正常。
- [x] MP4 进入和退出全屏后，画面、声音和控制行正常。
- [x] 从本地媒体切回最近直播地址后，画面、声音、不可定位进度和全屏正常。
- [x] 直播播放中直接关闭窗口，`MediaHub.exe` 完全退出且没有残留进程。

## 4. 慢连接与超时实机回归

本机 `127.0.0.1:18767` 使用只接受 TCP、保存连接但不返回任何媒体数据的临时服务。
服务只用于本次回归，测试结束后已停止，不进入仓库。

- [x] 连接期间移动、缩放、进入和退出全屏，窗口始终响应。
- [x] 15 秒后显示“连接超时”，播放按钮重新可用。
- [x] 点击播放能够重新进入连接状态，随后点击停止立即显示“已取消连接”。
- [x] 关闭窗口后 `MediaHub.exe` 完全退出；正常流和慢连接服务均已停止，无残留进程。

## 5. 结论与边界

v0.2 单 URL 直播第一至第四增量全部满足计划：网络打开、缓冲和超时可见，连接可取消，
真实断流能够有限重连，会话地址不持久化，直播与既有本地媒体能力可安全往返，关闭
不会留下进程。项目版本元数据已更新为 `0.2.0`。

本次只封存单 URL 能力，不生成新的公开发布包，也不开始频道库、M3U、远程订阅、
Logo 或 EPG。后续仍需在启动每个阶段前单独说明目标、文件范围和验收方式。
