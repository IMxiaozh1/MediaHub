# MediaHub v0.5 日常使用增强测试

> 状态：自动化、构建矩阵与 Windows 人工验收全部通过，v0.5 已完成。

## 1. 自动化范围

| 编号 | 场景 | 状态 |
|---|---|---|
| V5-A01 | 直播名称即时搜索、大小写与中文匹配、清空恢复 | 通过 |
| V5-A02 | 过滤后双击与右键命令仍命中原始频道 | 通过 |
| V5-A03 | 清单替换后重新应用搜索，零结果不影响当前播放 | 通过 |
| V5-A04 | 定位当前直播源时清空会隐藏当前项的搜索 | 通过 |
| V5-A05 | 收藏跨状态存储往返、去重和旧配置兼容 | 通过 |
| V5-A06 | 收藏跨 Presenter 重建恢复，不可用标记不恢复 | 通过 |
| V5-A07 | 成功本地播放进入最近记录，失败和网络媒体不进入 | 通过 |
| V5-A08 | 最近播放复用列表项，缺失文件被安全移除 | 通过 |
| V5-A09 | 中段断点只恢复一次，位置事件使用节流保存 | 通过 |
| V5-A10 | 前段、末段、停止和自然结束清除可恢复断点 | 通过 |
| V5-A11 | 启动只恢复展示，不产生内核或网络请求 | 通过 |
| V5-A12 | v0.4 播放、列表、直播、歌词、快捷键与响应式回归 | 通过 |

GUI 测试程序内部场景由 `81/81` 增至 `88/88`。Debug 与 Release 均在 100%、
125% 和 150% 三档模拟缩放下执行完整 GUI 套件，六次运行全部通过。

## 2. 构建矩阵

| 配置 | 命令 | 结果 |
|---|---|---|
| Debug 全量 | `ctest --test-dir cmake-build-debug --output-on-failure` | `74/74`，187.56 秒，通过 |
| Release 全量 | `ctest --test-dir cmake-build-release --output-on-failure` | `74/74`，最终 78.04 秒，通过 |
| Core-only | 核心独立配置与 CTest | `52/52`，1.94 秒，通过 |
| 无测试构建 | `BUILD_TESTING=OFF`、`MEDIAHUB_BUILD_TESTS=OFF` | Release 构建成功，CTest 为 0 |
| 格式与边界 | `git diff --check`、依赖边界和敏感日志扫描 | 通过 |

Debug 三档 GUI CTest 分别用时 45.71、63.44 和 63.31 秒；最终 Release 三档分别用时
14.78、24.11 和 23.90 秒。Core-only 不提供 Qt 或 libVLC 依赖仍可独立构建和测试；
无测试配置完整生成 `MediaHub.exe`，没有注册测试用例。

## 3. 人工验收

| 编号 | 场景 | 状态 |
|---|---|---|
| V5-M01 | 真实长直播清单搜索、清空和定位当前频道 | 通过，用户确认 |
| V5-M02 | 收藏后重启程序并重新载入清单，收藏标识恢复 | 通过，用户确认 |
| V5-M03 | 真实本地长媒体关闭重开后从中段继续 | 通过，用户确认 |
| V5-M04 | 主动停止和自然结束后重新打开不恢复旧断点 | 通过，用户确认 |
| V5-M05 | 100%、125%、150% 下搜索框和最近播放菜单可用 | 通过，用户确认 |

## 4. 结论

v0.5 实现、自动化、三档缩放回归和四类构建配置均已通过。2026-08-10，用户确认
V5-M01 至 V5-M05 全部通过：直播搜索、定位与收藏持久化符合预期，不可用标记重启后
消失，本地最近播放和断点清除正常，三档可见界面没有问题。v0.5 满足交付条件。

## 5. 完成后的界面易用性修正

2026-08-10，根据实际使用反馈补充两项界面调整：主窗口启动尺寸由 `960x720` 改为
`1200x800`；直播搜索框随窗口响应式档位使用 12、13、15、17 像素字号。直播工具区
两个按钮按文案长度调整为 7:5 的宽度比例，保证最小窗口与 Windows 中文字体环境下
仍不裁剪。

Release `mediahub_gui_tests` 构建通过；100%、125%、150% 三档完整 GUI CTest 串行
回归全部通过，总用时 56.57 秒。测试覆盖默认窗口尺寸、搜索框四档字号连续增长、
从 `960x720` 放大后的画布变化，以及三种展示模式下的控件边界。随后执行 Release
全量 CTest，`74/74` 全部通过，总用时 72.30 秒。最新版 Release 主程序构建成功并已
启动。用户确认默认 `1200x800` 窗口大小合适，搜索框字号会随窗口尺寸变化，占位文字、
清空按钮和直播工具按钮均无裁剪；本次 Windows 人工视觉验收通过。人工验收后的最终
Release 全量 CTest `74/74` 通过，总用时 73.76 秒。
