# v0.7 本地与直播界面重构测试

> 范围：原生播放页结构、播放器主题、辅助窗口、自定义背景、音频波形、歌词、直播频道栏、
> 响应式布局和行为回归。
>
> 状态：已完成（本地自用）

## 1. 自动化矩阵

| 编号 | 场景 | 通过条件 | 状态 |
| --- | --- | --- | --- |
| NPU-A01 | 模式导航 | 左对齐紧凑导航尺寸稳定，三模式切换和网页出声计数不回归 | 自动化通过 |
| NPU-A02 | 页面标题 | 只保留功能性中文标题，不显示英文眉题和模式徽章 | 自动化通过 |
| NPU-A03 | 播放舞台 | 视频、音频波形和歌词复用同一稳定媒体区域 | 自动化通过 |
| NPU-A04 | 本地队列 | 添加媒体位于列表标题行；横纵滚动条保持深色；列表模型、选择和右键操作不回归 | 定向通过 |
| NPU-A05 | 直播频道栏 | URL、历史、载入、取消、定位、搜索、标记和收藏可达 | 自动化通过 |
| NPU-A06 | 底部播放坞 | 当前媒体、状态、进度和全部传输控制稳定且启用状态正确 | 自动化通过 |
| NPU-A07 | 音频波形 | 深色单向波峰从底部向上绘制，无上下镜像；真实分析、暂停与切换语义不回归 | 定向通过 |
| NPU-A08 | 歌词页 | 同步、普通、加载、失败和偏移控制在深色舞台中可读 | 自动化通过 |
| NPU-A09 | 列表收起与全屏 | 舞台扩展、控制栏和全屏 Chrome 行为保持正确 | 自动化通过 |
| NPU-A10 | 响应式布局 | 760×640 至 1920×1080 无裁切、重叠；列表相关文字按四档受控放大 | 定向通过 |
| NPU-A11 | 主题边界 | 网页 Chrome 和辅助表面颜色、尺寸及结构保持原状 | 定向通过 |
| NPU-A12 | 无障碍与快捷键 | tooltip、accessible name、焦点和现有快捷键全部保留 | 自动化通过 |
| NPU-A13 | 网页激活边界 | 在其他页面调整窗口后，切回网页时先更新当前 Bounds 再恢复可见性 | 定向通过 |
| NPU-A14 | 帮助子窗口 | 快捷键、直播源备忘和确认框使用当前深浅模式及主题色，原有行为不回归 | 定向通过 |
| NPU-A15 | 个性化主题 | 五种强调色、本机背景、横向滑杆、合帧预览、取消回退和持久化正确 | 定向通过 |

结构测试使用 object name、父子关系、geometry、可见性、动态属性和信号断言，不使用像素
颜色代替功能证据。音画输出、真实 DPI 观感和界面层级必须单独人工验证。

## 2. 人工验收矩阵

| 编号 | 场景 | 通过条件 | 状态 |
| --- | --- | --- | --- |
| NPU-M01 | 本地音频 | 单向波峰、歌词、深色队列滚动条和底部控制层级清晰，无大面积装饰渐变 | 用户确认通过 |
| NPU-M02 | 本地视频 | 画面是视觉中心，播放坞和队列不抢占注意力 | 用户最终确认通过 |
| NPU-M03 | 直播播放 | 频道、连接状态、刷新和当前画面一眼可辨 | 用户最终确认通过 |
| NPU-M04 | 空状态与错误 | 未打开、加载、断开和失败信息简洁且可恢复 | 用户最终确认通过 |
| NPU-M05 | 普通/最大化 | 1200×800 和桌面最大化无裁切、空洞或比例失衡 | 用户确认通过 |
| NPU-M06 | 三档 DPI | 100%、125%、150% 下文字、图标和点击区域清晰稳定 | 自动化与用户确认通过 |
| NPU-M07 | 网页页回归 | 浏览器 Chrome、侧栏和网页内容保持现有视觉；跨页缩放后立即铺满且无黑区 | 用户确认通过 |
| NPU-M08 | 关闭应用 | 无 MediaHub、libVLC 或 WebView2 子进程残留 | 发布包冒烟通过 |
| NPU-M09 | 主题与帮助窗口 | 五种颜色、背景模糊/透明度、快捷键和直播源窗口层级自然，无模板化 AI 感 | 用户确认通过 |

## 3. 回归命令

- `cmake --build cmake-build-debug`
- `ctest --test-dir cmake-build-debug --output-on-failure`
- 定向运行 `gui.stage10_coverage`、`gui.v03_scale_125`、`gui.v03_scale_150`、
  `gui.ui_theme`、`gui.browser_page` 和音频波形相关测试。
- 可见界面候选稳定后执行必要的 Release 构建与全量 CTest。
- `git diff --check`

## 4. 当前自动化证据

- 新增主题测试在旧实现上因不存在 `themeButton` 明确失败；实现后主题入口、五种预设中的
  青绿选择、运行时 PNG 背景、模糊度 42、透明度 68、应用及重建窗口恢复通过。
- 快捷键和直播源窗口定向组合 `2/2` 通过；覆盖隐藏英文眉题、中文功能标题、普通计数文字、
  常规按钮尺寸，以及保存确认框不再使用渐变。
- v0.7 交付后的浅色主题回归确认，两个帮助窗口虽已收到当前主题，但 Qt 顶层对话框客户区仍
  可能沿用旧深色背景。现已显式同步快捷键窗口、直播源备忘及保存、关闭、地址校验确认框的
  语义调色板和窗口背景；`themesHelpDialogsWithCurrentAppearance` 覆盖浅色绿色、深色玫瑰、
  表格与按钮颜色及保存确认框，已通过。
- 主题状态 INI 往返与旧配置默认值组合 `2/2` 通过。
- 最新 `gui.stage10_coverage` 与 `gui.ui_theme` 组合 `2/2` 通过，耗时 `42.32s`。
- 透明度绘制和背景缓存优化后再次运行 `gui.stage10_coverage`，`1/1` 通过，耗时
  `42.42s`。
- RGB、模糊度和背景清晰度使用专用绝对定位滑杆，点击轨道会直接到达点击值。鼠标按住期间
  只更新手柄和数字，不重算主窗口 QSS；松开后才应用一次完整主题。Windows 原生平台验证
  R 从当前值直接点击到 30、连续拖到 200、拖动期间零次整窗预览及松手一次提交，主题完整
  流程和歌曲进度原有回归组合 `6 passed`。
- 125%、150% 缩放与完整 BrowserPage 组合 `3/3` 通过，耗时 `138.39s`；主题入口未挤压
  最小布局，动态主题选择器未改变网页页内部 UI。
- Debug 全量仍沿用本阶段此前 `91/92` 记录；唯一失败为真实 WebView2 访问测试程序
  `127.0.0.1` 页面时超时，不能记为全量通过。
- Debug 150% GUI 套件首次在原固定 120 秒上限处超时；同一程序独立运行最终退出码为
  `0`。三档 GUI 的 CTest 上限调整为 180 秒，Release 125%/150% 分别在 42.69/43.38 秒
  通过。
- 全新 `cmake-build-release-v07/` 使用 MSVC 19.44、Qt 5.14.2、libVLC 3.0.21 配置并
  完成全目标构建；Release 全量 CTest `92/92` 通过，总耗时 148.38 秒，真实 WebView2
  Runtime 用例 15.86 秒通过。
- 帮助窗口主题同步修复后，Debug `gui.stage10_coverage` 为 `1/1` 通过，耗时 102.49 秒；
  `gui.ui_theme` 为 `1/1` 通过，耗时 0.58 秒。此次修复没有重新生成 `dist` 发布包。

## 5. 最终人工与交付证据

- 用户逐项反馈并复验波形、歌词背景、浅色列表与悬浮面板、RGB 滑杆、队列滚动条、主题
  背景和帮助子窗口，最终确认“现在没问题了”，同意更新文档、保存并推送。
- `dist/MediaHub-0.7.0-win64/` 共 400 个文件、198,049,508 字节（188.87 MiB），包含
  365 个 VLC 插件和六项法律材料；校验器确认无链接、浏览器资料、Debug 或测试文件。
- 独立发布目录正常打开主窗口，Qt、VLC 依赖均从包内加载；正常关闭退出码为 `0`，没有
  残留 `MediaHub.exe`。
- `MediaHub.exe` 为 2,769,920 字节，构建时间 `2026-08-17 17:31:54`，SHA-256 为
  `0307F3F49E4AFED2BB8C6FC32A7391E2C385E2FAC04B7F6B167C16BA99023FDE`。
- 旧 `dist/MediaHub-0.6.0-win64/` 和两个 Debug 构建目录已删除，只保留新的 v0.7 正式包
  与 Release 构建目录。

## 6. 人工验收说明

本轮没有创建合并请求，因此未把界面截图写入仓库。用户在持续使用最终 Debug 候选的过程中
逐项提出并复验本地音频、播放列表、歌词、网页跨页缩放、浅色主题、自定义背景、滑杆和帮助
子窗口问题，最终确认无问题。真实音画观感不计入自动化覆盖；Release 发布包只补充了独立
启动、依赖来源、响应性、正常关闭和残留进程检查。
