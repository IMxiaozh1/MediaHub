# MediaHub 开发交接文档

> 最后更新：2026-08-12
>
> 项目路径：`C:\Users\adminstrator\MediaHub`
>
> 当前分支：`main`
>
> 远程仓库：`origin` → `git@gitee.com:imxiaozh1/media-hub.git`

## 1. 使用说明

本文件用于在不同开发窗口之间传递当前状态。开始新会话时先阅读本文件和项目根
目录的 `AGENTS.md`，然后执行：

```powershell
git status --short --branch
```

实际 Git 状态优先于本文档。如果两者不一致，先查明原因并更新本文档，不要直接
开始下一阶段。

每个阶段实现完成、规定测试通过且阶段测试文档回填后，自动创建本地 Git 提交；
测试失败或存在阻塞项时不提交。远程认证可用且没有冲突时自动推送，然后继续下一
阶段；禁止覆盖远端历史。

## 2. 当前状态

**v0.1 至 v0.5 均已按本地自用版本完成；v0.6 自动化交付候选完成，
W-M01 至 W-M18 待用户人工验收。**

已完成：

- 播放内核选型决策：使用 libVLC，并以抽象接口隔离界面、核心逻辑和真实播放能力。
- v0.3 重新用于界面重构，不恢复已取消的播放内核方案，不增加新的媒体处理逻辑。
- v0.1 范围确定：仅本地音视频播放，不含任何网络能力。
- 项目名确定为 `MediaHub`，可执行文件 `MediaHub.exe`。
- 全套规划、设计、测试、学习、交付文档已建立。
- 本机开发环境已实际扫描确认。
- 已初始化本地 Git 仓库，当前 `main` 跟踪 Gitee 的 `origin/main`；SSH 授权已
  生效，阶段 1 至阶段 7 提交已推送。
- 已从 VideoLAN 官方下载 VLC 3.0.21 win64 压缩包并通过 SHA-256 校验。
- libVLC SDK 已解压到 `C:\SDK\vlc-3.0.21`，编译与运行文件齐全。
- 已建立 C++20/CMake 最小探针，Debug 与 Release 均能初始化 libVLC、打印
  `3.0.21 Vetinari` 并正常释放。
- Debug 与 Release CTest 均为 1/1 通过，直接运行后没有残留进程。
- 已固定接入 GoogleTest 1.17.0，上游源码与许可证位于
  `Third_Party/googletest/`。
- 已建立全部基础 CMake 目标、Qt 5.14.2 空窗口、GoogleTest 和 Qt Test 骨架。
- Debug 与 Release 均生成 `MediaHub.exe` 和两个测试程序，最终 CTest 均为
  3/3 通过。
- Debug 与 Release 实际窗口均能取得 `MediaHub` 标题和原生句柄，正常关闭后
  返回 `0`，没有残留进程。
- `MEDIAHUB_BUILD_TESTS=OFF` 构建成功且 CTest 为 0，依赖隔离源码扫描通过。
- 已定义纯 C++ 核心值类型、`PlayerEngine`、`PlayerEventListener` 和来源识别。
- 已实现包含 8 个状态、64 个有序状态对规则的 `PlaybackStateMachine`。
- 已实现测试专用 `FakePlayerEngine`，可完整记录控制请求并精确触发五类事件。
- 已增加 `MEDIAHUB_CORE_ONLY`；不提供 Qt/libVLC 路径时，核心构建和 13 个单元
  测试全部通过。
- 阶段 3 Debug 与 Release 全量 CTest 均为 15/15 通过。
- 已实现 PImpl `VlcPlayerEngine`，公共头文件不暴露 libVLC 或 Windows 类型。
- libVLC 实例、媒体和播放器句柄均由带自定义删除器的 `unique_ptr` 管理，并按
  固定顺序确定性释放。
- 已完成状态、位置、时长、可定位、结束和失败事件映射，并验证事件来自内核线程。
- 已建立运行时静音 WAV 集成测试，中文空格路径、哑输出播放、暂停、定位、停止、
  自然结束和播放中析构均通过。
- 阶段 4 Debug 与 Release 全量 CTest 均为 23/23 通过；无测试构建为 0 个测试。
- 已建立正式主窗口、文件菜单、打开按钮、播放、暂停、停止、状态和错误提示。
- 已建立 `PlayerPresenter`，播放状态机与所有控件启用规则只在这一处维护。
- 已建立 `EngineEventBridge`，五类内核事件均通过 Qt 队列连接投递到 GUI 主线程。
- `main.cpp` 已构造真实 `VlcPlayerEngine`；`mediahub_gui_support` 仍不链接 libVLC。
- 正式 GUI 构建后会部署 libVLC DLL 与完整插件目录。
- Qt Test 内部 11/11 通过，工作线程事件最终在 `QApplication` 主线程处理。
- 阶段 5 Debug 与 Release 全量 CTest 均为 23/23 通过；核心独立为 13/13 通过。
- Release `MediaHub.exe` 窗口响应、标题和句柄正常，接收关闭消息后以 `0` 退出。
- 已建立 `VideoOutputWidget`，显示后发布原生子窗口句柄，公共边界不暴露 `HWND`。
- 已实现音频占位、视频准备与终止画面清理、窗口缩放和纯画面全屏。
- 关闭窗口会在停止前向内核提交空视频句柄，避免内核持有正在销毁的窗口。
- 阶段 6 Qt Test 内部 14/14 通过，Debug/Release CTest 均为 23/23 通过。
- 已修复视频活动状态被 Qt 重绘覆盖的问题，并避免强制 16:9 布局挤压操作控件。
- 已忽略播放开始后的迟到 libVLC 缓冲事件，界面不会错误退回缓冲状态。
- 使用 `C:\Users\adminstrator\test.mp4` 完成实机验证：硬件解码、连续画面、音频、
  暂停、继续、停止、全屏和安全关闭均通过；媒体文件未加入仓库。
- 支持以 `MediaHub.exe <媒体路径>` 直接打开本地媒体，便于重复验证。
- 已加入当前位置、总时长和归一化进度条，未知时长及不可定位媒体会禁用拖动。
- 拖动期间保持用户预览并吸收内核位置更新，松手只提交一次定位请求。
- 已加入 0 至 100 音量滑块和静音往返，音量操作不会改变播放状态。
- 阶段 7 Qt Test 内部 18/18 通过；真实 MP4 的进度、70% 定位、64% 音量、静音
  往返、默认布局和关闭均通过。
- 已实现纯核心播放列表、当前索引和顺序/列表循环/单曲循环三种播放模式。
- 已加入只读播放列表视图、多选打开、拖放添加、双击切换、移除、上一首和下一首。
- 命令行支持一次传入多个本地媒体路径，保持输入顺序并复用 GUI 的列表入口。
- 阶段 8 Debug/Release 全量 CTest 均为 29/29，核心独立为 19/19，无测试构建成功。
- 用户使用两个 `test.mp4` 完成桌面验收，确认列表切换和三种播放模式功能正确。
- 已新增 `mediahub_logging` 终端结构化日志，支持并发写入和异常保护，不创建日志文件。
- 日志只记录媒体显示名或文件名；错误详情中的驱动器路径和绝对路径会被脱敏。
- `.mp4` 明显损坏或文本伪装会在进入 libVLC 前归类为 `UnsupportedFormat`。
- 列表失败项可继续切换其他项，错误提示清除后恢复正常播放。
- 阶段 9 Debug/Release 全量 CTest 均为 33/33，核心独立为 22/22，无测试构建成功。
- 已提取 `tests/support/generated_media.*`，统一运行时生成和清理静音 WAV 与错误媒体。
- 状态机继续穷举 64 个状态对，播放列表和媒体名称推导补齐空、单项和移除边界。
- `PlaylistModel` 数据角色/当前项刷新以及 Presenter 非法/迟到事件均有细粒度测试。
- 六类错误日志值全部覆盖；GUI Qt Test 内部 25/25 通过且不使用整窗口截图。
- 异步测试只使用有界谓词，源码扫描未发现固定 `sleep` 或 `qWait`。
- 阶段 10 Debug/Release 全量 CTest 均为 38/38，核心独立为 27/27，无测试构建成功。
- 阶段 11 使用 `C:\Users\adminstrator\test.mp4` 完成画面、声音、暂停继续、定位、
  音量、静音、快速切换和播放中关闭的真实验收。
- 使用中文空格临时路径的 6 秒 440 Hz PCM/WAV 确认真实声卡能够听到声音。
- 修复带 UTF-8 BOM 的纯文本伪装 MP4 被 libVLC 当作自然结束、顺序推进下一项的
  问题；失败项现在保持选中并显示中文错误。
- Presenter 只接受当前媒体实际开始播放后的结束事件，旧媒体迟到结束事件不会推进
  新媒体列表项。
- 新增三秒退出保护；人工复验失败媒体关闭后 PID 已退出，系统无 `MediaHub.exe`
  残留。
- 修复暂停定位产生的缓冲事件覆盖暂停状态的问题；用户确认暂停时可连续拖动多次。
- 修复 Qt 文件选择器返回正斜杠路径时 Windows libVLC 无法创建媒体的问题；进入内核
  前统一转为原生分隔符，并保留 UTF-8 中文路径。
- 使用 `test.mp3` 确认真实声音、进度和约 4 分 52 秒时长；用户确认经文件选择器导入
  后，MP3、MP4 来回双击均能恢复正常播放。
- 路径修复后的 Release libVLC/GUI 定向 CTest `11/11` 通过，真实 MP3→MP4→MP3
  内核切换诊断也通过。
- 用户人工确认 8 字节损坏 MP4 显示中文失败提示且不跳项；随后双击 `test.mp4`
  能清除错误并恢复画面、声音和进度，M-17、M-19 通过。
- 用户确认 `测试 音频.wav` 的中文和空格显示正确，音频模式、声音和进度正常，
  M-20 通过。
- 用户从资源管理器同时拖入 `test.mp3`、`test.mp4`，列表保持顺序且首个新增项
  自动播放，M-11 通过；M-01 至 M-22 现均有通过证据。
- 用户确认最后一项 6 秒 WAV 自然结束后显示“播放结束”和 `00:06 / 00:06`，
  进度手柄停在最右侧且不会自动重播。
- 新增轨道单击定位；最后一项自然结束后仍可点击或拖动定位，并自动重新播放。
- 针对 libVLC 3 的 Ended 限制及结束后无声问题，播放按钮、空格和进度定位统一重新
  打开当前媒体，再执行 Play→Seek；运行时 WAV 的真实 libVLC 集成测试已确认恢复。
- 新增空格播放/暂停、上下键每次调整音量 5%、左右键快进后退；界面可把左右键
  跳转步长可选择 5、10、15、20 秒，默认 5 秒；倍速和跳转菜单均向上展开，鼠标
  离开按钮与菜单后自动收起。
- 倍速切换已撤销会造成多次音频重同步的分步过渡；现在每次只向 libVLC 下发一次
  最终倍率，并保留同倍率去重、默认高质量变速参数和 1.0× 热滤镜。
- 纯音频改倍率后在原位置重建时钟；视频不执行定位，并使用 30ms 本地缓存缩短 VLC 3
  必需的音视频队列刷新。用户确认 MP3/MP4 倍速切换流畅，视频不再先跳约两秒。
- 已实现 `Ctrl+左/右` 切换上一首/下一首，并覆盖列表边界、双向切换和无 Seek 回归。
- 倍速增加 0.75×，共六档；音量改为向上悬停的竖向滑块，点击按钮可静音或恢复；
  音量、左右键跳转秒数和倍速均位于进度条右侧同一行。
- 音量改用不抢占点击的独立悬浮面板，点击静音时面板保持原位、不再整框闪现；静音
  显示 0%，恢复时回到原音量；倍速和跳转秒数无论悬停还是点击均固定向上展开。
- 用户确认 I-16 的音量静音操作不再闪烁，百分比显示和悬浮交互正常。
- 进度条、上一首、播放/暂停、下一首、停止、倍速、音量、跳转秒数、播放模式和
  全屏已合并到同一控制行；播放和暂停共用一个动态图标按钮。
- 上一首、下一首、停止、音量、播放模式和全屏均改用程序内绘制图标；播放/暂停、
  喇叭/静音和进入/退出全屏会随当前状态切换，音量框只显示百分比数值。
- 播放模式移入控制行，并新增图标化随机播放；多项列表不会立即重复当前项，单项
  列表自然结束时仍重播当前项。
- 同一个全屏按钮可进入和退出全屏，全屏时控制行保持可见；播放列表可通过左右箭头
  折叠或展开，音视频区域随之自动伸缩。
- “打开文件”已移动到播放列表底部原“移出选中”位置。列表右键菜单支持播放、暂停、
  停止、仅列表内重命名、置顶、上移、下移和移出；当前项的三种播放操作按实际状态
  互斥启用，其他项只允许切换播放。`Ctrl`/`Shift` 支持独立或连续多选，多选时除移出
  列表外全部置灰。排序会保持当前播放媒体身份，重命名不改变源路径或文件名。
- 音量和倍速悬停命中改为跟随控件进入/离开事件，125%/150% 缩放不再受全局光标
  坐标换算影响；三档缩放自动化均已通过。
- 音量按钮已移动到停止按钮右侧；倍速、跳转秒数和播放模式改用音量同款的不抢占
  独立 Tool 悬浮窗，持续停在按钮上不会再因菜单抢占鼠标而反复闪烁。
- 播放模式按钮现在每次点击循环切换下一种模式；悬停列表保持显示并同步唯一勾选项。
- 纯音频使用独立 libVLC 实例中的辅助解码器取得当前媒体的真实 PCM，画面以 192 点
  绘制一条位于底部、不横向移动的连续线；PCM 归并为 24 个圆滑控制段，只从底部
  基线向上形成疏密适中的高波峰，并按当前声音强度快速升起和回落。辅助解码器不争用
  主时钟，声音、音量和倍速链路保持不变。
- 波形画布只保留文件名，内部进度条、百分比和说明文字已移除；控制行主进度条不变。
- 音频画布缓存静态背景并取消随画面宽度增长的三层双路径计算，解决全屏波形卡顿。
- 音频在打开、缓冲、播放和暂停状态下可以进入全屏，同一个按钮可退出，图标和
  控制行行为与视频全屏一致。
- I-01 至 I-25 自动化均已通过；I-14、I-16 的人工验收通过，其他待验收状态以
  `docs/测试/12-阶段11测试.md` 为准。
- 封存前最终回归：Release 全量 CTest `44/44` 通过，约 13.53 秒；Debug 全量 CTest
  `44/44` 通过，约 15.76 秒；核心独立 CTest `30/30` 通过，约 3.77 秒；无测试构建成功。
- 阶段 11 逐项记录已建立于 `docs/测试/12-阶段11测试.md`，状态为已封存。

已知遗留：

- 更多编码组合、结构化日志级别配置和持久化仍不在当前范围。
- FLAC、MKV 等其他真实格式尚未取得合法本机样本，仍待后续补充验收。
- 125%/150% 仅完成模拟布局测试，仍需在 Windows 实际缩放下进行人眼验收。
- M-05 已由用户确认：停止后进度归零，视频区域清空并显示“视频已停止”。
- M-01 至 M-22 已有通过证据；集中场景、其他格式和实际 DPI 仍待完成。
- 用户已决定把在线歌词作为下一增量，原“v0.1 不含网络能力”的边界将在新增量文档中
  调整；在线能力只用于歌词与元数据，不扩展为网络音频播放。

## 3. 长期开发规则

- 使用 C++20、CMake、Ninja、Qt 5.14.2 和 GoogleTest。
- 公共核心头文件放入 `include/mediahub/core/`，实现放入 `src/core/`。
- 内核实现放入 `src/engine_vlc/`，公共头文件放入 `include/mediahub/engine_vlc/`。
- GUI 代码放入 `apps/gui/`。
- 测试分别放入 `tests/unit/`、`tests/integration/` 和 `tests/gui/`。
- 每个阶段编写独立测试文档，文件名使用目录下一可用序号和两位阶段号，例如
  下一阶段使用 `27-阶段13测试.md`。
- 第三方源码统一放入 `Third_Party/<library>/`，不直接修改。
- **libVLC 不进入仓库**，通过 `MEDIAHUB_VLC_ROOT` 指向本机目录。
- **核心层不得依赖 Qt 和 libVLC。**
- **GUI 层不得链接 libVLC。**
- **内核事件回调中不得操作 Qt 控件。**
- `main.cpp` 只处理程序边界、对象组装和异常兜底。
- 项目自有代码注释必须使用中文。
- 涉及线程的函数，注释必须写明调用线程。
- 测试媒体在运行时生成，不依赖固定盘符或个人路径。
- 不提交任何有版权的音视频素材。
- 不提交 `build/`、`cmake-build-*/`、`.idea/` 和 `.vs/`。

## 4. 本机环境

2026-08-03 实际扫描结果：

| 组件 | 版本 | 位置 |
|---|---|---|
| Qt | 5.14.2 msvc2017_64 | `C:\Qt\Qt5.14.2\5.14.2\msvc2017_64` |
| Qt Creator | 随 Qt 安装 | `C:\Qt\Qt5.14.2\Tools\QtCreator` |
| MSVC | 14.44.35207 | VS 2022 Community |
| Ninja | VS 2022 自带 | `...\CommonExtensions\Microsoft\CMake\Ninja` |
| CMake | 4.4.0-rc3 | PATH |
| Git | 2.55.0 | PATH |
| libVLC | 3.0.21 win64 | `C:\SDK\vlc-3.0.21` |
| GoogleTest | 1.17.0 | `Third_Party/googletest` |

注意事项：

- 本机只有 `C:` 一个分区。
- **没有安装 CLion**，使用 Qt Creator、Visual Studio 或 VS 开发者终端。
- 普通终端找不到 `cl.exe` 和 `ninja.exe`，必须用开发者终端或 IDE 构建配置。

## 5. 阶段进度

| 阶段 | 主题 | 状态 |
|---|---|---|
| 0 | 文档与边界 | 已完成 |
| 1 | 环境准备与依赖接入 | 已完成 |
| 2 | 最小工程骨架 | 已完成 |
| 3 | 播放内核抽象与状态机 | 已完成 |
| 4 | libVLC 内核实现与本地音频 | 已完成 |
| 5 | GUI 播放控制与线程边界 | 已完成 |
| 6 | 视频输出 | 已完成 |
| 7 | 进度与音量 | 已完成 |
| 8 | 播放列表 | 已完成 |
| 9 | 错误处理与结构化日志 | 已完成 |
| 10 | 自动化测试补强 | 已完成 |
| 11 | 真实验收 | **已封存** |
| 12 | v0.1 Release 交付 | **已封存（本地自用）** |

各阶段的详细产出和通过条件见
[规划/02-项目完整大纲.md](规划/02-项目完整大纲.md) 第 10 节。

## 6. 已完成增量：在线歌词

**目标**：为纯音频增加圆形“词”按钮与在线歌词显示，不改变本地媒体播放链路。

**具体工作**：

1. 歌词按钮放在音量按钮旁边，保持圆形控制样式，按钮文字为“词”。
2. 优先读取音频内嵌歌词；在线查询按酷狗、网易云、LRCLIB、TheAudioDB 的顺序降级。
3. 酷狗按歌曲分组和音频条目哈希优先提供精确时间轴；网易云补中文同步与翻译歌词；
   LRCLIB 补同步歌词；TheAudioDB 补海外普通歌词、封面、专辑与艺术家信息。
4. 使用标题、艺术家、专辑和时长共同匹配，避免同名歌曲误配；查询异步执行并缓存，
   断网、超时或无结果时不影响播放。
5. 已完成按钮、歌词显示、录音变体排除、单曲时间校准，以及同步歌词、普通歌词、
   顶部信息和校准控件的全屏响应式缩放；`+0.5s` 让歌词变快，`-0.5s` 让歌词变慢，
   旧校准值会自动迁移并保持原有效果；封面与艺术家展示保留为后续增量。

**通过条件**：按钮布局稳定，歌词可切换显示，同步歌词跟随进度，失败可安全降级，
播放、倍速、波形和全屏行为无回归。

**结果**：两首真实 MP3、全屏进入/退出和响应式字号实机验收通过，Release 全量
CTest `44/44` 通过。阶段 12 从 Release 隔离目录、许可证材料和最终交付清单开始。

## 7. 已完成阶段：v0.2 单 URL 直播

**v0.1 已完成**：

1. 增加 Release 安装规则、发布包校验和隔离播放脚本。
2. 空目录 Debug/Release 全目标构建成功，CTest 均为 `44/44`。
3. Release GUI 套件连续 5 轮通过。
4. `dist/MediaHub-0.1.0-win64` 结构校验通过，许可证材料齐全且无 Debug/测试文件。
5. 最小环境下真实 MP3 播放、依赖来源检查和正常退出通过；强制 125%/150% 冒烟与
   用户人眼验收通过。
6. 已生成 78.91 MiB ZIP、记录 SHA-256，并创建
   `docs/交付/03-v0.1.0-最终交付清单.md` 交付候选。

**v0.1 公开发布保留项**：实际无开发环境 Windows x64 机器或虚拟机复测、Windows 系统级
125%/150% 布局复测，以及将交付候选清单转为正式公开发布。详细证据见
`docs/测试/13-阶段12测试.md`。

以上仅是公开发布前的保留项，不阻塞 v0.2 开发。

**v0.2 第一增量已完成**：

1. “文件”菜单增加“打开网络地址”，由用户输入自己提供的直播 URL。
2. 纯核心层校验绝对 URL、协议白名单和目标部分，非法输入不得进入播放内核。
3. 首批协议为 `http`、`https`、`rtsp`、`rtmp`、`rtmps`、`udp`、`rtp` 和 `srt`。
4. libVLC 使用网络位置入口异步连接；直播时长未知或不可定位时保持进度条禁用。
5. 日志和列表名称不暴露 URL 用户信息、查询参数或片段。

当前实现、Debug 全量 CTest `48/48` 和核心独立 CTest `33/33` 已通过。使用本机 VLC
循环输出的 HTTP MPEG-TS 地址完成真实网络打开；直播使用原生 `1.0×`，进入播放后
重新同步音量和静音状态，用户确认画面与声音正常。音量悬浮框左移、与按钮重叠并以
500ms 延迟收起，用户确认位置和移动稳定；悬浮框可见时会继续跟随按钮，避免 MP3
总时长出现或窗口布局变化后产生偏移。播放列表右键菜单补充暂停和停止，当前项
按播放状态互斥启用，其他项和多选不会误操作当前播放；GUI 定向测试和 Debug 全量
CTest 已覆盖。

**v0.2 第二增量实现与自动化已完成**：

1. `PlayerEventListener` 新增缓冲百分比事件，libVLC 通过 `EngineEventBridge`
   安全回到 GUI 线程，网络媒体显示“正在缓冲 N%”。
2. 网络打开使用 15 秒单次计时器；进入播放或失败、停止、切换、移除
   当前网络项时关闭。超时后停止内核、显示“连接超时”并允许重试。
3. 连接期间点击停止会立即显示“已取消连接”，不等待 libVLC 状态事件；
   取消或超时后的迟到事件被忽略。
4. 网络打开失败统一映射为“无法连接网络媒体”，不记录完整 URL、凭据、
   查询参数或片段。
5. 真实慢连接验收暴露 libVLC 3 同步停止旧直播可卡死 GUI。内核控制现已改为
   专用线程串行执行；取消或切换网络流时替换播放器，旧实例在后台回收。
6. Debug 全量 CTest `49/49` 通过；核心独立 CTest
   `33/33` 通过（约 0.86 秒）。

2026-08-05 用户实机确认：先播放 `127.0.0.1:18766/live.ts` 正常 HTTP
MPEG-TS，再切换到只接受 TCP 但不返回媒体的 `18767` 慢连接，窗口保持响应；
点击停止后可取消，随后重新打开 `18766` 仍有画面和声音。

**第三增量历史方案已完成**：曾使用 libVLC 活动统计、播放位置兜底和 1、2、4 秒
退避实现最多 3 次自动重连；本机可控断流的自动恢复、次数耗尽、停止/切换取消均已
验收。会话内最近地址最多 10 条，去重、最近优先且不持久化。公网流复验后最终策略
改为用户主动刷新，历史方案和证据保留在第三增量测试记录与提交 `39b3cf7` 中。

2026-08-05 本机 HTTP MPEG-TS 实机验收确认：停止中继后，即使 libVLC 仍保持
`Playing` 且不发送 `Failed`/`EndReached`，界面也会进入 `1/3` 重连并在恢复中继后
自动恢复画面和声音；不可用时只耗尽三次，手动播放可恢复；停止后不会被旧重连
唤醒；切换到《套马杆》后，即使恢复旧直播源也不会抢回画面或声音。最近地址可从
可编辑下拉框选择，重启后下拉框为空。

第三增量已由提交 `39b3cf7` 推送到 `origin/main`。

**第四增量已完成**：本机 `18766` HTTP MPEG-TS 的真实画面、声音、不可定位进度、
窗口移动缩放、音量与静音、暂停继续、停止后不重连和全屏均通过；直播与本地 MP3、
MP4 往返后，进度定位、波形、歌词、画面、声音和全屏无回归。使用只接受 TCP 但不
返回媒体的 `18767` 验证连接期间移动、缩放和全屏保持响应，15 秒后显示连接超时，
点击播放可重试并能立即停止取消。播放中直接关闭后 `MediaHub.exe` 已完全退出，
两个本机测试服务均已停止且没有残留进程。项目版本元数据更新为 `0.2.0`。

2026-08-05 封存后公网兼容复验确认：HLS 即使报告有限时长或可定位窗口，界面仍
保持 `--:--` 和不可拖动的直播语义；HTTP-FLV 的 libVLC 活动统计会在正常播放时
短暂冻结，不适合作为跨协议自动重连依据。最终删除自动重连计时器、活动轮询和停滞
看门狗，网络直播增加“刷新直播”按钮与 `F5`；用户在播放、缓冲、失败或普通画面
停顿时均可主动重开当前列表项，本地媒体不响应。直播 A→直播 B→本地歌曲逐项实测
通过，歌曲无需调节音量即可立即听到声音。直播刷新和直播间切换复用当前 libVLC
实例，切回本地媒体时创建新实例隔离旧直播后台停止；旧播放器关闭音视频轨后停止，
新播放器继承并在进入播放后再次应用音量和静音状态。公网地址只用于人工输入，未
进入仓库。

2026-08-06 对最终手动刷新版本重新构建并回归：Debug/Release 全量 CTest 均为
`50/50`，分别用时约 18.87 秒和 15.83 秒；Core-only CTest `33/33` 通过（约
0.69 秒），无测试构建成功且 CTest 为 0。修改文件格式检查、`git diff --check`
和模块边界扫描均通过。用户实机确认按钮与 `F5` 刷新后均恢复真实直播音画，随后
切回本地歌曲立即有声音，不需要调节音量。

2026-08-06 第五增量完成：纯核心层新增 `LiveChannel`、`LiveChannelLibrary`、
UTF-8 M3U 解析与序列化，支持 BOM、CRLF/LF、中文、分类、可选 EPG/Logo 字段、
非法/重复统计和往返一致性。诊断不携带完整 URL，失败序列化不返回半成品。新增
17 个 M3U 场景；Debug/Release 全量 CTest 均为 `67/67`，Core-only `50/50`，无测试
构建成功且 CTest 为 0。GUI 测试的歌词 `QSettings` 已隔离到临时目录，不再污染真实
用户配置。第五增量不含可见界面，无需人工验收。

2026-08-06 用户取消频道分类、本地 `channels.m3u` 和逐频道添加、编辑、删除方案。
新目标是输入一个远程 M3U/M3U8 清单 URL，显示当前链接内的扁平直播条目，并把
“本地播放列表”和“直播列表”完全分开。清单读取需要异步超时、大小/条目上限、
相对地址补全和迟到结果隔离；载入失败保留上次有效直播列表。分类、Logo、EPG、
收藏和定时订阅不在当前范围。

2026-08-06 第六与第七增量实现和自动化完成：新增 `LivePlaylistService`，只读取
清单文本，不下载 MP3、MP4 或其他音视频；限制 10 秒、5 次安全重定向、2 MiB 正文
和 5000 项，禁用 Cookie，拒绝普通 HLS 分片清单。核心解析支持以最终响应 URL 补全
相对条目。播放列表侧栏新增“本地播放列表 / 直播列表”标签、清单 URL 输入和扁平
只读直播列表；本地与直播模型完全分开，选择直播条目复用既有网络播放链路。

用户真实 HTTPS 清单首次验收暴露两个问题：Qt 5.14 部署中没有 OpenSSL 1.1，导致
`QNetworkAccessManager` 无法建立 HTTPS；`Ctrl+L` 也只把地址当作单路直播。Windows
产品读取已切换为线程池内的系统 WinHTTP，保留证书校验、Cookie 禁用、安全重定向、
超时和资源限制；`Ctrl+L` 现在自动探测 `.m3u`/`.m3u8`，频道清单展开，普通 HLS
自动回退为直接播放。没有新增第三方 TLS 运行库。

首次修正后的页面复验仍出现“无法连接、列表只有一个文件”，最终定位为 `.m3u` 与
`.m3u8` 共用了单路 HLS 回退。现已固定 `.m3u` 只能作为频道清单，读取或解析失败时
绝不新增为单路直播；只有有歧义的 `.m3u8` 才允许在检测到真实 HLS 控制标签后回退。
HLS 标签改为逐行检查行首，避免频道名称或元数据中的相似文本造成误判。真实清单经
服务和完整 Presenter 链路诊断均解析出 441 项，且不会向播放内核发送清单 URL。

新增 2 个核心相对地址场景、独立本机 HTTP 清单测试和双列表 GUI 回归。最终语义收紧
后的 Debug/Release 全量 CTest 均为 `70/70`，分别用时约 24.92 秒和 18.81 秒；Core-only
`52/52`，无测试构建成功且 CTest 为 0。

2026-08-06 首轮页面验收发现：先选择明显卡顿的直播源，再切换其他源时，会创建
脱离 MediaHub 且无法由主界面关闭的独立视频窗口。第一次删除旧播放器后台回收时的
空 HWND 并保留原嵌入句柄，自动化通过但真实复验仍会弹窗。第二次为每个旧播放器在
回收线程创建独享的非空隐藏 HWND，先迁移视频输出并关闭音视频轨，确认准备完成后
新播放器才绑定主画面；A→B→C 句柄回归连续验证两次有序交接且没有空句柄，但用户
复验仍能弹窗。

第二次复验的原生窗口枚举确认，弹窗是父窗口和所有者均为空的顶层
`VLC video main`，其子窗口为 `VLC video output`；此时旧播放器的隐藏 HWND 已经
销毁，证明弹窗属于当前替换播放器。Qt 视频宿主 HWND 也具备 libVLC 要求的
`WS_CLIPCHILDREN` 样式。高概率原因进一步收敛到每次直播切换都创建新的 libVLC
实例，后创建实例的 vout 会回退到自建顶层窗口。

第三次修正让直播刷新、直播 A→B→C 只替换播放器并复用当前主实例，同时保留旧
播放器的隐藏 HWND 后台回收；直播切回本地媒体仍创建新实例，继续隔离旧直播停止
对本地声音的影响。新增实例计数回归：A→B→C 始终只有主播放和音频分析 2 个实例，
直播切本地生成 WAV 达到 3 个实例并继续产生波形和进度，但用户复验仍会弹窗。

第三次失败现场再次枚举原生窗口，同时观察到旧 `VLC video main` 仍作为 MediaHub
视频宿主的可见子窗口，新 `VLC video main` 已成为父窗口和所有者均为空的可见顶层
窗口。旧内嵌画面随后才结束，与用户观察一致。这证明 `set_hwnd` 无法迁移已经创建的
活动 vout；只要旧 vout 尚未完全退出，新播放器就可能退回独立窗口。

第四次修正删除隐藏 HWND 迁移：控制方法仍立即返回 GUI，但内核控制线程会完整关闭
旧播放器音视频轨，等待 `stop` 返回并释放播放器后，才绑定和启动新播放器。打开请求
增加递增编号；旧 vout 停止期间如果用户又选择其他源，被取代的中间请求直接丢弃，
最终只打开最后选择的源。停止门闩回归在旧实现上稳定复现“旧播放器未停止便打开
新源”，修正后验证释放前不会进入新 `Opening`，且 A→B→C 会跳过 B。最新
Debug/Release 全量 CTest 均为 `72/72`，分别用时约 23.27 秒和 21.79 秒，第四版
Release 已构建。用户随后按相同的卡顿源连续切换路径实机复验通过，独立窗口未再
出现，最终选择的源保持在 MediaHub 视频区域播放。

2026-08-06 新增直播右键菜单：只保留“播放/暂停”、“停止”、“标记/取消标记”和
“收藏/取消收藏”，明确不提供删除；停止只对当前可停止直播源启用。右键单击只弹菜单，
右键双击不播放；标记以直播源
地址作为会话内身份。
条目显示“【不可用】”、浅红警示背景、深红加粗文字和悬停提示，同时置灰且不可选择
或播放，仍可右键取消标记，标记当前源会提交停止。清单刷新后按源身份而不是相同行号
判断“播放/暂停”。标记状态现只刷新目标行，长列表不会跳回首项；直播页新增“定位
正在播放”，按当前源地址居中选中对应行。最终 Debug/Release 全量 CTest 均为
`72/72`，最新回归分别用时约 22.50 秒和 19.17 秒。

2026-08-06 右键菜单新增会话内“收藏/取消收藏”，按直播源地址识别并以“★”前缀
显示；收藏只刷新目标行，不改变播放能力、列表顺序或滚动位置。定向 GUI 首次回归发现
标记当前播放源后定位按钮未立即禁用，Presenter 现只在标记当前源并提交停止时同步
重绘，非当前行标记或收藏仍不会把无选中长列表滚到当前播放项。两个新增场景单独通过，
GUI 套件通过；最新 Debug/Release 全量 CTest 均为 `72/72`，分别用时约 24.16 秒和
20.76 秒，最新 Release 已构建。

2026-08-06 用户实机复验发现标记条目后仍会自动定位到当前播放源。窗口现于标记或
收藏动作前保存滚动值，并在同步处理完成及下一事件循环各恢复一次，不再依赖 Qt 平台
插件的数据刷新行为；自动化会主动模拟模型刷新期间滚到当前源，确认保护有效。直播
右键同时新增“停止”，仅在目标就是当前可停止直播源时启用。最新 Debug/Release 全量
CTest 均为 `72/72`，分别用时约 22.93 秒和 19.54 秒，最新 Release 已重新构建。

用户随后使用最新 Release 实机确认：四项右键菜单、停止启用规则、标记/收藏不自动
定位到当前播放源均正常，直播右键交互验收通过。

用户继续确认真实清单名称与顺序、双击直播音画、停止/刷新，以及无效清单提示错误后
保留上一次有效直播列表均正常。

用户最终确认两个列表标签和清单输入区布局正常，本地 MP3/MP4 与直播列表互不混入，
切换标签不会停止当前媒体。第六与第七增量的全部人工验收项通过。

2026-08-06 新增独立 `WindowIconManager`，程序启动时读取 `MediaHub.exe` 旁固定目录
中的 `icons/taskbar.png` 和 `icons/window.jpg`，分别控制 Windows 任务栏大图标与
标题栏左上角小图标。任一图片缺失或损坏时只回退对应的内嵌资源；Windows 原生
`HICON` 由组件统一创建、恢复和销毁。构建与 Release 安装规则会生成默认图片，用户
以后只需替换同名文件并重启，无需重新编译；资源管理器中的 EXE 文件图标仍是编译期
资源。外部优先、缺失/损坏回退和两个槽位隔离测试已加入 GUI 套件，最新 Debug/Release
全量 CTest 均为 `72/72`，分别用时约 27.70 秒和 20.55 秒。

用户已在 Release 的 `icons` 目录替换两张同名图片，并在连续两次重启后确认任务栏与
标题栏左上角均显示正确，运行时图标人工验收通过。

上述远程清单、双列表、直播右键、切换修正和图标增量已提交为
`4c9e663 feat: 完成远程直播清单与可替换图标`，并成功推送到 `origin/main`；提交后
工作区干净且本地、远端均指向 `4c9e663`。

2026-08-07 已清理取消的播放内核候选方案。当前仓库只保留已采用的 libVLC 内核与
测试用假内核，不存在其他播放内核的源码、依赖、构建目标或后续版本计划。受版本
控制内容和文件名的残留扫描为零匹配；Release 全量 CTest `72/72` 通过（24.61 秒）。

2026-08-06 v0.2 第八增量完成：全新 Release `72/72`（21.20 秒）、Core-only
`52/52`（6.52 秒）和无测试构建通过；发布目录共 398 个文件、187.82 MiB、365 个
VLC DLL 插件，两个运行时图标已纳入结构校验。真实 MP3 隔离运行确认 Qt/VLC 依赖
来自发布目录且退出码为 0。已生成 80.77 MiB ZIP，SHA-256 为
`9A47F599413B9D88610E1843E24D4A76BF01C63CDD87793DA1D6E0D96FB0C618`。详细证据见
`docs/测试/21-v0.2-直播增量08测试.md` 和 `docs/交付/05-v0.2.0-最终交付清单.md`。

**v0.2 封存结论**：v0.2 已完成交付，不继续在该版本增加功能。后续界面工作归入
独立的 v0.3 规划，v0.2 的播放与直播行为作为不可回归基线。

详细设计与验收项见 `docs/规划/05-v0.2-URL直播播放.md`、
`docs/规划/06-v0.2-直播频道库与M3U规划-已废弃.md`、
`docs/规划/07-v0.2-远程直播清单URL规划.md`、
`docs/测试/15-v0.2-直播增量01测试.md`、
`docs/测试/16-v0.2-直播增量02测试.md` 和
`docs/测试/17-v0.2-直播增量03测试.md`、
`docs/测试/18-v0.2-直播增量04测试.md` 和
`docs/测试/19-v0.2-直播增量05测试.md`、
`docs/测试/20-v0.2-直播增量06-07测试.md`、
`docs/测试/21-v0.2-直播增量08测试.md`、
`docs/交付/04-v0.2.0-单URL直播交付说明.md` 和
`docs/交付/05-v0.2.0-最终交付清单.md`。

用户提供的仓库外目录只作为第三方格式参考，不属于 MediaHub 源码。开发时不得执行
其中脚本、接入构建或提交其直播地址和资源；自动化使用运行时生成的测试 M3U 和
本机可控测试流。

## 8. 已完成阶段：v0.3 界面重构

**目标**：本地音频提取 QQ 音乐式的轻盈绿色音乐界面语言，本地视频提取腾讯视频式
的深色影院层级，直播流提取 PotPlayer 式的紧凑深色工具界面；不复制品牌资源，所有
现有按钮、快捷键、悬浮层、列表和 Presenter 命令语义保持不变。

2026-08-07 阶段 A 完成：

1. 新增 `ui_theme.*`，把主窗口内联样式提取为独立主题模块。
2. `PlayerViewState` 只推导本地音频、本地视频和直播三种视觉模式，不引入业务分支。
3. 标题区改为紧凑场景标题栏，播放工作区、媒体信息条和控制台继续复用原有控件。
4. 三种模式使用动态属性切换样式和密度，不重建控件、不重新连接信号。
5. 新增模式往返测试，确认播放按钮、进度条和播放列表对象身份保持不变。
6. Release 全目标构建成功，全量 CTest `72/72` 通过（24.42 秒）。

2026-08-07 阶段 B 完成：

1. 音频画布改为浅色低饱和绿色背景、装饰唱片环和绿色真实 PCM 波形，原波形数据、
   动画、进度及全屏逻辑保持不变。
2. 歌词页改为同一浅绿音乐主题，同步歌词、普通歌词和时间校准行为保持不变。
3. 本地视频占位改为冷黑影院画布；活动视频时 Qt 仍不绘制，避免覆盖 libVLC 帧。
4. Windows 标题栏会随音频/视频模式切换亮暗，列表标签收紧后不再裁剪；控件对象、
   标签索引和 Presenter 路由保持不变。
5. 使用仓库外 `test.mp3` 完成开发侧视觉检查；Release 全量 CTest `72/72` 通过
   （22.64 秒）。

2026-08-07 阶段 C 完成：

1. 远程清单地址、载入、定位和状态已收拢为独立信号源工具区，两个操作按钮改为同行
   紧凑布局；原按钮对象、文案状态和信号连接保持不变。
2. 直播侧栏使用更小边距、暖黄色状态、方形控制、高密度列表和窄滚动条，列表标题会
   在本地与直播之间切换，但模型、索引和选择规则不变。
3. 双列表往返测试补充工具区和控件归属断言，确认只切换布局且不产生额外内核命令。
4. 未输入外部地址的实际 Windows 空列表视觉检查通过；Release 全量 CTest `72/72`
   通过（23.58 秒），其中 GUI 套件 8.65 秒。

2026-08-07 阶段 D 自动化完成：

1. 新增三场景响应式边界测试，覆盖最小窗口、`960x720` 和 `1200x800`，确认主要
   面板、进度条、全部控制按钮和直播工具文字均未越界或裁剪。
2. GUI CTest 增加 `QT_SCALE_FACTOR=1.25` 与 `1.5` 两个独立入口，完整套件会在
   100%、125% 和 150% 三档模拟缩放下运行，包括既有全屏与悬浮层回归。
3. Release 全量 CTest `74/74` 通过（46.74 秒）；Debug 全量 CTest `74/74` 通过
   （91.79 秒）。
4. 项目版本元数据更新为 `0.3.0`，README、变更记录、版本路线和测试文档已同步。

2026-08-07 最终人工确认：

1. 本地与直播列表往返时宽度、高度和字号保持一致，列表宽度与字号随窗口尺寸调整。
2. 真实长直播清单载入后可正常播放；播放中连续调整三档窗口尺寸保持画面、声音和
   程序响应。个别频道提示网络不佳属于直播源或链路超时，不是 resize 导致。
3. 用户确认当前版本无问题，v0.3 按本地自用版本完成。

**当时下一步（现已完成）**：建立独立增量，实现本地列表、直播清单 URL 与历史记录持久化，增加
图标历史按钮和独立历史窗口，并按最新要求增加 `Ctrl+下键` 静音、`Ctrl+上键`
恢复声音；不改变单独 `F5` 刷新直播和 `F11` 双向切换全屏的语义。

## 9. 已完成阶段：v0.4 状态持久化与快捷键

2026-08-07 已确认范围：

1. 本地列表、最后直播清单 URL 和最多 20 项成功清单历史写入用户本机配置；启动时
   只恢复展示，不自动播放或解析。
2. URL 编辑框右侧使用纯图标历史按钮，独立窗口支持回填、删除和取消；回填不加载。
3. 清单载入按钮在进行中可取消，取消后不接受迟到结果。
4. `F11` 双向切换全屏，`Ctrl+下键` 静音，`Ctrl+上键` 恢复，单独 `F5` 刷新直播，
   `Ctrl+F5` 不分配功能。
5. 菜单栏增加“帮助 → 快捷键”，独立窗口列出全部现有快捷键并用“确定”返回。

2026-08-07 实现和自动化回归完成：生产程序通过注入的 `QSettings` 存储恢复本地
列表、最后直播清单 URL 和最多 20 项成功历史，启动恢复不会自动打开、解析或播放；
历史图标与独立窗口、清单载入取消、`F11` 双向全屏、`Ctrl+下/上` 静音恢复以及
“帮助 -> 快捷键”窗口均已加入。Release 与 Debug 全量 CTest 均为 `74/74`，GUI
套件内部 `78/78`，三档模拟缩放回归通过，`git diff --check` 通过。完整规划和测试
证据见
`docs/规划/09-v0.4-状态持久化与快捷键.md`、
`docs/测试/23-v0.4-状态持久化与快捷键测试.md`。

同日用户反馈初版帮助窗口颜色、按钮观感不佳，并指出标题栏“？”点击无反应。该
按钮是 Qt 默认上下文帮助入口，但项目没有对应功能，现已从标题栏移除；窗口改为
深墨绿与薄荷绿视觉，增加标题卡片、优化后的快捷键表、滚动条和明确的“确定”主按钮。
Release 重新构建成功，帮助窗口定向测试及 100%、125%、150% 三档完整 GUI 回归
均通过，用户确认新版外观和按钮交互正常。

同日用户扩展 v0.4：保留现有“本地列表 / 直播列表”，在“帮助”下新增第二个“直播源”
选项。独立窗口以两列编辑地址和备注，顶部红色提示“本窗口为方便用户保存直播源”，
新增、删除、返回和保存按钮使用彼此区分的高对比颜色。保存按钮和仅限该窗口的
`Ctrl+S` 均先显示自绘确认窗口；返回或 `X` 遇到未保存编辑时可选择保存后关闭或
放弃关闭。资料写入同一个本机 `QSettings` 边界，不解析、不播放、不进入日志。

新增定向测试覆盖 INI 往返、帮助菜单层级、保存取消、快捷键确认保存、返回放弃和
`X` 保存关闭，且确认播放内核与直播清单服务均收到零个请求。GUI 套件内部增至
`80/80`；最新 Release/Debug 全量 CTest 均为 `74/74`，分别用时 50.28 秒和
99.54 秒，三档缩放全部通过。用户完成本地人工验收并确认当前版本没有问题，v0.4
本地自用版本完成。

同日追加主窗口 `Ctrl+M` 快捷键，可直接打开“直播源”窗口；帮助窗口同步列出该操作。
新增 GUI 用例实际发送组合键并确认窗口打开，同时验证没有触发播放或清单载入。
Debug 构建通过，全量 CTest `74/74` 通过（111.03 秒），三档 GUI 均为内部
`81/81` 通过。

## 10. 已完成阶段：v0.5 日常使用增强

2026-08-10 已建立 v0.5 目标和规划，范围为直播列表即时搜索、直播收藏持久化、本地
媒体最近播放与安全断点续播。启动恢复不自动播放或访问网络，不可用标记仍只在当前
会话有效。详细设计和验收边界见 `docs/规划/10-v0.5-日常使用增强.md`，测试记录见
`docs/测试/24-v0.5-日常使用增强测试.md`。

2026-08-10 实现与自动化完成：

1. 直播页增加按频道显示名即时过滤的搜索框；过滤不改变底层行号，双击、右键、标记、
   收藏和定位仍命中原频道，“定位正在播放”会在必要时先清空搜索。
2. 直播收藏按源地址写入现有 `QSettings` 并跨重启恢复，不可用标记仍只在当前会话
   有效；状态版本保持为 `1`，v0.4 配置缺少新字段时可直接读取。
3. “文件 -> 最近播放”最多保存 20 项真正进入播放状态的本地媒体；缺失文件会移除
   记录且不调用内核。
4. 本地可定位、已知时长媒体保存安全断点；前 10 秒和末尾 10 秒不恢复，主动停止和
   自然结束清除断点。位置写入使用 5 秒单次计时器节流，暂停、切换和关闭立即保存。
5. GUI 测试程序内部场景由 `81/81` 增至 `88/88`。Debug/Release 全量 CTest 均为
   `74/74`，Debug 用时 187.56 秒，最终 Release 用时 78.04 秒；100%、125%、150%
   三档 GUI 全部通过。
6. Core-only CTest `52/52` 通过（1.94 秒）；关闭 `BUILD_TESTING` 和
   `MEDIAHUB_BUILD_TESTS` 的 Release 构建成功且 CTest 为 0；`git diff --check`、
   依赖边界和敏感日志扫描通过。

2026-08-10 最终人工验收完成：用户确认 V5-M01 至 V5-M05 全部通过。真实直播清单
搜索、清空和定位正确，收藏跨重启恢复而不可用标记按设计消失；真实本地长媒体中段
恢复、主动停止和自然结束清除断点均正常；100%、125%、150% 下搜索框和最近播放
菜单可用。启动仍不自动播放或访问网络。

当前状态：v0.5 实现、自动化、构建矩阵和人工验收全部完成，按本地自用版本封存。
详细证据见 `docs/测试/24-v0.5-日常使用增强测试.md`。

v0.5 功能提交为 `85bebb9 feat: 完成v0.5日常使用增强`。首次推送时 SSH 22 与
HTTPS 443 均连接超时，阻塞状态以 `c35bd35 docs: 记录v0.5推送阻塞` 留档；同日
HTTPS 路径恢复后，v0.5 提交已普通快进推送到 Gitee，远端 `main` 与本地
`origin/main` 已同步。SSH 22 不可达时可临时使用同仓库 HTTPS 地址，仍禁止强制
覆盖远端历史。

2026-08-10 v0.5 完成后追加界面易用性修正：主窗口默认尺寸由 `960x720` 调整为
`1200x800`，直播搜索框接入 compact、normal、large、extraLarge 四档响应式字号
（12、13、15、17 像素）。直播工具区按钮按 7:5 分配宽度，避免 Windows 中文字体
在最小窗口下裁剪。Release GUI 测试目标构建成功，100%、125%、150% 三档完整 GUI
CTest 串行回归全部通过（总用时 56.57 秒），Release 全量 CTest `74/74` 通过
（72.30 秒）。用户授权结束占用文件的旧版进程后，最新版 Release 主程序构建成功，
现已启动为 PID `42644`。用户确认默认 `1200x800` 窗口大小合适，搜索框字号随窗口
尺寸变化，搜索框与直播工具按钮文字均无裁剪；人工验收完成。人工验收后的最终
Release 全量 CTest `74/74` 通过（73.76 秒），本增量满足提交与推送条件。
功能提交 `039ea48 fix: 优化直播搜索框与默认窗口尺寸` 与验收状态提交
`5097ff4 docs: 记录界面调整验收状态` 已通过 SSH 普通快进推送到 Gitee；本条推送
完成记录随文档提交一并推送。

2026-08-10 开始辅助直播窗口易用性修正，并按用户要求拆分版本归档：历史直播源窗口
的石墨黑、暖金和低饱和红色主题，以及快捷键帮助窗口“确定”按钮的视觉层级记入
v0.3；历史窗口移除无效标题栏“？”、直播源长备注默认完整换行记入 v0.4。两个
定向 GUI 场景 `4/4` 通过，100%、125%、150% 三档完整 GUI CTest `3/3` 通过
（53.99 秒）。用户澄清看不清的是“帮助 -> 快捷键”的“确定”按钮，不是直播源保存
确认；误改的保存按钮已恢复。快捷键“确定”现改为亮蓝背景、白色粗边框和白字，并
增大到至少 `168x50`。因窗口级选择器在实机未生效，现进一步改为独立对象名和按钮
自身样式；定向测试抓取实际按钮图像并确认背景像素为蓝色。最终三档完整 GUI CTest
`3/3` 通过（39.55 秒）；最新版 Release 已启动为 PID `31072`。用户确认历史窗口
配色、标题栏、长备注完整展示和快捷键“确定”按钮均无问题，人工验收通过。人工验收
后的最终 Release 全量 CTest `74/74` 通过（56.02 秒），本增量满足提交与推送条件。
功能提交 `b463d83 fix: 优化直播辅助窗口可读性` 与验收状态提交
`0d02ea6 docs: 记录辅助窗口修正验收状态` 已通过 SSH 普通快进推送到 Gitee。

2026-08-10 修复直播源资料单元格编辑时右方向键无效的问题。根因是主窗口用于短按
右键定位、长按右键临时倍速的应用级事件过滤器越过了窗口边界；现只处理来自主窗口
自身的按键，独立弹窗保留 Qt 默认文本光标行为。新增自动化确认右键移动光标且不触发
主窗口定位；直播源流程与播放快捷键 7 个定向场景通过，Debug 三档完整 GUI CTest
`3/3` 通过（96.11 秒），Debug 全量 CTest `74/74` 通过（125.59 秒）。用户在最新
Release 中确认左右方向键均可正常移动光标；人工验收后的 Release 全量 CTest
`74/74` 通过（52.33 秒），本修正满足提交与推送条件。
功能提交 `bb5ec7e fix: 修复直播源编辑方向键冲突` 与验收状态提交
`036e691 docs: 记录方向键修复验收状态` 已通过 SSH 普通快进推送到 Gitee。

2026-08-10 完成项目文档命名和内容归档整理：`docs/` 下 50 个 Markdown 文件全部按分类使用
独立的两位连续序号，版本文件统一为 `NN-v0.x-主题.md`，阶段号和直播增量号统一补零。
根入口调整为 `01-文档导航.md` 和 `02-开发交接.md`，导航内容按编号重排并补齐 v0.5；
AGENTS、README 及文档间引用已同步更新。本次只调整文档，不改变产品源码；
内容盘点确认规划、设计、测试、学习和交付五类目录边界合理，并补齐 v0.3、v0.4、
v0.5 三份版本完成说明以及按版本归档矩阵。用户确认只保留 v0.5 ZIP，且该 ZIP 已在
仓库外独立生成；文档不虚构当前工作区无法核对的路径、大小或哈希。连续编号、Markdown
相对链接、仓库路径和空白检查均通过，满足提交与推送条件。
本轮增量已提交为 `d2abea1 docs: 整理文档分类并补齐版本说明`，并通过 SSH 普通
快进推送到 `origin/main`。

2026-08-10 至 2026-08-11 开始修复真实长直播切换阻塞。用户现场表现为 A 持续
播放时选择 B，A 仍输出，B 最终误报“连接媒体网络超时”。根因是 2026-08-06 为防止
独立 `VLC video main` 窗口而采用的同步让位策略：唯一内核控制线程必须先等待旧
libVLC 播放器完整停止和释放，某些直播会长期卡在轨道关闭或 `stop`；与此同时 GUI
从点击 B 时就开始 15 秒计时，B 尚未真正打开便被判定超时。

当前修复为每次打开返回唯一 `OpenRequestId`，只在内核发出当前请求的
`onOpenStarted` 后启动网络计时和自动播放；每次视频切换使用新的 Qt 原生子窗口，
不同句柄的旧播放器放到独立线程回收，新播放器无需等待且不会与旧 vout 共用 HWND。
回收完成后通过共享事件分发器通知 GUI 销毁旧子窗口，引擎关闭后不会访问失效监听器；
同句柄转交和本地同步切换的窗口所有权也有独立回归。普通切换最多保留 3 个异步回收，
并为 Stop 预留第 4 个名额；达到上限会归还新句柄并明确提示稍后重试，不会无限累积
线程或播放器。

修复前测试稳定证明：点击 B 后计时器立即启动，旧播放器停止门闩阻塞时 B 在 500ms
内不能进入 `Opening`。修复后的 libVLC 定向场景、旧请求迟到隔离和非空独立句柄
断言和回收容量上限均通过；最终 Release 全量 CTest `77/77`（56.10 秒），Debug
全量 `77/77`（114.89 秒），两套均包含 100%、125%、150% 完整 GUI 回归。用户确认
真实直播 A→B→C、旧声音停止、无超时误报、无独立 VLC 窗口以及 Stop/再次播放/关闭
全部没有问题。最终有限回收版 Release 已构建并启动为 PID `27608`。详细记录见
`docs/测试/25-v0.5-直播切换阻塞修复测试.md`；本轮不生成或替换 ZIP，用户已有的
v0.5 ZIP 继续保留在仓库外。本修复已满足提交与普通推送条件。

本修复已提交为 `a45c518 fix: 修复直播切换阻塞与超时误报`，并通过 SSH 普通快进
推送到 `origin/main`；未生成或替换仓库外已有的 v0.5 ZIP。

2026-08-11 修复顺序播放到本地列表最后一个视频并自然结束后，点击播放只有进度、
没有画面，必须再次双击列表项才能恢复的问题。Presenter 原有重播路径已经创建新 HWND
并执行 Open -> Play -> Seek(0)；根因是 libVLC 在发出 `EndReached` 后即使状态已变为
`Ended`、`Stopped` 或 `NothingSpecial`，旧 vout 仍可能保留，旧实现却根据终态跳过
`libvlc_media_player_stop()`。

本地播放器现在只要仍绑定旧媒体，就在更换媒体或 HWND 前显式 Stop。新增 libVLC
集成用例确认 Stop 完成后才应用重播句柄并释放旧句柄；新增 GUI 用例覆盖顺序播放末项、
新非空 HWND 和 Open -> Play -> Seek(0) 顺序。Debug 全量 CTest `78/78`（120.61 秒）、
Release 全量 `78/78`（78.53 秒），两套均包含 100%、125%、150% 完整 GUI 回归。
用户确认真实末项视频自然结束后只点击播放即可恢复画面和进度，不再需要双击列表项。
详细记录见 `docs/测试/26-v0.5-本地视频末项重播修复测试.md`。本轮仍不生成或替换 ZIP，
仓库外已有的 v0.5 ZIP 保持不变；本修复已满足提交与普通推送条件。

本修复已提交为 `dd32162 fix: 修复本地视频末项重播无画面`，并通过 SSH 普通快进
推送到 `origin/main`；未生成或替换仓库外已有的 v0.5 ZIP。

## 11. 已完成修复：本地 M3U/M3U8 直播清单

2026-08-11 用户报告 VLC 桌面版能够打开本地 IPTV M3U8、展开频道并播放，但
MediaHub 既不解析也不能播放。根因已经确认：本地文件入口把 `.m3u/.m3u8` 当作单个
普通媒体交给 libVLC，只有远程 URL 才会进入现有 M3U 解析服务。

现已增加本地清单异步读取并复用核心解析器；成功后切换到直播列表并按原顺序展示频道。
真正的 HLS 主清单或媒体清单仍回退为本地媒体直接播放；混合多选明确拒绝，完整本地路径
不进入日志、远程清单历史或持久化配置；`.m3u/.m3u8` 已加入文件选择器默认媒体筛选。

用户提供的 8 个仓库外真实样本已由项目核心解析器验证，接受条目数依次为 377、377、
195、195、10、10、172、172，全部零跳过、零重复、零解析问题。最终 Debug/Release
全量 CTest 均为 `78/78`，分别用时 201.10 秒和 70.85 秒；最后一次“只在解析成功后
保留清单”收口后的 Debug/Release GUI 与清单服务定向 CTest 均为 `2/2`，分别用时
49.49 秒和 11.06 秒。首次让用户复验时仍运行的是修复前的旧 Release，因此看不到
默认筛选中的清单且拖入无效；最新 Release 已纠正该环境偏差。

修复前已经保存在本地列表中的 M3U8 在双击时也会重新解析；新打开的本地清单按路径
去重并保留在本地列表，切换其他 URL 清单后可再次双击恢复频道。用户已用 377 项真实
清单完成复验，并于 2026-08-11 确认当前没有问题，人工验收通过。详细记录见
`docs/测试/27-v0.5-本地直播清单支持测试.md`。本轮不生成或替换 ZIP，现已满足提交与
普通推送条件。

本修复已提交为 `7cb46c0 feat: 支持本地直播清单`；未生成或替换仓库外已有的
v0.5 ZIP。

## 12. 自动化证据收口阶段：v0.6 内置网页浏览器

2026-08-11 用户确定下一版本为 v0.6，在本地和直播之外增加真正的第三个“网页”展示
模式，可以输入网址、登录自己的账号并观看网站提供的直播或视频。技术路线确定为
Microsoft WebView2，不使用 Qt 5.14.2 中较旧的 Qt WebEngine；本机已检测到 Evergreen
Runtime，但正式实现仍必须提供运行时缺失提示。

网页模式使用 MediaHub 专用持久 Profile，正常重启后由网站 Cookie 和站点存储保持
登录，不与 Edge 个人资料共享。MediaHub 不读取或保存账号密码；进入网页时暂停正在
播放的 libVLC 媒体，离开网页时挂起并静音网页，两边都不自动恢复播放。

用户进一步明确这是本人开发自用，需要接近 Edge 的完整正常浏览能力。v0.6 因此允许
基础下载和上传、登录弹窗、摄像头/麦克风等逐站点权限、本地文件、外部协议以及当前
会话自签名证书例外，但所有会影响本机文件、设备或其他应用的动作都必须显示来源并由
用户确认。仍禁止把密码、Cookie、令牌和完整 URL 写入日志，禁止向任意网页暴露本地
宿主对象，也不实现绕过登录、付费、DRM、地区或网站访问控制。

规划、设计和测试计划已经分别建立于
`docs/规划/11-v0.6-内置网页浏览器.md`、
`docs/设计/05-v0.6-WebView2集成设计.md` 和
`docs/测试/28-v0.6-内置网页浏览器测试.md`。本节后续按 Task 1 至 Task 11 保留实施
历史；当前已完成固定 SDK、浏览器抽象与页面、第三展示模式、真实 WebView2 后端、
安全交互、弹窗/全屏/生命周期、受控环回、响应式界面和发布回归。Task 12 已完成
文档和自动化证据收口，真实网页人工验收尚未执行。

v0.6 规划、设计与测试文档已提交为
`7834c40 docs: 规划v0.6内置网页浏览器`；本提交不包含 SDK、程序源码或构建产物。

2026-08-11 已依据三份 v0.6 文档完成可执行实施计划，路径为
`docs/superpowers/plans/2026-08-11-v0.6-webview2-browser.md`。计划把工作拆分为固定官方
SDK、CMake/Runtime 探针、假后端与地址策略、网页页面、第三展示模式、真实 WebView2
后端、安全权限与下载、弹窗/全屏/生命周期、环回集成测试、响应式界面、发布回归和
人工验收交接 12 个阶段；每个实现阶段均按测试先行、定向回归和独立提交推进。

官方 NuGet 版本元数据已于 2026-08-11 核对，计划固定当前最新稳定版
`Microsoft.Web.WebView2 1.0.4129.50`。用户已通过 Azure China NuGet 镜像取得
`microsoft.web.webview2.1.0.4129.50.nupkg`，包大小为 `9,245,553` 字节，SHA-256 为
`D3934F482D484B89FB4825DF720C710664E1143A1E90F7B3A60794EF33F473D2`；包内元数据为
`Microsoft.Web.WebView2 / 1.0.4129.50 / Microsoft`。

同日实施前 Debug 基线全量 CTest `78/78` 通过，用时 142.96 秒；当时 `main` 与
`origin/main` 同步，既有功能基线正常。此前因无法联网取得官方包造成的依赖阻塞现已
解除。NuGet Author 签名主体为 `Microsoft Corporation`，Repository 副署主体为
`NuGet.org Repository by Microsoft`，签名核验退出码为 `0`；离线环境无法访问证书
撤销服务器，因此撤销状态未知，不据此声称证书未被吊销。

本依赖阶段只把未修改的 `WebView2.h`、`WebView2EnvironmentOptions.h`、x64
`WebView2LoaderStatic.lib`、`LICENSE.txt`、`NOTICE.txt` 和包 nuspec 纳入
`Third_Party/webview2/`。x86、ARM64、Runtime、DLL、.NET 程序集、工具和用户资料均未
进入仓库；Evergreen Runtime 保持为外部依赖。该依赖提交本身没有实现产品源码、CMake
目标或 Runtime 探针。

计划 Task 2 已于 2026-08-11 完成。根 CMake 新增 `MEDIAHUB_WEBVIEW2_ROOT` 缓存路径，
并且只在非 Core-only 分支校验两个固定头文件和 x64 静态 Loader；当前 WebView2 后端
明确只支持 Windows x64。隔离的 `mediahub_webview2_sdk` 接口目标使用 SYSTEM include，
链接 `WebView2LoaderStatic.lib`、`version.lib` 和 `ole32.lib`，只有控制台目标
`mediahub_webview2_probe` 使用该接口。`mediahub_core`、`mediahub_logging`、
`mediahub_engine_vlc` 和 `mediahub_gui_support` 均未链接或包含 WebView2 SDK。

Loader 采用静态库，没有复制或部署 `WebView2Loader.dll`。Debug 探针自动重配并构建
成功，直接运行输出 `WebView2 Runtime version: 151.0.4129.72`；对应
`stage1_webview2_probe` CTest 为 `1/1` 通过。全新的 Ninja Core-only
目录在不传 Qt、libVLC 或 WebView2 路径的情况下完成配置和构建，全部 Core-only CTest
为 `52/52` 通过；CMake trace 只执行 WebView2 缓存路径声明，没有执行 SDK 文件检查、
目标创建或链接配置。

计划 Task 3 已于 2026-08-11 完成。新增 GUI 层稳定浏览器值类型、事件监听接口、可注入
后端抽象、无副作用顶层地址分类和记录命令的假后端。浏览器 GUI 测试使用独立 Qt Test
目标，避免和既有主窗口测试的入口冲突；先确认缺少地址策略头文件时编译失败，再实现
最小代码使 `gui.browser_page` 为 `1/1` 通过（0.17 秒）。实施前最新 Debug 全量基线为
`79/79` 通过（127.29 秒）。边界扫描确认核心层没有 WebView2/COM 命中，GUI 支持层仅在
浏览器抽象注释中说明隔离关系。下一步从计划 Task 4 开始，实现 BrowserPage 导航、
状态、快捷键和清除资料界面。

计划 Task 4 已于 2026-08-11 完成。新增独立 `BrowserPage`，提供后退、前进、刷新或
停止、主页、地址访问、`Ctrl+L`、`Ctrl+R`、`F5`、页面标题和稳定中文错误状态；首次
显示取得原生宿主句柄后才异步初始化，resize 只提交有效物理像素边界。清除网页数据
使用明确说明且不影响 Edge 的确认窗口，取消零调用、确认单次调用，旧代次回调不会
覆盖当前页面。浏览器测试程序内部 6 项通过；Debug 的 `gui.stage10_coverage` 与
`gui.browser_page` 组合 CTest `2/2` 通过（50.77 秒）。下一步从计划 Task 5 开始，接入
本地、直播、网页三个展示入口并协调 libVLC 暂停和网页隐藏状态。

计划 Task 5 已于 2026-08-11 完成。中央区域现在由始终可见的本地、直播、网页入口栏和
页面栈组成；原生播放页保留现有列表、歌词、视频和控制行，网页页使用可注入的
`BrowserBackend`。`MainWindow` 默认注入 Runtime 缺失后端，生产组合根后续注入真实
WebView2；Presenter 进入网页时对 Opening、Buffering、Playing 请求一次暂停，离开网页
只静音、挂起并隐藏网页，不自动恢复原生媒体。网页页启用期间原生播放快捷键被禁用，
事件过滤器不再截获网页输入。模式和暂停场景定向测试通过，Debug `gui.stage10_coverage`
与 `gui.browser_page` 组合 CTest `2/2` 通过（31.09 秒）。另修复了直播打开后页签切回
本地时 Presenter 模式值滞后导致模型被刷新回直播的问题。下一步从计划 Task 6 开始，
实现真实 WebView2 初始化、导航和持久 Profile。

计划 Task 6 已于 2026-08-11 完成。新增独立 `mediahub_browser_webview2` 静态目标和
PImpl 公开头，COM、Windows 与 WebView2 SDK 类型只存在于适配器私有实现；
`mediahub_gui_support` 仍不链接 WebView2，GUI 假后端测试也不依赖真实 Runtime。
环境和 Controller 在 GUI STA 异步创建，完成处理器使用共享生命周期令牌、生命周期
序号与请求代次丢弃迟到回调；navigationId 保留每次导航代次，旧导航取消不会覆盖新
页面。脚本和普通上下文菜单开启，DevTools、密码保存和常规自动填充关闭；未使用 Host
Object 或脚本注入。导航、前进后退、刷新停止、物理像素边界、可见性、静音、挂起和
Profile 清除已接入，事件令牌在关闭 Controller 前由 RAII 撤销。

生产组合根注入真实后端，并固定使用
`AppLocalDataLocation/WebView2/Profile-v1`，不复用 Edge 个人 Profile。RED 阶段在
测试目标补齐 Qt Widgets 链接后真实失败于公开后端头不存在；GREEN 阶段使用指定 VS
环境构建 `mediahub_webview2_tests mediahub_gui` 成功。最终 WebView2 定向 CTest
`2/2` 通过（Runtime 探针 0.02 秒、真实初始化 7.16 秒，总用时 7.22 秒），GUI 定向
回归 `2/2` 通过（31.99 秒）。静态扫描确认核心层无 WebView2/COM 命中，GUI 层仅组合
根和抽象提示文字命中；适配器日志只有事件类别和 HRESULT。提交前组合复验覆盖上述
四项 CTest，为 `4/4` 通过（39.65 秒）。下一步从计划 Task 7 开始，实现来源感知的
权限、外部协议、证书和安全下载；Task 8 之后的能力尚未实施。

Task 6 提交后的规格复核补齐了敏感默认行为安全门禁。修复前真实后端没有订阅权限、
下载、证书错误、外部 URI 或新窗口事件，`answer*` 空实现不能阻止 Runtime 默认行为。
现在上述五类事件在 ready 前强制注册并立即 DENY/CANCEL/Handled；下载、证书和外部
URI 分别要求 `ICoreWebView2_4`、`ICoreWebView2_14`、`ICoreWebView2_18`，任一接口或
注册失败都会稳定报告初始化失败、撤销已有事件并关闭 Controller。所有安全事件令牌
均在创建 COM 对象的 GUI STA、Controller Close 前由 RAII 撤销；线程相关私有函数也
已补齐中文调用/回调线程约束。

修复 RED 为结构门禁缺少 `registerPermissionRequested()`，定向测试 `2` 通过、`1`
失败；GREEN 后测试程序内部 `4/4` 通过（6.717 秒、无 warning），最终 Runtime 探针、
真实初始化和两项 GUI 回归组合 CTest `4/4` 通过（36.87 秒）。本阶段没有在缺少 Task 9
环回服务和 Task 7 受控下载目标时触发可能写入用户下载目录的 RED；下载文件、系统权限、
外部应用和证书端到端验证仍明确留在 Task 7/9，不以结构门禁冒充完成。

上述提交又经过代码质量复核并完成收口。挂起/恢复现在由内部 desired、actual、
in-flight 与 request serial 协调器驱动：pending TrySuspend 期间的恢复请求继续保持静音，
挂起成功后只执行一次纠正 Resume，并在恢复成功后才解除静音；HRESULT/BOOL 失败、重复
请求、迟到完成和生命周期失效均有纯行为回归。导航 generation 在 Navigate 成功接受后
进入 FIFO，首次 NavigationStarting 才绑定 navigationId，同 ID 重定向不重复消费；
只有 active ID 的 Completed 能上报，旧 ID 在读取完成状态或快照前即静默退出，Starting
到达前刷新按钮也会正确选择 Stop；清除资料推进 generation 时同步更新历史导航所用的
当前代次。

真实初始化测试现在先用 WebView2 Loader 判定 Runtime 是否存在；本机存在 Runtime 时
必须 Ready 且零错误，不再把任意 InitializationFailed/ProfileUnavailable 当作通过。
五类默认拒绝已提取为可行为测试的生产内部窄策略，fake args 精确验证每个 `put_*` 的
参数和调用次数；源文本仅保留注册函数段、ready gate 与 reset-before-Close 辅助门禁。
注册失败仍未通过可注入真实后端端到端触发，该边界已在测试文档明确记录。

同时修正三项低风险问题：`mediahub_browser_webview2` 以 PUBLIC usage dependency 暴露
`mediahub_gui_support`，WebView2 SDK/COM 仍为 PRIVATE；空或相对 Profile 稳定归类为
ProfileUnavailable，组合根不会对空 AppLocalDataLocation 拼相对路径；默认 NewWindow
拒绝不再误报“登录弹窗数量已达上限”。各轮 RED 分别实际失败于缺少状态协调器、缺少
导航追踪器、缺少默认拒绝策略、公开依赖不可见、Profile 错误分类和误导弹窗通知。

最终 VS x64 双目标构建成功，WebView2 测试程序内部 `13/13` 通过（6.580 秒、零跳过）；
Runtime 探针、GUI 阶段覆盖、浏览器 GUI 与真实初始化组合 CTest `4/4` 通过（35.47 秒）。
下一步仍从计划 Task 7 开始；本轮没有实现权限、下载、外部协议、证书或弹窗交互 UI。

计划 Task 7 已于 2026-08-11 完成。新增来源感知的权限三态窗口和单任务下载条；权限、
外部协议、证书错误和下载都以 requestId 关联当前可见请求，关闭、超时、导航、替换和
页面销毁统一使用安全决定，迟到回答不会影响新请求。下载路径在 GUI 与真实后端两次
校验，只允许父目录存在、目标不存在的绝对新文件；取消保存窗口不会产生默认下载。

真实 WebView2 handler 不再立即完成 Task 6 的窄默认拒绝，而是在先设置 DENY/CANCEL/
Handled 后取得 deferral。四类 pending 同时保存 lifecycle serial、generation 和 requestId，
每个 ID 只取出并完成一次；30 秒有界超时和 shutdown 都逐项拒绝并 Complete。下载持有
operation，订阅字节和状态事件，终态撤销 token 后释放 COM；关闭时取消全部 pending 与
active 下载，不等待 Runtime。权限记住行为按固定 SDK 的
`ICoreWebView2PermissionRequestedEventArgs3` 设置 SavesInProfile，Profile 的
`ALL_PROFILE` 清理后还会异步清除会话证书错误动作。日志没有加入 URL、Profile、标题、
Cookie、凭据或目标文件路径。

本阶段 RED 逐项证明缺少权限窗口、协议/证书确认、下载条、保存取消处理、pending 策略、
deferral handler、证书动作清理、生命周期失效默认拒绝、超大字节进度和导航旧请求隔离；
GREEN 后浏览器 GUI 测试程序 `11/11`（0.230 秒）、WebView2 测试程序 `15/15`
（6.566 秒）。指定 Runtime 探针、浏览器 GUI 与真实初始化 CTest `3/3`（6.98 秒），主 GUI
回归 `1/1`（51.25 秒），三目标构建、diff check、模块边界与敏感日志扫描均通过。

Task 7 提交后规格复核发现下载启动失败分支可能只取消 operation 而遗留 deferral，标准
权限也可能在无效来源、空 listener 或 Args3 不可用时提前返回。现已增加生产内部下载
事务：事件注册、活动任务存储或二次路径校验失败都会撤销 token、设置 Cancel、取消
operation 并 exact-once Complete。标准权限固定先 DENY、取得 base deferral 和 Args2/Args3，
再检查来源与 listener；只要存在 deferral，拒绝路径就会完成一次。

固定 SDK 的屏幕捕获不在 PermissionKind 枚举中，但由 `ICoreWebView2_27` 的独立主 WebView
`ScreenCaptureStarting` 事件提供。该事件现在是 Ready 的必需安全门禁；来源只从
OriginalSourceFrameInfo 规范化，AllowOnce 才解除 Cancel，Deny 与 RememberForOrigin 都
保持拒绝。GUI 禁用屏幕捕获记住按钮并说明限制。清除资料会在 generation 递增前拒绝旧
权限、协议、证书和未选路径下载；旧权限按钮/timeout、旧下载更新不会影响新请求，已提交
路径的 active 下载在普通导航后继续。

规格修复后的最终 fresh 复验中，浏览器 GUI 测试程序内部 `15/15` 通过（0.118 秒），
WebView2 测试程序内部 `18/18` 通过（6.606 秒）；Runtime 探针、浏览器 GUI 与真实初始化
组合 CTest `3/3` 通过（6.92 秒），主 GUI 回归 `1/1` 通过（50.60 秒）。

第二轮质量审查继续修正五项安全完成路径。DownloadStarting 的 Cancel、Handled、operation
和 deferral 获取现在彼此独立，证书与外部协议也不会因安全 setter 失败而跳过 deferral；
所有已取得对象都会执行可行的 Cancel/Complete 并保留首个 HRESULT。活动下载取消进入
Cancelling，CancelFailed 可以重试，只有 WebView2 终态才释放单任务槽位；shutdown 始终
再次取消。下载快照先读 State，进度 getter 失败不会阻止终态投递和事件 token 释放。

清除 Profile 和证书动作后，真实后端会请求内部 `about:blank`，只有匹配 navigationId
成功完成才报告清除成功；旧页和其他导航不会改写普通地址或标题。外部协议确认现在分别
显示规范化来源和目标，并说明由 Windows 默认应用处理。`CON.foo.bar`、`NUL.anything`
等保留设备名按首个 `.` 前段拒绝。下载目标的第二次检查只提供静态防护；WebView2 直接
写最终路径无法消除并发同名创建或 junction 替换的 TOCTOU，本地自用威胁模型暂列已知
残余，Task 9 再做受控实测，不声称原子保证。

本轮最终三目标构建成功；浏览器 GUI 测试程序内部 `19/19`、WebView2 测试程序内部
`23/23` 通过。Runtime 探针、浏览器 GUI 与真实初始化组合 CTest `3/3` 通过（6.81 秒），
主 GUI 回归 `1/1` 通过（28.94 秒）；Runtime 151 仍在默认 Windows 平台真实初始化。

第三轮复核又修正待选路径下载的取消状态闭环。此前 pending 分支在移出 store 后直接完成
Cancel，未注册活动下载事件，因此成功取消后 GUI 会永久停在 Cancelling；同步失败时也不
上报 CancelFailed，operation 已释放而无法重试或由 shutdown 保底。现在 pending 取消先
尝试注册终态事件并存入 active，再 exact-once 设置 args Cancel、调用 operation Cancel 和
完成 deferral。可观察且收口成功时只等待真实终态；收口失败时 active 保留、恢复可重试
状态并投递 CancelFailed，之后只重试 operation。

注册或 active 存储失败会撤销已有 token：取消成功时直接投递一次 Cancelled；取消失败时
把无订阅 operation 保留在独立 retry 集合，供同一 requestId 重试并由 shutdown 再次取消。
生产内部事务测试覆盖正常顺序、args/operation/deferral 各自失败、注册失败和存储失败；
GUI 回归明确覆盖尚未提交路径时保持 Cancelling、拒绝新任务、后端确认 Cancelled 后释放
槽位以及 CancelFailed 后重试。

本修复 fresh 验证中三目标构建成功；浏览器 GUI 测试程序内部 `19/19`、WebView2 测试
程序内部 `26/26` 通过（WebView2 6.536 秒、零跳过）。Runtime 探针、浏览器 GUI 与真实
初始化组合 CTest `3/3` 通过（6.83 秒），主 GUI 回归 `1/1` 通过（28.98 秒）。模块边界与
敏感日志门禁保持通过；真实文件下载仍等待 Task 9 受控环境或人工验收。本修复不进入
Task 8，后续阶段边界仍是登录弹窗子窗口、网页全屏和完整生命周期。

真实设备权限、真实页面屏幕捕获、真实文件下载写入、外部应用启动和自签名 HTTPS 仍等待
Task 9 环回站点或人工验收；当前自动化只覆盖安全事件注册、决策和生命周期完成路径，不
虚构端到端副作用覆盖。Task 8 的登录弹窗子窗口、网页全屏和完整生命周期能力尚未开始。
下一步严格从计划 Task 8 开始，不进入 Task 9 的完整环回站点或持久数据实现。

计划 Task 8 已于 2026-08-12 完成。NewWindowRequested 现在先设置 Handled 并取得
deferral，再用同一 Environment 和显式相同 Profile 名称异步创建受控原生登录子窗口；
主页面与嵌套弹窗共用最多 3 个窗口的容量协调器，创建失败、用户关闭和应用关闭都会释放
预约。子窗口在交给 Runtime 前已注册权限、屏幕捕获、下载、证书、外部协议和嵌套弹窗
处理器；当前统一默认拒绝这些敏感动作，避免绕过主页面确认，完整弹窗内交互留给 Task 9。

网页 Fullscreen API 事件会隐藏网页工具栏、信息行、下载条以及 MediaHub 页头、模式入口、
侧栏和菜单，并在退出时恢复进入前的窗口状态与各控件可见性。`Esc` 优先请求网页退出
全屏；切出网页固定执行静音、挂起、隐藏，切回固定执行显示、恢复、请求解除静音。关闭
时先处理未决安全请求和下载，再断开 GUI listener；仍在网页全屏时请求退出，随后同步关闭
全部登录子窗口，最后关闭主后端。子窗口事件 token 先于子 Controller 撤销，全部子窗口又
先于主 Controller、Environment 和 COM 引用释放；迟到回调由弱生命周期令牌和 serial 丢弃。

本阶段各项 RED 分别证明缺少网页全屏 Esc、弹窗容量协调器、关闭命令、全屏工具栏隐藏、
主窗口全屏联动、弹窗安全 handler、全屏中新下载隐藏、弹窗事务和 deferral 失败完成路径。
GREEN 后三目标 fresh 构建成功；浏览器 GUI 测试程序内部 `23/23`、WebView2 测试程序内部
`28/28` 通过（WebView2 6.501 秒、零跳过）。`gui.stage10_coverage`、`gui.browser_page` 和
`integration.webview2.initialization` 组合 CTest `3/3` 通过（30.30 秒）；`git diff --check`、
模块链接边界和敏感日志扫描通过。真实 OAuth/SSO、真实网页全屏、Windows DPI、弹窗内
敏感操作提示和持久会话仍属于 Task 9/人工验收；下一步从计划 Task 9 开始。

Task 8 提交后的规格复核又修正三项关闭竞态。后端新增永久生命周期门禁：首次及 shutdown
前的明确 initialize 仍可用，但一旦调用 shutdown，后续 initialize 会无副作用返回，不能
重新创建 Environment 或 Controller。主 Controller 的异步完成若发现弱生命周期失效、
serial 或 generation 已过期，会对 Runtime 返回的非空 Controller 先调用一次 Close，再
丢弃回调；有效完成不会提前关闭 Controller。

关闭释放不再调用带 `isShuttingDown`/Ready 门禁的公开全屏退出接口，而是通过专用无等待
事务实际提交幂等的 `document.exitFullscreen()` 请求。之后固定关闭 popup、撤销主 WebView
事件 token，最后 Close 主 Controller；脚本完成仍用弱生命周期和 serial 丢弃，关闭不等待。
真实 child HWND 的 Esc 路由没有在本修复扩展，仍按 Task 9/人工验收边界处理。

三项 RED 分别实际失败于缺少 `BrowserLifecycleGate`、缺少主 Controller 完成事务，以及
`releaseBrowserResources` 中不存在可达的 shutdown 全屏退出调用；结构 RED 为 `2/3`
通过并准确失败于全屏退出标记缺失。GREEN 后组合定向测试 `5/5` 通过。最终三目标构建
成功，浏览器 GUI `23/23`（0.401 秒）、WebView2 `31/31`（6.593 秒、零跳过），规定 CTest
`3/3` 通过（30.30 秒）。

Task 8 的代码质量复核随后修正三项 Important。第一，主页面和 popup 的 Controller 创建
完成原先只区分迟到回调，当前生命周期若收到失败 HRESULT 和非空 Controller，既不接管
也不 Close；现在完成入口由 adoption transaction 守卫，只有“当前生命周期、成功状态、
非空 Controller”可以接管，迟到、失败和其他未接管的非空 Controller 都会 Close 一次。
对应 RED 先编译失败于缺少事务类型，接线门禁再得到 `2 passed, 1 failed`，GREEN 后定向
`3/3` 通过。

第二，popup 的权限、屏幕捕获、下载、证书和外部协议拒绝 setter 失败时，旧 handler 会
丢弃 HRESULT 并继续返回 `S_OK`。这五个 handler 保持同步 setter，不取得 deferral；最终
实现会尝试该动作所需的全部 setter 并保留首错，任一拒绝写入失败都会在 handler 返回前
同步进入 `closeInternal(true)`，立即失效 popup 生命周期、撤销事件、Close Controller 并
销毁宿主窗口；owner 仍在当前回调栈退出后延迟 erase 对象。RED 先编译失败于缺少屏幕
捕获取消和统一完成 helper，接线门禁再得到 `2 passed, 1 failed`；额外同步关闭审查 RED
准确失败于旧 helper 调用约定，GREEN 后单项 `3/3` 通过。

第三，同步 popup 创建失败原先既通知 owner 排队回收，又由调用方立即 erase，旧裸地址
回调可能在地址复用后误删新 popup。现在同步创建失败只执行 `closeInternal(false)`，由
调用方唯一立即回收；异步完成或配置失败才执行 `closeInternal(true)`，由 owner 唯一延迟
回收。RED 先编译失败于缺少失败时序类型和单一回收 helper，接线门禁再得到
`2 passed, 1 failed`；GREEN 行为测试覆盖地址复用、容量释放和异步 exact-once 回收。

质量修复后的 fresh 三目标构建成功；三项组合定向测试 `5/5` 通过（11 ms），浏览器 GUI
测试程序 `23/23`（0.416 秒），WebView2 测试程序在默认 Windows 平台 `33/33`
（6.558 秒、零跳过）。规定的 `gui.stage10_coverage`、`gui.browser_page` 和
`integration.webview2.initialization` 组合 CTest `3/3` 通过（29.81 秒）；diff check、
模块边界和新增日志扫描通过。程序化离开 Web 模式尚未显式退出网页全屏，真实 child HWND
获得焦点后的 `Esc` 路由也未做端到端覆盖；两项继续作为 Task 9/人工验收边界，不在本轮
扩展。

计划 Task 9 已于 2026-08-12 完成。新增测试专用 IPv4 loopback HTTP 服务，只监听
`127.0.0.1` 随机端口，并在运行时提供持久 Cookie、LocalStorage、IndexedDB、重定向、
共享 Profile 弹窗、麦克风请求、下载、上传控件、静音 WAV、全屏入口和外部协议页面。
全部 Profile 与下载目标均位于临时目录，不访问公网、Edge Profile 或用户下载目录。
质量复核又把 WebView2 Profile 默认下载目录固定到当前 Profile 的 `Downloads` 子目录，
并在 Ready 前完成目录创建与 SDK 设置；失败会阻止 Ready，用户明确选择的下载路径不变。
取消 Runtime 用例同时断言该隔离目录为空，避免测试自身回退写入用户 Downloads。
测试 Profile 由 RAII 首次清理，套件结束对本轮未清项目再做一次有界清理；失败只报告
数量并令测试失败，不输出目录。上传、全屏与外部协议自动化只证明受控入口页面可加载。

TDD 首次有效 RED 在 VS x64 环境准确失败于 `support/local_web_test_server.h` 缺失。
实现后的完整 Runtime 首轮为 `40/41`；固定分类诊断得到
`cookie=0;local=1;indexed=1`，确认测试写入的是会话 Cookie，而不是产品 Profile flush
问题。只为测试 Cookie 增加 `Max-Age=3600` 后，持久与清除定向测试 `3/3`（1.641 秒）。
最终普通 PowerShell 默认 Windows 平台 WebView2 程序内部 `41/41` 通过（9.399 秒、零
跳过），覆盖资料跨后端持久和清除、重定向、弹窗 Profile 共享、麦克风拒绝、受控下载
写入/取消，以及上传、媒体、全屏和外部协议受控页面加载。

非 Runtime 纯逻辑与本地服务组合为 `33/33`（61 ms）；Debug 规定
`gui.stage10_coverage`、`gui.browser_page`、`integration.webview2.initialization`
组合 CTest 安全加固后为 `3/3`（47.27 秒）。Codex 受限环境曾令 GPU 子进程以 `0xC0000022`
退出并阻止 Renderer 启动；临时 ProcessFailed 诊断已移除，Runtime 结论采用普通
PowerShell 的 `41/41` 证据。

提交前在 Codex 沙箱单独运行
`ctest --test-dir cmake-build-debug -R "webview2" --output-on-failure` 时，Runtime probe
通过，`integration.webview2.initialization` 在 90.04 秒 CTest 上限超时，实际结果为
`1/2`，未通过。该结果只记录已确认的沙箱 WebView2 子进程限制，不视为产品缺陷，也不
覆盖普通 PowerShell 完整程序 `41/41`（9.399 秒、零跳过）与规定组合 CTest `3/3`
（47.27 秒）的产品门禁。

Task 9 不替代人工边界：真实 OAuth/SSO 和公共站点、DRM、网页音画质量、实际 Windows
DPI、系统上传选择器、用户手势进入全屏及 child HWND 的 `Esc`、外部应用和自签名 HTTPS
仍待后续人工验收。下载检查后创建仍有同名文件与 junction 替换 TOCTOU 残余，不宣称
原子保证。下一步严格从计划 Task 10 开始响应式视觉、菜单和快捷键帮助；本提交不进入
Task 10。

计划 Task 10 已完成实现并进入最终验证。BrowserPage 按自身可用宽度在 900、1200、1500
像素切换 `compact`、`normal`、`large`、`extraLarge`，只在档位变化时刷新工具栏关键控件
样式；地址栏保持至少 180 像素。主窗口继续使用既有宽高联合断点保护播放器控制行，网页
页独立按宽度分档。主题新增网页地址栏字体档位，960x640、1200x800、1600x1000 的
offscreen 测试同时验证属性、实际字体、工具栏边界和控件可见性。

网页模式新增 `Alt+左键`/`Alt+右键` 后退前进，输入框获得焦点时仍可用；既有 Ctrl+L、
Ctrl+R、F5 和 Esc 行为不变。快捷键帮助现在准确区分网页与直播模式，不再把 Ctrl+L 只
描述为打开网络地址，也明确 Esc 优先退出网页全屏。主窗口会在网页模式禁用原 Ctrl+L
菜单动作，防止与地址栏快捷键冲突；该行为的追加 RED 准确出现 Qt “Ambiguous shortcut”
警告且地址栏未选中。首轮有效 RED 为浏览器 GUI `23/25`，准确失败于缺少 responsiveSize
与 Alt 后退路由；帮助内容精确断言属于实现后补强，不虚记 RED。
GREEN 后浏览器 GUI `25/25`（0.405 秒），主 GUI `95/95`（88.745 秒）。实际 Windows DPI
观感仍属于人工验收，offscreen/环境缩放回归不替代该边界。最终 CTest 与提交信息待回填。

最终 fresh 三目标构建成功；增加 Ctrl+L 冲突回归后的浏览器 GUI `26/26`（0.659 秒），
主 GUI `95/95`（29.610 秒）。`gui.stage10_coverage`、`gui.v03_scale_125`、`gui.v03_scale_150` 和
`gui.browser_page` 组合 `4/4` 通过（139.02 秒）。Task 9 回归中两项 GUI `2/2` 通过，
`integration.webview2.initialization` 在 Codex 沙箱 90.03 秒超时，组合实际 `2/3`；该结果
与 Task 9 已记录的 WebView2 子进程沙箱限制一致，不改产品代码、不假记为通过。

Task 10 提交后的规格补强将三档响应式测试扩展到网页工具栏全部七个关键控件。每档均核对
页面实际尺寸、控件可见且几何非空、映射到工具栏后的矩形完全包含，以及地址栏实际宽度
不小于 180、按钮实际宽度不小于自身 `sizeHint()`。强化测试在现有实现首次运行即 `3/3`
通过（107 毫秒），准确记录为实现后规格补强而非独立 RED；Task 10 前旧实现仍由已有的
空 `responsiveSize` 断言提供有效 RED。本补强不修改生产布局。

Task 10 质量审查发现 Qt 快捷键只覆盖 Qt 控件焦点，WebView2 原生 child HWND 获得焦点时
没有稳定路由。现已新增不暴露 Windows 类型的 `BrowserAccelerator` 事件边界；Controller
在 Ready 前注册 `AcceleratorKeyPressed`，识别 Ctrl+L、Alt+左/右、Ctrl+R、F5，以及仅
网页全屏时的 Esc。只处理首次 KEY_DOWN/SYSTEM_KEY_DOWN；重复、KeyUp、未知或附加
Shift/Win 修饰不吞。读取失败不设置 Handled；Handled 写入失败会尝试恢复 FALSE，且不会
向 GUI 投递。事件 token 在 Controller Close 前撤销，迟到事件继续受生命周期和 generation
门禁保护。策略、失败语义、生产接线和 BrowserPage 当前代次路由均有自动化；真实 child
HWND 的物理按键仍需普通 Windows Runtime/人工证据，Qt 输入框测试不再冒充该覆盖。

同时把浏览器 compact 主题选择器限定到 `QFrame#browserToolbar`，并新增 800x600 compact
布局回归，继续对七个控件执行完整几何和 sizeHint 门禁。首个有效 RED 为缺少
`webview2_accelerator.h` 导致 WebView2 测试编译失败；最小实现后 accelerator 定向 `5/5`、
compact 定向 `3/3`、GUI 稳定事件路由 `3/3` 通过。最终验证与独立提交待回填。

Fresh 四目标构建成功；浏览器 GUI `27/27`（0.703 秒）、主 GUI `95/95`（56.095 秒）、
WebView2 非 Runtime 纯行为与接线 `36/36`（63 毫秒）。四项 GUI CTest `4/4` 通过：
58.25、60.52、75.64 和 0.81 秒，总计 195.26 秒。Codex 沙箱的完整 WebView2 程序再次
表现为 Renderer 无结果，外层 120 秒超时前至少六项 Runtime 行为失败，未产生 Totals；
该结果不算通过，也没有重复运行。新增 Accelerator 注册仍需要普通 PowerShell 完整程序
`44/44` 证明真实 Runtime 能进入 Ready。用户随后在普通 PowerShell 执行同一 fresh 构建
产物，WebView2 完整程序 `44/44` 通过（12.021 秒、零跳过）；新增映射、失败语义和生产
接线三项以及 Task 9 全部 Runtime 行为均通过。该证据与沙箱 Renderer 失败明确区分，
满足提交门禁。

计划 Task 11 已完成发布规则、静态门禁、构建矩阵和隔离包校验，并已取得 Release 普通
环境 Runtime 证据。TDD RED 在受控假包准确失败于缺少 `licenses/WebView2-LICENSE.txt`；
GREEN 后固定 SDK 的 LICENSE 与 NOTICE 分别安装为 `WebView2-LICENSE.txt`、
`WebView2-NOTICE.txt`，第三方声明明确静态链接 x64 Loader，不发布任何
`WebView2Loader.dll`，Evergreen Runtime 也不打包。校验器同时拒绝 Profile、Cache、Cookie、
x86/ARM64 Loader、测试/probe、库文件和构建缓存，并兼容计划旧变量 `PACKAGE_ROOT`。真实
包首次校验发现 VLC 两个合法 `libcache_*` 插件的假阳性，收窄为浏览器资料路径段后，禁入
夹具保持失败且正式包通过。

静态扫描确认 core 零 Qt/libVLC/WebView2/COM/Windows 类型，`mediahub_gui_support` 零
libVLC/WebView2 链接。生产 WebView2 日志仍只有固定事件名与 HRESULT；完整 URL、Profile
路径、Cookie、Authorization、password、token 和下载目标均未进入日志。测试命中来自固定
`example.test`、IPv4 loopback/Cookie 持久性夹具与敏感值不入日志的反向断言。

Debug 和启用发布安装规则的 Release 完整构建均成功。Codex 沙箱全量 CTest 分别为
`80/81`（233.30 秒）和 `80/81`（157.17 秒），唯一失败都是
`integration.webview2.initialization` 在 90.04/90.03 秒超时；其余 80 项全部通过，未把
环境限制记作全绿，也未重复运行。普通环境 Debug WebView2 为 `44/44`（12.021 秒、零
跳过），普通环境 Release WebView2 为 `44/44`（11.276 秒、零失败、零跳过），Task 9/10
Runtime 场景均通过。

原 `cmake-build-core-only` 的 Visual Studio 17 缓存与本轮 Ninja 生成器冲突，目录保持原样。
本轮使用被忽略的 `cmake-build-core-only-v06`，构建 22 步并通过 `52/52`（7.43 秒）；新建
`cmake-build-release-no-tests`，关闭两个测试开关后构建 46 步成功，CTest 为 0。两次确认
精确 dist 目标不存在后，安装到 `dist/MediaHub-0.6.0-win64`；正式校验为 400 个文件、
365 个 VLC 插件、六项法律材料，禁入项 0。Task 11 不启动窗口、不进入 Task 12 人工验收。

Task 11 提交后的规格审查已用两个 TDD 循环收紧门禁。单配置 Debug + package-ON 的新目录
先错误配置成功；GREEN 后要求 `CMAKE_BUILD_TYPE` 严格等于 Release。多配置改在 install
阶段检查 `CMAKE_INSTALL_CONFIG_NAME`，Ninja Multi-Config 的 Debug 安装在复制文件前准确
失败，Release package-ON 刷新、构建和安装保持成功，package-OFF 不受影响。

受控假包中的 `DawnWebGPUCache`、`DawnGraphiteCache`、`GrShaderCache` 初始均被错误放过。
实现后复验还发现原正则被 CMake list 分号截断，使 ShaderCache/Cookie 分支不可达；现用
`string(CONCAT)` 形成单个正则，并逐项拒绝三类新增缓存及 Profile-v1、Cache、Code Cache、
GPUCache、ShaderCache、Cookie、Cookies。规则仍允许 VLC 两个合法 `libcache_*` 插件。
正式 Release 重装和校验数字不变：400 个文件、365 个 VLC 插件、六项法律材料。

Task 11 第二轮质量审查已完成变量、文件类型和 reparse 门禁。两个包目录变量现在分别忽略
空值并绝对规范化：一个非空时使用它、两个指向同目录时允许、两个不同目录或双空时失败，
空字符串不再回退到当前工作目录。required 项必须是非目录、非链接的普通路径；唯一允许
的 exe 是大小写不敏感但相对路径严格等于顶层 `MediaHub.exe`，嵌套同名 exe 会被拒绝。

本机无符号链接创建特权，file/dir symlink 均未生成；directory junction 可由受控
`mklink /J` 无提权创建。CMake 4.4 对该 junction 实测 `IS_SYMLINK=TRUE`，并能通过
`REAL_PATH` 解析目标。旧 validator 会放过 junction，修复后在遍历内容前拒绝；回归移除
链接后目标目录仍存在。最终原十六类禁入夹具全部失败，VLC 合法 cache 插件通过；Release
刷新、构建、重装和双变量同目录校验均成功，仍为 400 个文件、365 个 VLC 插件、六项法律
材料，包内 reparse 项为 0。

Task 11 等价路径复核补齐尾分隔符语义。CMake 4.4 会把 `path/.` 保留为带尾 `/` 的规范路径，
旧字符串比较因此把它与 `path` 误判为不同目录；直接 `path/` 和 Windows 反斜杠表示原本
已经通过。现在访问路径保持绝对 canonical 形式，比较键统一目录尾分隔符且不破坏驱动器或
UNC 根。三种表示均通过，不同目录、双空和缺失驱动器根仍准确失败，junction fresh 拒绝；
正式包别名加 `/.` 后 fresh 校验数字不变。required、嵌套 exe、十六类禁入项与 VLC 例外
本轮未改，沿用上一轮逐项 fresh 证据；本轮重建与清理 ignored 临时夹具因自动审批基础设施
模型不可用而未执行，不宣称本轮重跑。

Task 11 已完成，最终修正提交为 `6169699 fix: 统一发布包等价目录路径`，已普通快进
推送到 `origin/main`；该提交之后由 Task 12 继续完成收口。Task 12 自动化证据与文档
收口已经完成，`docs/测试/28-v0.6-内置网页浏览器测试.md` 已按规划原始语义建立 W-01 至 W-18
分层证据矩阵：纯自动化可完整证明的编号记为“自动化通过”，含真实账号、音画、直播、
系统交互、实际 DPI 或进程观察语义的编号记为“自动化部分通过，整项待人工”。W-M01
至 W-M18 全部保持待用户人工验收。浏览器 GUI
`27/27`、主 GUI `95/95`、普通环境 Debug WebView2 `44/44`（12.021 秒）、四项 GUI
CTest `4/4`，以及普通环境 Release WebView2 `44/44`（11.276 秒，零失败、零跳过）
均引用 Task 10/Task 11 已取得证据。

沙箱 Debug/Release 全量仍分别为 `80/81`（233.30 秒）和 `80/81`（157.17 秒），唯一
失败为 `integration.webview2.initialization` 在 90.04/90.03 秒超时，属于已确认的沙箱
WebView2 子进程限制，不能写成全绿。Core-only 为 `52/52`（7.43 秒），无测试 Release
为 0 项；`dist/MediaHub-0.6.0-win64` 的既有校验为 400 个文件、365 个 VLC 插件、六项
法律材料、0 reparse、0 禁入项，架构边界与敏感日志扫描零违规。

Task 12 把 CMake 项目版本元数据由 `0.5.0` 更新为 `0.6.0` 后，已在普通 Windows
环境 fresh 运行构建矩阵。Release 和 Debug 都因版本变化重新配置、生成成功并构建退出
`0`；沙箱外全量 CTest 分别为 Release `81/81`（103.20 秒，
`integration.webview2.initialization` 11.97 秒）和 Debug `81/81`（226.09 秒，
该集成测试 12.74 秒），均为 100% 通过。因此 W-18 现为“自动化通过”，自动化矩阵为
6 项通过、12 项部分通过且整项待人工。上述 fresh 证据不改写 Task 11 的沙箱历史
`80/81`，也不代表 W-M 人工验收完成。

**下一步边界**：先由用户使用最新 Release 逐项执行 W-M01 至 W-M18，重点覆盖真实账号
与二次验证、公共网站导航、网页视频与直播音画、DRM/网站兼容性、真实弹窗会话、系统
下载/上传选择器、实际 Windows 100%/125%/150% DPI、外部应用、自签名 HTTPS 和含视频
弹窗时的退出观察。验收不得在仓库记录完整 URL、账号、Cookie、Profile 路径、凭据或
私人截图。人工矩阵全部取得证据前，v0.6 只能称为“自动化交付候选完成、人工验收待执行”，
不得标记为全部完成。Task 12 提交与推送状态以本节后的实际 Git 状态为准。

## 13. 待决事项

| 事项 | 需要决定的时机 |
|---|---|
| 项目自身源码许可证 | 对外分发前 |
| 是否配置 `.clang-format` | 阶段 2 之后 |

## 14. 协作约定

1. 每个增量开始前先说明目标、文件范围和验收方式。
2. 单次尽量只改 1～3 个文件。

## 15. 内置浏览器选项卡增量（2026-08-12）

用户已确认把内置浏览器完整页架构设为当前开发目标，网页功能与 MediaHub 本地媒体、
直播播放链路保持独立。设计文档为
`docs/设计/06-内置浏览器选项卡与资料持久化设计.md`，实施计划为
`docs/superpowers/plans/2026-08-12-browser-tabs-history.md`。

### 阶段一已完成

- 地址栏始终保持可编辑；首次点击或获得焦点时全选旧地址，`Ctrl+L` 仍可全选。
- 地址提交继续复用当前网页页，不创建新窗口。
- 成功 HTTP/HTTPS 导航写入浏览器专用历史，按 URL 去重并更新标题/时间。
- 新增 `BrowserDataStore` 与 `QSettingsBrowserDataStore`，当前只保存历史，不接入
  播放器 `AppStateSnapshot`，不写入密码、Cookie、Token 或 Profile 内容。
- 阶段测试文档为 `docs/测试/29-浏览器选项卡增量01测试.md`。
- VS x64 构建 `mediahub_browser_gui_tests`、`mediahub_gui_tests`、`mediahub_gui` 成功；
  `gui.browser_page` 与 `gui.stage10_coverage` 定向 CTest 通过（`2/2`），新增历史
  跨实例和地址栏行为断言通过。

### 下一步边界

阶段二只处理网页模块内的 `QTabBar`/标签控制器、共享 WebView2 Environment/Profile、
`window.open` 转新标签、标签独立导航状态和最后一个标签保留空白页；不修改播放器、
直播清单或现有主窗口持久化模型。阶段三再加入收藏夹和完整 Profile 清除类别。
3. 改动完成后先展示 diff、构建结果和设计理由。
4. 阶段测试和文档通过后自动提交；远程无冲突且认证可用时自动推送并继续下一阶段。
5. 推送失败时保留本地提交并记录原因，禁止强制覆盖远端，不扩展计划外功能。
6. 如果实现暴露出设计问题，先回到文档修正规则，再继续编码。
